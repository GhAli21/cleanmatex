"use client";

/**
 * Hook to get the current locale
 * Re-exports useLocale from useRTL for consistency
 * This ensures all locale hooks use the same source
 */
export { useLocale } from '@/lib/hooks/useRTL';

