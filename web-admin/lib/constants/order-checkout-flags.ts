/**
 * New-order checkout toggles.
 *
 * Promo codes and gift cards are temporarily disabled in the create-order flow until
 * gifts/promotions features are fully implemented. Set to false to re-enable UI + API fields.
 */
export const NEW_ORDER_PROMO_GIFT_DISABLED = true;
