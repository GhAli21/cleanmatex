import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const primary = Color(0xFF1A56DB);
  static const primaryDark = Color(0xFF7EA7FF);

  // Page background — warm light grey
  static const background = Color(0xFFF5F7FA);
  static const backgroundDark = Color(0xFF0D1117);

  // Card and input surface — pure white
  static const cardSurface = Color(0xFFFFFFFF);
  static const cardSurfaceDark = Color(0xFF161B27);

  static const surface = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFF101521);

  static const textPrimary = Color(0xFF101828);
  static const textPrimaryDark = Color(0xFFF3F6FC);
  static const textMuted = Color(0xFF667085);
  static const textMutedDark = Color(0xFF98A2B3);
  static const success = Color(0xFF0E9F6E);
  static const warning = Color(0xFFF79009);
  static const error = Color(0xFFD92D20);
  static const border = Color(0xFFE4E7EC);
  static const borderDark = Color(0xFF2D3748);
}
