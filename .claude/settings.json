{
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); model=$(echo \"$input\" | jq -r '.model.display_name'); remaining=$(echo \"$input\" | jq -r '.context_window.remaining_percentage // 100'); echo \"$model | Context: $remaining%\""
  },
  "permissions": {
    "allow": [
      "Bash(npm run build *)",
      "Bash(npm run check:i18n *)",
      "Bash(npm run typecheck *)",
      "Bash(npm run lint *)",
      "Bash(supabase --version)",
      "Bash(supabase status *)",
      "Bash(supabase migration list *)",
      "Bash(docker info)",
      "Bash(docker ps *)",
      "Bash(docker logs *)"
    ]
  }
}
