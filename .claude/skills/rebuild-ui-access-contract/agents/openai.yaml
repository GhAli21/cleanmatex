display_name: Rebuild UI Access Contract
short_description: Roll out or repair contract-first route, action, and linked API access for web-admin, including inspector and docs.
default_prompt: Rebuild or fix the web-admin page access contracts for a specific dashboard route path or for all routes, including gated actions, linked API dependencies, inspector alignment, and the contract-aligned UI/API permissions docs.
