# Current status — Workflow Order Advance

| Field | Value |
|-------|--------|
| Phase | **V1.0 release hardening reopened** |
| Status | Engine cutover, public tracking, auditable counter pickup, delivery proof/audit, warning/override gate decisions, compiled POD method lists, and the read-only Workboard are implemented. Processing, Assembly, QA, Packing, and Ready/Release use versioned stage-owned APIs; Ready Details presents make-available, remaining collection, and confirm-pickup in one panel. Floor worklists use `workflow_screen` membership from each order's compiled artifact or the live catalog for unsnapshotted historic orders. Profile-stamped orders without a compiled artifact fail closed and are not listed through a graph pin. Automated semantic-profile assurance covers Pilot/PUBLISHED, forged screen/channel, snapshot integrity, and `PROFILE_*` HTTP 409 mapping. Staff delivery completion uses the atomic stop command (`POD` + stop + route + `CONFIRM_DELIVERY`); compiled profiles may name independent optional signature, photo, POD, and notes methods and the method dropdown is filtered by that compiled evidence. Pickup completion enforces required compiled pickup notes. Local DB tests cover collection blocking, tenant isolation, OTP reject, already-delivered, engine-failure rollback, happy path, stale-version rollback, idempotent replay, and serialized dual-complete. Complete requires `delivery:pod` plus `orders:transition`. Legacy capturePOD/route writers remain `503`; generic `/actions` and `/transition` reject staff `CONFIRM_DELIVERY`. Staff POD delivery smoke **S10** is not signed off. Semantic orders resolve one immutable artifact at creation. `CREDIT_INVOICE` delegates to the isolated B2B payment-hold seam. Durable B2B credit/invoice policy remains a separate B2B feature. |
| Date | 2026-08-22 |
| Version | [version.txt](version.txt) → `0.4.15-p7r-profile-assurance` |
| Scope ADR | [ADR_SCOPE_AND_CORRECTION_PASS.md](ADR_SCOPE_AND_CORRECTION_PASS.md) |
| Checkpoint | [OVERNIGHT_CHECKPOINT.md](OVERNIGHT_CHECKPOINT.md) |
| Writers | [WRITER_INVENTORY.md](WRITER_INVENTORY.md) |
| Next (engineering) | Database RLS/ledger integration tests, historic no-legacy cutover, then operator/e2e canary for staff POD delivery smoke **S10** (`p7-harden`). Durable B2B credit/invoice policy remains a separate B2B feature behind `evaluateB2BFulfilmentPaymentHold`. |
| Next (operator) | Review and apply `0455_workboard_permission_navigation.sql` if not already applied, then verify Workboard visibility. Continue direct/staged counter-handover, pay-on-collection, and public-tracking smoke. Do not treat staff S10 as accepted until the explicit rollout decision. In-flight orders that have `wf_profile_id`/`wf_version_no` but no compiled artifact identity will fail closed until they complete on a legacy path or are recreated under a compiled profile. |
| Next (product) | V1.1 return sub-order / projection follow-up after V1.0 acceptance |
| Ready for production canary | **No for full V1.0** — public confirmation is green; staff S10 delivery canary is not signed off |
