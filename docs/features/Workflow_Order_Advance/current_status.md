# Current status — Workflow Order Advance

| Field | Value |
|-------|--------|
| Phase | **V1.0 release hardening reopened** |
| Status | Engine cutover, public tracking, auditable counter pickup, delivery proof/audit, warning/override gate decisions, compiled POD method lists, and the read-only Workboard are implemented. Processing, Assembly, QA, Packing, and Ready/Release use versioned stage-owned APIs; Ready Details presents make-available, remaining collection, and confirm-pickup in one panel. Delivery Details now follows the same ActionBar + stage-owned complete pattern: `/dashboard/delivery/{id}` confirms delivery from the order when no stop exists, or uses the stop proof panel when a pending/in-transit stop exists. No dummy route is created. Simple vs routed is HQ profile policy (`delivery_stop_active` / POD evidence on `CONFIRM_DELIVERY`), not a new catalog seed. The semantic-only code cutover is complete: engine, workflow-context, Workboard, floor worklists, client transition hook, and new-order creation use only the immutable order artifact; unsnapshotted orders fail closed for operational actions and queues. Migration `0464_require_semantic_order_snapshots.sql` is applied locally and remotely and generated types are refreshed. HQ effective preview and tenant-context simulation read the same exact artifact, while legacy HQ authoring writes return `LEGACY_WORKFLOW_RETIRED`. Compiled profiles may name independent optional signature, photo, POD, and notes methods and the stop method dropdown is filtered by that compiled evidence. Pickup completion enforces required compiled pickup notes. Local DB tests cover collection blocking, tenant isolation, OTP reject, already-delivered, engine-failure rollback, happy path, stale-version rollback, idempotent replay, and serialized dual-complete — **re-verified 2026-09-05** after fixing a stale test fixture (seeded orders had no `wf_profile_id`/`wf_version_no`/`wf_profile_version_id`, so 4/9 had been silently failing closed since Gate 4; not a service bug). All 9/9 pass against `WF_V2_ROUTED_POD` v1. Order-keyed floor complete unit tests cover collection blocking, refusing an existing stop, notes-only happy path, and rejecting compiled photo proof without a stop. Complete requires `delivery:pod` plus `orders:transition`. Legacy capturePOD/route writers remain `503`; generic `/actions` and `/transition` reject staff `CONFIRM_DELIVERY`. Staff POD delivery smoke **S10 is signed off (2026-09-05)** — real operator, real UI, `ORD-20260903-0005` on `WF_V2_SIMPLE` v4, verified atomically on remote (order/stop/route/history). Seeded via reviewed one-off migration `xz0490_s10_canary_route_seed.sql` (applied, then archived per repo convention) since `STAFF_DELIVERY_WRITES_ENABLED=false` still blocks route creation through the normal path — **note**: migration number `0490` was reused the same day for the unrelated Delivery Feature Completion programme's schema (`0490_org_drivers_mst_and_route_safety.sql`); the S10 seed is the archived file, not the active one. `CREDIT_INVOICE` delegates to the isolated B2B payment-hold seam. Durable B2B credit/invoice policy remains a separate B2B feature. |
| Date | 2026-09-04 |
| Version | [version.txt](version.txt) → `0.4.16-p7r-delivery-floor` |
| Scope ADR | [ADR_SCOPE_AND_CORRECTION_PASS.md](ADR_SCOPE_AND_CORRECTION_PASS.md) |
| Checkpoint | [OVERNIGHT_CHECKPOINT.md](OVERNIGHT_CHECKPOINT.md) |
| Writers | [WRITER_INVENTORY.md](WRITER_INVENTORY.md) |
| Next (engineering) | **HC1 + HC2 both CLOSED**, confirmed on remote via `org_order_history` audit trail. Fixed 3 bugs along the way: (1) tenant-side `semanticEvidenceSchema` rejected `fulfilment_channel='home_collection'`, hiding every floor screen for `WF_V2_HOME_COLLECTION` orders; (2) `FAIL_HOME_COLLECTION` missing from `WorkflowActionBar`'s reason-prompt set; (3) — not a code fix — operator changed `CONFIRM_HOME_COLLECTION.to_status` from `intake` to `preparing` in Studio, resolving the stuck-at-intake gap for this flow. Regression-checked clean on `WF_V2_SIMPLE` v4. Next: hold H1–H4. **S10 delivery canary now SIGNED (2026-09-05)** — see `13_Production_Readiness_Checklist.md`. Gate 5 compile retirement stays out of this slice (also needs artifact-unused proof beyond S10 alone). **Delivery Feature Completion programme (plan file: `C:\Users\JHNLP\.claude\plans\merry-singing-pike.md`) — Phase 1 + Phase 2 done, 2026-09-05.** Root cause: S10 only worked via a manual seed because route-create/driver-assign were hard-disabled on a legacy, buggy `DeliveryService` (non-transactional, silently swallowed stop-insert failures; also exposed a second unsafe POD/OTP path bypassing all delivery-completion safety work). Phase 1: `0490_org_drivers_mst_and_route_safety.sql` applied by operator to local+remote — `org_drivers_mst`, composite FK from `org_dlv_routes_mst.driver_id`, the `uq_dlv_stops_active_order` partial unique index (real double-booking backstop), race-safe `org_dlv_route_seq_cf` counter; `prisma/schema.prisma` updated + regenerated. Phase 2: new `delivery-route-command.service.ts` (`createRoute`/`addOrdersToRoute`/`removeStopFromRoute`/`cancelRoute`/`assignDriver`, typed Prisma + transactional row locking, mirroring `delivery-completion.service.ts`); legacy `delivery-service.ts` and the 3 unsafe POD/OTP routes deleted entirely; 5 route-planning API routes wired to the new service (still behind `STAFF_DELIVERY_WRITES_ENABLED=false`); 12/12 new DB-integration tests green against real local DB including a genuine concurrent double-booking race and a direct proof of the partial unique index. **Phase 3 (Drivers CRUD) also done, 2026-09-05.** `DRIVER_APP` feature-flag gate removed from nav (`0491_nav_drivers_remove_driver_app_gate.sql`, awaiting operator apply) and from `drivers-access.ts` (the real enforcement layer); real `DriversListScreen`/`DriverFormDialog` replace the placeholder at `/dashboard/drivers`; new `drivers-actions.ts` server actions with per-action permission checks. Found and fixed a repo-wide pitfall along the way: a discriminated-union `ActionResult` type fails to narrow under this repo's `strict:false` tsconfig — fixed to match the established flat-type convention, documented in memory. **Next: Phase 4 (route-planning UI) — shared `driver-picker.tsx`, the `/dashboard/drivers/routes` workspace, reassign affordance on `DeliveryRouteManifest`, extend `drivers-access.ts` apiDependencies.** `npm run build` still pending this session (tool permission denied); no browser available to manually click through the new screens this session — both should happen before Phase 3/4 are treated as fully closed. |
| Known gap (still open, general case only) | `intake → preparing` (`CONFIRM_PHYSICAL_INTAKE`) has no UI trigger for any *other* path that lands an order at `intake` (e.g. POS/staff drop-off) once physical intake is already `received` — the home-collection path itself is now resolved (see above). Owner decision needed on approach (extend the order-detail banner condition vs. a proper `new_order` action surface) if/when this needs fixing generally. |
| Ready for review (not implemented) | [05_PLANNED_CHECK_POLICY_CODES_ROLLOUT_PLAN.md](future_work_in_wf/05_PLANNED_CHECK_POLICY_CODES_ROLLOUT_PLAN.md) — drafted 2026-09-04 while the operator was away. 9 batches sequencing the 46 planned-but-not-emitted Check-policy codes (HQ, `cleanmatexsaas`), safest-first, each gated on a regression dry-run against the two live PILOT profiles (`WF_V2_HOME_COLLECTION` v1, `WF_V2_SIMPLE` v4) before promotion. 2 open product calls flagged (`core_processing_missing`, `fulfilment_missing_collection_gate` severity) + 1 gap found (`initial_rule_uncovered_create_path` has no planned row at all despite being file-01 Must-add). No code changed — plan only. |
| Next (operator) | Recreate any pre-cutover unsnapshotted test orders under an assigned live profile when you next need floor smoke. This is not a required handoff to continue implementation. |
| Next (product) | V1.1 return sub-order / projection follow-up after V1.0 acceptance |
| Ready for production canary | **S10 staff routed POD canary SIGNED 2026-09-05** (real operator, real UI, `ORD-20260903-0005` on `WF_V2_SIMPLE` v4 — see `13_Production_Readiness_Checklist.md`). Public confirmation is also green. **Still No for full V1.0** — `PAY_ON_COLLECTION` gate, outbox, route-create/assign hardening, canary+rollback rehearsal, T01–T18 remain open. |

## Delivery Feature Completion — Phase 4–6 close-out (2026-09-05)

`0491_nav_drivers_remove_driver_app_gate.sql` and `0492_nav_drivers_routes_orders_read.sql` are applied locally and remotely. The dispatcher route-planning workspace replaces `/dashboard/drivers/routes` placeholder: it selects only tenant `out_for_delivery` orders that do not already have an active stop, previews the selection, creates a route, and manages planned-route add/remove/cancel and driver assignment. The shared searchable driver picker is reused on the workspace and Delivery Route Manifest, where an existing active route is a non-blocking warning. All mutations remain in the transactional command service and its tenant/RBAC/CSRF API routes. The DB navigation record now requires the same `drivers:read` + `orders:read` permission set as the live page.

The owner approved Phase 5 rollout, so `STAFF_DELIVERY_WRITES_ENABLED` is now `true`; the retired POD/OTP path remains deleted. Route command tests, access-contract checks, i18n parity, targeted lint, and inventory synchronization are recorded below. A post-deploy browser smoke with live eligible orders is still required as release evidence; it must not be represented as completed until an operator captures it.
