# Current status — Workflow Order Advance

## RESUME HERE — ordered backlog to finish all remaining plans (as of 2026-09-12)

**Instruction to whichever session reads this next: work this list top to bottom. Re-read this whole file, `12_Test_Plan.md`, and `13_Production_Readiness_Checklist.md` first — don't trust this summary alone once time has passed.**

**Closed this session (2026-09-11), engineering-only, no one else needed:**
1. ~~Re-run HQ Check policy on `WF_V2_FULL_PATH`~~ — **closed.**
2. **T01 / T02 / T04 automated tests closed**; T04 product-corrected to `preparing`.
3. **Preparation details Edit Order** — header button on `/dashboard/preparation/[orderId]` (gated `orders:update`, only while the order can still be prepared). Empty-state button in FastItemizer unchanged.
4. `isOrderEditable` includes live status `preparing`. Access contract `editOrder` on Preparation Details.
5. HQ ADR-0010 + tenant ADR V1.0 accept (`Approved_By_Jh`). Canary + rollback rehearsal **done**. Hold H1–H4 **done**. Collect-payment on Ready **done**.
6. **SIMPLE v4 Check policy passed** after `0499`+`0500` + HQ deploy (`POS_QUICK_DROP` + `preparing` on Overview sequence).
7. **Order details ActionBars** — `WorkflowActionBar` `screen=new_order` **and** `screen=order_control` (`hideWhenEmpty`) on `OrderActions` and workspace Actions. Engine lists each screen’s edges for the pinned current status. Remote `draft` + `pending_dropoff` banner unchanged.
8. **Leftover-intake ActionBar smoke — operator confirmed done 2026-09-12.**
9. **Quick-drop floor smoke — operator confirmed done 2026-09-12** (create → `preparing` → Preparation **Edit Order** → complete preparation).
10. **`0501` leftover SIMPLE dest — product closed 2026-09-12.** Applied as no-op. Keep live dest as-is (`intake→processing`, home-collection confirm `→intake`). Any later dest change is HQ Studio + Check policy, not a new tenant migration.
11. **`lwpr-tenant-docs-final` done 2026-09-12.** Canonical pack audited for live Check policy / live rows. Historical Full Pack, `old/`, and Audit reports are archives, not runtime law.

**Needs a decision from the user before proceeding (don't guess):**
- None for leftover SIMPLE dest.

**Cannot be done from code — needs the user/an operator directly, do not fabricate or claim these:**
- T15's CI graph-validator: **owner deferred 2026-09-11**, not a V1.0 blocker.
- HQ remaining operator test: **soak**. **S7 PASS 2026-09-12** (operator Effective preview). All other HQ S1–S8 / T1–T10 items are code-pass, tenant-signed floor, or parked (T8 = V10x-M4). `lwpr-hq-docs-final` **done**.
- Promote the 46 planned Check-policy codes: **not started** — [05 plan](future_work_in_wf/05_PLANNED_CHECK_POLICY_CODES_ROLLOUT_PLAN.md) needs owner review first.

**Unrelated pre-existing failure noticed while running the full DB-integration suite this session (not caused by this session's changes, not investigated):** `__tests__/db-integration/wf-policy-issue-catalog-seed-invariants.db.test.ts` — `'holds for 0472 SIMPLE published live policy when present'` fails (`sequence.length` expected `> 0`, received `0`) against local DB. Not touched this session; flagging for whoever picks up local-DB-integration work next.

| Field | Value |
|-------|--------|
| Phase | **V1.0 engineering close-out done. Remaining operator watch: HQ soak.** |
| Runtime law | Tenant execute: `WorkflowPolicyResolver` on live `sys_wf_prof_ver_*` rows. HQ validation: **Check policy**. Compiled artifacts retired (`0494`). |
| Status | Engine cutover, public tracking, auditable counter pickup, delivery proof/audit, warning/override gate decisions, compiled POD method lists, and the read-only Workboard are implemented. Processing, Assembly, QA, Packing, and Ready/Release use versioned stage-owned APIs; Ready Details presents make-available, remaining collection, and confirm-pickup in one panel. Delivery Details now follows the same ActionBar + stage-owned complete pattern: `/dashboard/delivery/{id}` confirms delivery from the order when no stop exists, or uses the stop proof panel when a pending/in-transit stop exists. No dummy route is created. Simple vs routed is HQ profile policy (`delivery_stop_active` / POD evidence on `CONFIRM_DELIVERY`), not a new catalog seed. The semantic-only code cutover is complete: engine, workflow-context, Workboard, floor worklists, client transition hook, and new-order creation use only the order-bound live profile version; unbound orders fail closed for operational actions and queues. Migration `0464_require_semantic_order_snapshots.sql` is applied locally and remotely and generated types are refreshed. HQ effective preview and tenant-context simulation read live profile-version rows and `policy_revision`, while legacy HQ authoring writes return `LEGACY_WORKFLOW_RETIRED`. Live profiles may name independent optional signature, photo, POD, and notes methods and the stop method dropdown is filtered by that compiled evidence. Pickup completion enforces required compiled pickup notes. Local DB tests cover collection blocking, tenant isolation, OTP reject, already-delivered, engine-failure rollback, happy path, stale-version rollback, idempotent replay, and serialized dual-complete — **re-verified 2026-09-05** after fixing a stale test fixture (seeded orders had no `wf_profile_id`/`wf_version_no`/`wf_profile_version_id`, so 4/9 had been silently failing closed since Gate 4; not a service bug). All 9/9 pass against `WF_V2_ROUTED_POD` v1. Order-keyed floor complete unit tests cover collection blocking, refusing an existing stop, notes-only happy path, and rejecting compiled photo proof without a stop. Complete requires `delivery:pod` plus `orders:transition`. Legacy capturePOD/route writers remain `503`; generic `/actions` and `/transition` reject staff `CONFIRM_DELIVERY`. Staff POD delivery smoke **S10 is signed off (2026-09-05)** — real operator, real UI, `ORD-20260903-0005` on `WF_V2_SIMPLE` v4, verified atomically on remote (order/stop/route/history). Seeded via reviewed one-off migration `xz0490_s10_canary_route_seed.sql` (applied, then archived per repo convention) since `STAFF_DELIVERY_WRITES_ENABLED=false` still blocks route creation through the normal path — **note**: migration number `0490` was reused the same day for the unrelated Delivery Feature Completion programme's schema (`0490_org_drivers_mst_and_route_safety.sql`); the S10 seed is the archived file, not the active one. `CREDIT_INVOICE` delegates to the isolated B2B payment-hold seam. Durable B2B credit/invoice policy remains a separate B2B feature. |
| Date | 2026-09-12 |
| Version | [version.txt](version.txt) → `0.4.16-p7r-delivery-floor` |
| Scope ADR | [ADR_SCOPE_AND_CORRECTION_PASS.md](ADR_SCOPE_AND_CORRECTION_PASS.md) |
| Checkpoint | [OVERNIGHT_CHECKPOINT.md](OVERNIGHT_CHECKPOINT.md) |
| Writers | [WRITER_INVENTORY.md](WRITER_INVENTORY.md) |
| Next (engineering) | **`lwpr-tenant-docs-final` done 2026-09-12.** Remaining: HQ soak. Gate 5 **done** (`0494`). Delivery Feature Completion Phases 1–6 **done**. The rest of this cell is 2026-09-05/11 session diary, not the remaining backlog. **HC1 + HC2 both CLOSED**, confirmed on remote via `org_order_history` audit trail. Fixed 3 bugs along the way: (1) tenant-side `semanticEvidenceSchema` rejected `fulfilment_channel='home_collection'`, hiding every floor screen for `WF_V2_HOME_COLLECTION` orders; (2) `FAIL_HOME_COLLECTION` missing from `WorkflowActionBar`'s reason-prompt set; (3) — not a code fix — operator changed `CONFIRM_HOME_COLLECTION.to_status` from `intake` to `preparing` in Studio, resolving the stuck-at-intake gap for this flow. Regression-checked clean on `WF_V2_SIMPLE` v4. **Hold floor smoke H1–H4 operator-confirmed done 2026-09-11.** **S10 delivery canary now SIGNED (2026-09-05)** — see `13_Production_Readiness_Checklist.md`. Gate 5 compile retirement **done** (`0494`, later this programme). **Delivery Feature Completion programme (plan file: `C:\Users\JHNLP\.claude\plans\merry-singing-pike.md`) — Phase 1 + Phase 2 done, 2026-09-05.** Root cause: S10 only worked via a manual seed because route-create/driver-assign were hard-disabled on a legacy, buggy `DeliveryService` (non-transactional, silently swallowed stop-insert failures; also exposed a second unsafe POD/OTP path bypassing all delivery-completion safety work). Phase 1: `0490_org_drivers_mst_and_route_safety.sql` applied by operator to local+remote — `org_drivers_mst`, composite FK from `org_dlv_routes_mst.driver_id`, the `uq_dlv_stops_active_order` partial unique index (real double-booking backstop), race-safe `org_dlv_route_seq_cf` counter; `prisma/schema.prisma` updated + regenerated. Phase 2: new `delivery-route-command.service.ts` (`createRoute`/`addOrdersToRoute`/`removeStopFromRoute`/`cancelRoute`/`assignDriver`, typed Prisma + transactional row locking, mirroring `delivery-completion.service.ts`); legacy `delivery-service.ts` and the 3 unsafe POD/OTP routes deleted entirely; 5 route-planning API routes wired to the new service (still behind `STAFF_DELIVERY_WRITES_ENABLED=false`); 12/12 new DB-integration tests green against real local DB including a genuine concurrent double-booking race and a direct proof of the partial unique index. **Phase 3 (Drivers CRUD) also done, 2026-09-05.** `DRIVER_APP` feature-flag gate removed from nav (`0491_nav_drivers_remove_driver_app_gate.sql`, awaiting operator apply) and from `drivers-access.ts` (the real enforcement layer); real `DriversListScreen`/`DriverFormDialog` replace the placeholder at `/dashboard/drivers`; new `drivers-actions.ts` server actions with per-action permission checks. Found and fixed a repo-wide pitfall along the way: a discriminated-union `ActionResult` type fails to narrow under this repo's `strict:false` tsconfig — fixed to match the established flat-type convention, documented in memory. **Next: Phase 4 (route-planning UI) — shared `driver-picker.tsx`, the `/dashboard/drivers/routes` workspace, reassign affordance on `DeliveryRouteManifest`, extend `drivers-access.ts` apiDependencies.** `npm run build` still pending this session (tool permission denied); no browser available to manually click through the new screens this session — both should happen before Phase 3/4 are treated as fully closed. |
| Known gap (still open, general case only) | Remote `draft` + `pending_dropoff` still uses the order-detail drop-off banner (unchanged). Quick-drop uses Preparation + **Edit Order**. Order details now shows **New order actions** (`new_order`) and **Order actions** (`order_control`). **0499 + 0500 applied.** SIMPLE leftover dest **accepted as-is** (`CONFIRM_PHYSICAL_INTAKE` `intake→processing`, `CONFIRM_HOME_COLLECTION` `→intake`). Future dest edits: HQ Studio, not a tenant migration. |
| Ready for review (not implemented) | [05_PLANNED_CHECK_POLICY_CODES_ROLLOUT_PLAN.md](future_work_in_wf/05_PLANNED_CHECK_POLICY_CODES_ROLLOUT_PLAN.md) — drafted 2026-09-04 while the operator was away. 9 batches sequencing the 46 planned-but-not-emitted Check-policy codes (HQ, `cleanmatexsaas`), safest-first, each gated on a regression dry-run against the two live PILOT profiles (`WF_V2_HOME_COLLECTION` v1, `WF_V2_SIMPLE` v4) before promotion. 2 open product calls flagged (`core_processing_missing`, `fulfilment_missing_collection_gate` severity) + 1 gap found (`initial_rule_uncovered_create_path` has no planned row at all despite being file-01 Must-add). No code changed — plan only. |
| Next (operator) | Recreate any pre-cutover unsnapshotted test orders under an assigned live profile when you next need floor smoke. This is not a required handoff to continue implementation. |
| Next (product) | V1.1 return sub-order / projection follow-up after V1.0 acceptance |
| Ready for production canary | **S10 staff routed POD canary SIGNED 2026-09-05.** Public confirmation is also green. HQ Studio Check policy on `WF_V2_FULL_PATH` v1 **done**. HQ Studio Check policy on `WF_V2_SIMPLE` v4 **done** (after `0499`+`0500` + HQ deploy). One real UI FULL_PATH order **done**. Delivery route UI smoke **done**. Collect-payment UI smoke on Ready **done**. Hold floor smoke H1–H4 **done**. **Canary + rollback rehearsal operator-confirmed done 2026-09-11.** Assign prior Published: new creates follow the new assignment; in-flight pages/actions stay on the order pin. Workboard lists by **each order’s pinned Workboard observer module**, not the tenant’s currently assigned profile — operator re-checked 2026-09-11. **ADR V1.0 product/engineering accept 2026-09-11** (`Approved_By_Jh`) on [ADR_SCOPE_AND_CORRECTION_PASS.md](ADR_SCOPE_AND_CORRECTION_PASS.md) and HQ ADR-SAAS-MNG-0010. **Post-`0442` engine smoke:** remote demo **is** the V1.0 production-like environment (owner 2026-09-11). Leftover-intake ActionBar smoke and quick-drop floor smoke **done 2026-09-12**. SIMPLE leftover dest **accepted as-is**. **`lwpr-tenant-docs-final` done 2026-09-12.** Remaining operator watch: HQ soak. |
| 2026-09-11 session picks back up here | **T03 closed** — found retail's "must pay at POS" rule was client-side only (`use-order-submission.ts`); no server guard existed in `OrderService.createOrder`/`.createOrderInTransaction`, and retail lands straight at terminal `delivered` with no downstream collection gate. Fixed both create paths (`errorCode: RETAIL_PAY_ON_COLLECTION_NOT_ALLOWED`), new test `__tests__/services/order-service-retail-payment-guard.test.ts` (2/2). **`WF_V2_FULL_PATH` test profile** drafted to make T01 (full plant chain prep→processing→assembly→QA→packing→ready) actually testable: `0495` (seed, PILOT) + `0496` (fixed 4 dual-owner Check-policy errors + missing `draft→intake` edge + hold-from-every-stage warnings, reusing `0486`'s existing exception catalog rather than duplicating it). **HQ Studio Check policy after `0496` — operator confirmed done 2026-09-11. One real UI FULL_PATH order — operator confirmed done 2026-09-11. Delivery route UI smoke — operator confirmed done 2026-09-11. Collect-payment UI smoke on Ready — operator confirmed done 2026-09-11. Hold floor smoke H1–H4 — operator confirmed done 2026-09-11.** **T15's CI-wiring half — owner-deferred 2026-09-11**, not a blocker (see `12_Test_Plan.md`). **Canary + rollback rehearsal — operator confirmed done 2026-09-11.** Meaning used: Assign prior Published for **new** orders; in-flight stay pinned (no migrate). Floor pages and ActionBar follow the pin. Workboard is **not** the current assignment’s Workboard module — it unions each open order’s pin (`wf_profile_version_id` + that version’s Workboard observer statuses). Operator re-checked after initially reading it as current-assignment. **Post-`0442` engine smoke — owner 2026-09-11:** remote demo is the V1.0 production-like environment; no extra paying-prod smoke. T01/T02/T04 automated tests are closed. |

## Remaining across all WF plans (2026-09-12)

Remote DB applied through **`0504`**. SIMPLE leftover dest is **accepted as-is**; later dest changes go through HQ Studio. Order details already shows **New order actions** (`new_order`) and **Order actions** (`order_control`).

### V1.0 — close-out (operator floor smokes done 2026-09-12; `lwpr-tenant-docs-final` done)

| Item | Owner | Notes |
|------|-------|--------|
| Leftover-intake ActionBar smoke | Operator | **Done 2026-09-12.** **New order actions** listed engine edges on leftover `intake`. |
| Quick-drop floor smoke | Operator | **Done 2026-09-12.** Create quick-drop → `preparing` → Preparation header **Edit Order** → complete preparation. |
| Optional `0501` leftover dest | Operator | **Done 2026-09-12.** Keep live SIMPLE dest as-is (`intake→processing`, home-collection confirm `→intake`). `0501` no-op stays in history. Any later dest change: HQ Studio + Check policy. No new tenant migration. |
| `lwpr-tenant-docs-final` | Engineering | **Done 2026-09-12.** Canonical pack aligned to live Check policy / live rows. Historical Full Pack, `old/`, Audit reports, and dated CHANGELOG diary stay archives. |
| T15 CI graph-validator | Owner deferred | Not a V1.0 blocker. |
| Local flake | Engineering later | `wf-policy-issue-catalog-seed-invariants` SIMPLE published `sequence.length` 0. |

### V1.0.x — next product train (not V1.0 go-live)

| ID | Item | Repo |
|----|------|------|
| V10x-M2 / 05 plan | 46 planned Check-policy codes + missing `initial_rule_uncovered_create_path` | HQ; owner review first |
| V10x-M3 | Open-order version migrate | Tenant + HQ UI |
| V10x-M4 | `execution_binding_duplicate` / `execution_permission_invalid` | HQ catalog |
| V10x-S1 | Gate `parameters_json` schema | HQ |
| V10x-S2 | Nav from server workflow-context | Tenant |
| V10x-C1 | Starter-template import polish | HQ |
| V10x-C2 | Drop leftover artifact runtime columns | Tenant after acceptance |

### Later versions (locked, not started)

| Version | Must |
|---------|------|
| V1.1 | Return sub-order, stage executions SoT, work groups, projections |
| V1.2 | Outsourcing, richer HQ designer, milestone notifications |
| V1.3 | Partial pickup/delivery, OTP/PIN |
| V2 | Facade, multidimensional state as contract, legacy purge |

### HQ live-runtime plan leftovers (sibling repo)

HQ remaining: **soak**. **S7 PASS 2026-09-12** (operator). All other HQ S/T items are code-pass, tenant-signed, or parked (T8). `lwpr-hq-docs-final` **done 2026-09-12**. `HQ_RBAC_ENFORCEMENT_ENABLED=true` before a second HQ operator. Do not start Gate 5 again.

## Delivery Feature Completion — Phase 4–6 close-out (2026-09-05)

`0491_nav_drivers_remove_driver_app_gate.sql` and `0492_nav_drivers_routes_orders_read.sql` are applied locally and remotely. The dispatcher route-planning workspace replaces `/dashboard/drivers/routes` placeholder: it selects only tenant `out_for_delivery` orders that do not already have an active stop, previews the selection, creates a route, and manages planned-route add/remove/cancel and driver assignment. The shared searchable driver picker is reused on the workspace and Delivery Route Manifest, where an existing active route is a non-blocking warning. All mutations remain in the transactional command service and its tenant/RBAC/CSRF API routes. The DB navigation record now requires the same `drivers:read` + `orders:read` permission set as the live page.

The owner approved Phase 5 rollout, so `STAFF_DELIVERY_WRITES_ENABLED` is now `true`; the retired POD/OTP path remains deleted. Route command tests, access-contract checks, i18n parity, targeted lint, and inventory synchronization are recorded below. **Delivery route UI smoke — operator confirmed done 2026-09-11.**
