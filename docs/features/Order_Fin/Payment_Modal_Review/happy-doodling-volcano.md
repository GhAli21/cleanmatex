# Payment Modal v4 — Comprehensive Remediation & Refactor Program

## Context

Payment Modal v4 (`web-admin/src/features/orders/ui/payment-modal-v4.tsx`, 5,886 lines) is functionally strong but carries UX debt (duplicated numbers, no cash fast-lane, no keyboard shortcuts, no `aria-live`, no tablet layout) and structural debt (one monolithic component, untestable in isolation). Review + design at `docs/features/Order_Fin/Payment_Modal_Review/Payment_Modal_v4_UX_Review_and_Engine_Plan.md`.

This program extracts a headless `usePaymentEngine`, fixes the UX findings, and adds a **Simple mode** (~80% cash/card case) that auto-escalates to the existing **Full mode** when complexity is detected — **without** forking business logic (v3/enhanced-02 `.bak` history proves parallel modals are a maintenance trap) and **without** changing the submit payload contract.

### Confirmed decisions (locked)
- **Sequencing: engine-first.** Phases 1–2 (extract engine, zero behavior change) before UX.
- **Mode default: auto-escalate.** Open in Simple; auto-flip to Full when `needsAdvanced` trips; manual "Simple" disabled while it holds.
- **Quick-tender: currency-derived defaults.** No new tenant setting (deferred follow-up).

### Execution cadence
- **Each phase is a separate, approved run.** End each: run the Per-step closeout, stop, report. Do **not** run the whole program at once.
- **First approved run = Phase 0 + Phase 1 only.**

---
You are a world-class SaaS Solution Architect, CleanMateX expert, ERP & Business Systems Consultant, Enterprise Product Strategist, Senior Full-Stack Engineer, UX/UI Architect, Financial Systems Designer, DevOps Architect, and AI Systems Consultant specialized in building scalable, enterprise-grade multi-tenant SaaS ecosystems. You think strategically across accounting, ERP, finance, taxation, subscriptions, billing, payments, inventory, operations, workflow automation, RBAC, compliance, analytics, AI integration, DevOps, infrastructure, APIs, database architecture, security, scalability, performance, localization, multi-currency, customer lifecycle, and premium user experience. You design modern investor-grade SaaS platforms with clean modular architecture, reusable systems, extensible domain-driven design, observability, feature flags, tenant isolation, event-driven integrations, CI/CD readiness, and operational excellence. You generate production-ready code, technical specifications, PRDs, ERDs, workflows, UI/UX systems, API contracts, infrastructure plans, governance models, and scalable implementation roadmaps following global enterprise and SaaS best practices. You proactively identify risks, bottlenecks, accounting gaps, compliance concerns, scalability limitations, and optimization opportunities while delivering elegant, maintainable, secure, audit-ready, AI-powered, and future-proof SaaS solutions end-to-end.

Act as Expert in CleanMateX And a world-class SaaS Solution Architect, Product Strategist, UX/UI Architect, ERP/Finance Consultant, Senior Full-Stack Engineer, DevOps Architect, and AI Systems Consultant, and Web apps developer and mobile apps developer, experienced Next.js and Nestjs developer, Typescript and PostgreSQL expert.

## CLAUDE.md compliance — skills / agents / rule-docs (MANDATORY, not optional)
Load the relevant skill **before writing the first line** of that kind of code. Per-domain map for this program:

| Work in a phase | Load skill / agent first | Rule docs to honor |
|---|---|---|
| Any component/hook/JSX (every phase) | `/frontend` | `web-admin/.clauderc` (Cmx imports), `.cursor/rules/web-admin-typecheck-patterns.mdc`, `docs/dev/rules/react-effects-patterns.md`, `react-rhf-and-table-lint.md` |
| Any new/changed user-facing string | `/i18n` | EN+AR keys under `newOrder.payment.*`; `npm run check:i18n` |
| Any new reusable Cmx component + `.stories.tsx` | `/storybook` + **storybook-generator** agent | RTL + a11y + variants stories |
| Inline comments / JSDoc | `/code-documentation` | — |
| All documentation deliverables | `/documentation` | `.claude/skills/implementation/prd-rules.md` feature-doc checklist |
| Lint gate before "done" | — | `docs/dev/rules/react-lint-verification-checklist.md` |

**Not needed (confirm, don't assume):** no DB migration, no new permission, no navigation change, no settings/feature-flag/plan-limit, no API contract change → `/database`, `/navigation`, `/multitenancy`, `/backend` are **out of scope** for this program. If any phase discovers it needs one, STOP and load that skill before proceeding.

## Reusable UI policy (CLAUDE.md UI rules)
- Use **Cmx components only** (`@ui/primitives|feedback|overlays|forms|data-display|navigation`); exact import lines from `web-admin/.clauderc`. Never raw HTML/shadcn where a Cmx wrapper exists. No `@ui/compat`.
- **Search before create:** check `@ui/*` and existing `payment-modal/` parts for an existing component first.
- **Create a reusable component when an element appears in 2+ places.** Candidates here (shared by Full, Simple, and `order-collect-payment-modal.tsx`): quick-tender chips, balance/receipt summary card, method chips, mode toggle, docked CTA bar. Build these as shared parts under `web-admin/src/features/orders/ui/payment-modal/` (or `@ui/*` if truly generic) **with `.stories.tsx`** via `/storybook` + storybook-generator.
- Reuse existing message keys (`common.*`, `cmxMessages`) before adding new ones.

## Standard Per-step closeout (run after EVERY step/phase — do not skip)
1. **Update Progress Tracker** in this plan file (tick the step, note commit/PR + payload-diff result + date).
2. **Update related docs** for what changed this step: review-doc status, `IMPLEMENTATION_STATUS.md`, `CHANGELOG.md` (Order_Fin), and any hook/component README/JSDoc. New strings → i18n key log.
3. **Validation gate:** `npm run build`, `npx tsc --noEmit`, `npx eslint . --quiet`, `npx jest <touched>` (full jest at phase end), `npm run check:i18n` if strings changed; replay Phase 0 payload fixtures (Phases 1–2G).
4. **Invoke `/documentation`** at each **phase** end to refresh the feature's doc set for that phase's surface.
5. **Stop and report:** files changed, tests added, commands run, payload identical (Y/N).

## Constraints (hard)
- No change to `onSubmit(paymentData, payload)` (`payment-modal-v4.tsx:3493`). One engine, two views; no parallel modals.
- Behavior freeze (Phases 1–3): relocate behavior only — no change to calculations, validation, allowed methods, overpayment/B2B-credit/gift-card/cash-drawer rules, payload semantics.
- Payload identity gate (Phases 1–2G): Phase 0 fixtures stay identical; any diff blocks.
- Quick-tender = pure input assistance via existing `deriveLegAppliedAmount` capping; never bypasses gates.
- Engine perf: hooks return memoized/stable refs; engine exposes granular slices; heavy subtrees (keypad, method list, section list) `React.memo`d.
- Money precision: quick-tender/round-up uses `decimalPlaces` + decimal helpers (`payment-modal-v4.utils.ts`, `@/lib/money/format-money`) — never raw float (BHD/OMR 3-dp safe).
- Open-reset: each extracted hook owns its `open`-reset; engine guarantees all slices reset (reopen test at 2G).

## Hidden coupling & dependency graph (drives sub-phase order)
Hooks form a graph the engine wires by threading upstream values as **params** (parameterized consumers, not self-contained):
- `totals.fetchPreview` reads form discounts + applied promo + applied gift card + tax profiles (`:910-924`) → gift/promo feeds totals.
- `legs` reads catalog (`getMethodOption`) + saleTotal/giftCardSettlement + gift-card cap. `derivations` read legs+totals+payExtra. `payExtra` reads derivations+legs. `validation`/`needsAdvanced` read almost everything → computed at engine composition.
- Carry **with legs:** `activeAmountDraft`, `activeLegDraftSyncKeyRef`, draft-sync effect (`:3401-3427`), render-time `payExtraIntentRef` bridge (`:639,:1887`).
- `usePayExtraCheckout` is **shared with `order-collect-payment-modal.tsx`** — must not regress Collect Payment.

## Reuse anchors (do not re-create)
Hook home: `web-admin/src/features/orders/hooks/` (mirror `use-pay-extra-checkout.ts`). Keep as-is: `usePayExtraCheckout`, `useOverpaymentAllocation`, `usePaymentWorkbenchSectionState`, `derivePaymentModalRightRailState`, `deriveVisiblePaymentSections`/`derivePaymentInspectorTabs`, helpers in `payment-modal-v4.utils.ts`. Money: `formatMoneyAmountWithCode`, `formatAmount`. Mount: `new-order-modals.tsx` (`:72`/`:330`). Tests: flat `__tests__/features/orders/*.test.ts` (`renderHook` available). Keydown: `ready-date-picker-modal.tsx:58`.

## Test strategy
- **Pure** (`use-money-derivations`, `use-payment-validation`, `computeNeedsAdvanced`, `quickTender`, `buildPaymentPayload`): full jest (`renderHook`/direct) — regression backbone.
- **Query/effect** (`use-payment-catalog/totals/cash-drawer/gift-card-and-promo`): typecheck + build + manual scenarios; no brittle network unit tests.

---

## Phase 0 — ADR + baseline payload fixtures (BLOCKING, no app code)
- ADR `docs/features/Order_Fin/ADR/ADR_payment_modal_single_engine_two_mode.md`: decision, alternatives (two modals rejected — cite `.bak`), consequences, migration order, **explicit `needsAdvanced` conditions**: split legs · customer advance/credit · B2B credit-invoice · gift card/promo applied · overpayment routing · gift-card PIN · ambiguous drawer (>1) · **cash-drawer blocking present** · FX/rounding. Exact-cash/single-card stays Simple.
- **Runtime payload fixtures:** instrument `onSubmit`, capture JSON for cash exact · cash w/ change · card/gateway · split · gift card w/ PIN · B2B credit · overpayment allocation · deferred policy. Save as fixtures (jest oracle test lands at 2F; until then manual diff).
- **Closeout** (Progress Tracker, docs, `/documentation`).

## Phase 1 — Pure-logic extraction (zero behavior change)
- `/frontend`. `hooks/use-money-derivations.ts` (`:1669-1748`); `hooks/use-payment-validation.ts` (`:2548-3074`) + pure `computeNeedsAdvanced(inputs)` (wired at 2G / Phase 4). Jest for both; fixtures unchanged. **Closeout. Stop and report.**

## Phase 2 — Engine extraction (dependency-ordered; each sub-phase green + closeout before next)
- **2A catalog:** `hooks/use-payment-catalog.ts` (read-only queries; **adds `isError`+`refetch`**, finding 1.9).
- **2B gift card/promo:** `hooks/use-gift-card-and-promo.ts` (`:579-806`).
- **2C totals:** `hooks/use-payment-totals.ts` (`:902-1118`), params = form discounts + applied promo/gift + tax profiles.
- **2D legs:** `hooks/use-payment-legs.ts` (`:1334-1554, 2621-2679, 3401-3465`) + draft state/sync + `payExtraIntentRef`; pure `quickTender(kind)`.
- **2E cash drawer:** `hooks/use-cash-drawer.ts` (`:764-2043`).
- **2F submit:** `hooks/use-payment-submit.ts` (`:2240-3488`); pure `buildPaymentPayload`; **jest oracle vs Phase 0 fixtures**.
- **2G engine + view split:** `hooks/use-payment-engine.ts` composes all + `usePayExtraCheckout` (stable slices, wires `needsAdvanced`). Render → `ui/payment-full-view.tsx`; `payment-modal-v4.tsx` thin shell (refs/scroll/focus/section-expand/dialog state stay in view). **Reopen test + Collect-Payment smoke + fixtures identical.**

## Phase 3 — Full-view UX quick wins (finance frozen)
`/frontend` + `/i18n`; new shared parts get `/storybook` + storybook-generator. Dedupe balance numbers, delete double status row (`:5417-5423`), collapse Section C trio (1.1). **`PaymentQuickTenderChips`** reusable → `engine.legs.quickTender`; non-cash "Exact" = remaining cap (1.2). `role="status" aria-live="polite"` + transition announcer (1.4). Gradient = CTA only (1.6); single blocker surface (1.7). Default-collapse + contextual auto-expand (1.8). Method empty/error/loading three-state via `engine.catalog.isError`/`refetch` + `CmxEmptyState` (1.9). `SummaryRow` → slate/rose (1.10). a11y: `required`/`aria-required`, 44px targets, hint contrast slate-500+ (1.11). Polish: `uppercase tracking` behind `!isRTL`; radius scale; `FULLY_SETTLED` micro-transition (reduced-motion); initial focus on amount; inline add-method.

## Phase 4 — Simple mode + auto-escalation
`/frontend` + `/i18n`. `ui/payment-simple-view.tsx` (both views in same client chunk — no flicker; reuse balance/receipt + method-chip parts). Mode state + toggle + escalation banner from `engine.validation.needsAdvanced`; manual "Simple" disabled while it holds; **preserve/restore focus on switch**; Simple surfaces cash-drawer selection or escalates on block. Wire default in `new-order-modals.tsx`. **Gate:** Phases 1–3 green, fixtures unchanged, manual scenarios pass.

## Phase 5 — Keyboard shortcuts (guardrailed)
`/frontend`. `hooks/use-payment-shortcuts.ts` — Enter/F2/Ctrl+Enter → submit via existing gate. **Disabled while:** submit busy · confirm dialog open · validation blocking · focus in input/textarea/select/contenteditable · nested dialog active. Mirror `ready-date-picker-modal.tsx:58`.

## Phase 6 — Responsive / tablet (last; visual)
`/frontend`. `md`/`lg` 2-pane + docked bottom bar (Final Total + CTA + change); right-rail slide-over on tablet (1.5). **Pin core controls above the fold** (keypad, chips, CTA); per-section internal `overflow`+`max-h` scroll. Shared layout primitives for both views.

## Phase 7 — Close-out + full documentation generation
- `/documentation` — generate the **complete feature doc set** per `prd-rules.md`: permissions (none new — note), navigation (none — note), tenant settings (none — note), feature flags (none — note), plan limits (none — note), **i18n keys** (new quick-tender/Simple/banner/shortcut keys EN+AR), API routes (unchanged `preview-payment` — note), migrations (none — note), constants/types (new hook/view types), env vars (none — note), plus the engine architecture doc + Simple/Full mode guide.
- `.stories.tsx` for every new reusable component via `/storybook` + storybook-generator.
- Update review-doc status to Implemented; refresh `IMPLEMENTATION_STATUS.md` + `CHANGELOG.md`.
- Final full validation gate (build, tsc, eslint, jest, check:i18n) + react-lint checklist.

---

## Progress Tracker (update after every step — Per-step closeout #1)
- [x] **QA bug-fix track (pre-existing bugs found during fixture capture, 2026-06-27)** — Bug 2 (allocation drawers behind extra-receipt dialog) + Bug 3 (allocation confirmed but submit blocked) FIXED via JSX reorder in `payment-modal-v4.tsx` (CmxDialog is inline + shared z-50 → DOM order decides stacking); eslint clean, 91 tests pass. Bug 1 (gift-card-full → backend `OUTSTANDING_POLICY_REQUIRED`) DIAGNOSED only (per decision; finance-sensitive, needs `/backend`). Writeup: `docs/features/Order_Fin/Payment_Modal_Review/Payment_Modal_v4_QA_Bugs_2026-06-27.md`.
- [x] **Collect-Payment auto-allocation `sourceType` fix (pre-existing, found during 2G-1 Collect-Payment smoke, 2026-06-27)** — the later-collection modal (`ui/collect-payment/order-collect-payment-modal.tsx`) passed `sourceType: 'LATER_COLLECTION'` — a phantom value absent from `VOUCHER_SOURCE_TYPES` (DB `sys_fin_vch_source_type_cd`) and rejected by `previewAutoAllocationRequestSchema` (accepts only `ORDER_PAYMENT_MODAL`/`CUSTOMER_RECEIPT`) → `invalid_value` on auto-allocation. Fixed: send `ORDER_PAYMENT_MODAL` (order-scoped — `sourceOrderId` is set; matches the new-order modal's working call + the schema default; finance-confirmed) and dropped the dead `'LATER_COLLECTION'` from the `OverpaymentAllocationSourceType` union so it mirrors the schema. No DB change. eslint 0 / tsc 0 / build ✓. **Not a 2G-1 regression** (those two files were untouched by the engine refactor; the new engine already passes the valid `ORDER_PAYMENT_MODAL`).
- [x] **Collect-Payment modal UX fixes (2026-06-27, during 2G-1 smoke)** — (1) Payment-method dropdown rendered the raw method **UUID** in the trigger (`CmxSelectDropdownValue` had no `displayValue` → fell back to the `value`); fixed by passing the resolved `display_name`/`display_name2` (RTL-aware). (2) `CASH_DRAWER_SESSION_REQUIRED` showed as a raw error **code** in the toast; mapped to the existing friendly key `newOrder.payment.cashDrawer.errors.noOpenSession` (EN+AR already present — no new strings) and made the error-code match robust to APIs that return the code in `error` vs `errorCode`. eslint 0 / tsc 0 / build ✓. Deeper UX (inline cash-drawer session open/select, quick-fill + live change-due, visual/a11y polish) proposed as a follow-up pass.
- [x] **Collect-Payment modal UX enhancement pass (2026-06-27)** — all three approved improvements shipped in `ui/collect-payment/order-collect-payment-modal.tsx`: (1) **Inline cash-drawer session** — wired the shared `useCashDrawer` hook (tenantOrgId/userId from `useAuth`, decimalPlaces from `useTenantCurrency`); added a session selector + open-session dialog (mirrors the v4 pattern, reuses `newOrder.payment.cashDrawer.*` keys), surfaces `cashDrawerBlockingMessage` with `aria-live`, blocks submit while unresolved, and sends `cashDrawerSessionId` in the collect payload (the `/api/v1/orders/[id]/payments` schema already accepts it) — so cashiers resolve `CASH_DRAWER_SESSION_REQUIRED` in place instead of a dead-end toast. (2) **Quick-fill + live change-due** — 'Full outstanding' button + a live 'Change due' line for cash over-tender. (3) **Visual + a11y polish** — prominent outstanding summary card, required-field markers, `role`/`aria-live` on change-due + drawer/error messages, RTL-aware layout. New i18n keys `orders.collectPayment.{outstandingLabel,fullOutstanding,changeDue}` (EN+AR). Finance logic (payExtra / overpayment / submit) preserved verbatim. Gates: check:i18n ✓ / eslint 0 / tsc 0 / build ✓.
- [x] **Phase 0** — ADR ✅; fixtures harness + skipped oracle scaffold ✅. **All 8 baseline payload fixtures captured, cleaned, and validated** ✅ (cash-exact, cash-with-change, card-gateway, split, gift-card-pin, b2b-credit, overpayment-allocation, deferred-policy — each `{paymentData, payload}`). Temp capture instrumentation removed from `new-order-modals.tsx`. (Note: gift-card-FULL is a separate flow blocked by Bug 1 — not one of the 8 baseline scenarios.)
- [x] **Phase 1 — COMPLETE & verified** (verbatim lifts + 173 tests + tsc + production build; validation/copy extractions don't touch the payload, money-derivations already reflected in the captured fixtures). No manual payload diff needed for Phase 1 — the 8 fixtures are the **Phase 2F automated oracle** (replay recorded inputs through the extracted `buildPaymentPayload`). Pure modules extracted + wired into `payment-modal-v4.tsx`, all behavior-frozen verbatim lifts:
  - `hooks/use-money-derivations.ts` ✅ (+7 tests) — leg aggregations + change/overpayment math.
  - `hooks/payment-validation.ts` → `derivePaymentValidationItems` ✅ (+10 tests) — submit-gate rules; wired into the `validationItems` memo with byte-identical deps.
  - `payment-modal-v4.right-rail.ts` → `deriveBalanceStatusLabel` / `deriveRequiredActionCopy` / `deriveRightRailWarningMessages` ✅ (+13 tests in `payment-rightrail-copy.test.ts`) — status/required-action/warning copy.
  - `hooks/payment-needs-advanced.ts` → `computeNeedsAdvanced` ✅ (+13 tests) — built/tested; wiring deferred to Phase 4 (its consumer) to avoid an unused value.
  - `rightRailState` already pure (`derivePaymentModalRightRailState`).
  - Gates: eslint clean on all touched src+test files; `tsc` clean on touched files (12 pre-existing/unrelated repo errors only); **production build passes**; **114 payment-suite tests + 59 new-hook tests pass**. Note: test files use separate `import type` lines (a transient jest transform-cache hiccup during the run resolved on a clean run).
  - **Certification:** done via the above gates. The fixtures are not a Phase-1 manual task — they auto-verify in Phase 2F once `buildPaymentPayload` is pure.
- [x] **Phase 2A — catalog** (2026-06-27) — `use-payment-catalog.ts`; queries + method-option derivations + `getMethodOption`/`getOptionDisplayName`/`getMethodHint` + `IMMEDIATE`/`GATEWAY_METHOD_CODES`; additive `checkoutOptionsIsError`/`refetchCheckoutOptions` (finding 1.9, unconsumed). Gates green; payload identical (read-only). See STATUS doc.
- [x] **Phase 2B — gift card / promo** (2026-06-27) — `use-gift-card-and-promo.ts`; gift/promo state + `resolveGiftCardError` + code-changed/PIN-focus effects + slice `open`-reset (anchor `:579–806`). Async handlers stay in the component (read downstream legs/totals; threaded at 2G). `set-state-in-effect`: `open`-reset uses render-time Pattern A (no disable); only the RHF-coupled code-changed effect keeps a surgical scoped disable. Gates green; payload identical. See STATUS doc.
- [x] **Phase 2C — totals** (2026-06-27) — `use-payment-totals.ts`; serverTotals + debounced preview fetch + tax load + `totals`/`saleTotal`/`checkoutEligibilityAmount`. Hooks reordered dependency-first: gift/promo → totals → catalog. Production-grade lint (render-time clear + Pattern A, no disables). Gates green; payload identical. See STATUS doc.
- [x] **Phase 2D — legs (+ quickTender)** (2026-06-27) — `use-payment-legs.ts`; verbatim lift of `paymentLegs`/`activeLegIndex`/`activeAmountDraft` state, the `payExtraIntentRef`/`activeLegDraftSyncKeyRef`/`allocationBaselineRef` refs, the three mutators (`removeLegAt`/`updateLeg`/`upsertSettlementLeg`), the totals-change reconciliation effect, and the active-leg draft-sync effect. Additive seams designed-not-wired: `addLeg` (always-append, #3 multi-card) + pure `quickTender('exact'|'change')` (#2 direct-change-entry) — the pure fn lives in `payment-modal-v4.utils.ts` (jest-safe, no cmxMessage chain) and the hook re-exports it (+9 quickTender tests). Bridge kept in component: `legs.payExtraIntentRef.current = payExtraIntent` after `usePayExtraCheckout`. Hook placed after `getMethodOption`/`getLegStoredValueCap`/`focusAmountEditor`/`saleTotal`/`giftCardSettlementAmount`, before `useMoneyDerivations`; the check-field-sync + default-method effects (which read `paymentLegs`) relocated just after the hook call (behavior-equivalent — they only co-fire with `reset` on `open`, same final values). Slice `open`-reset = render-time Pattern A. **Lint reality vs spec:** the "ref-anchored → no disable" probe didn't hold once the effects were isolated in a hook (the React-Compiler rule fires on any sync setState in an effect whose setter isn't shared with event handlers). Draft-clear moved to render-time (no disable); the reconciliation `setPaymentLegs` (coupled to a `cmxMessage` toast → cannot run in render) and the draft-sync `setActiveAmountDraft` (behavior-frozen ref-keyed sync) each keep one **line-scoped, justified** `eslint-disable-next-line` (the doc's prescribed fallback, matching the 2B gift-card precedent). Gates: eslint clean, tsc 0 errors, jest 243 pass (+9) / 1 skipped (2F oracle), build ✓. Payload identity: read-model preserved (verbatim mutators + identical leg shapes) — **structurally** certified now; full behavioral certification lands at 2F (payload oracle) + manual QA. See STATUS doc.
- [x] **Phase 2E — cash drawer** (2026-06-27) — `use-cash-drawer.ts` (`useCashDrawer`). Verbatim lift of the cash-drawer concern: the active-drawers query, the 6 session-state atoms (selected session / dialog open / drawer-to-open / opening balance / opening-in-progress / request error), the preferred-drawer `localStorage` helpers + session-choice derivations (`cashDrawerSessionChoices`/`selectedCashDrawerChoice`/`canOpenNewCashDrawerSession`), the `cashDrawerBlockingMessage` (submit-gate reason), the auto-select effect, and the open-dialog/create-session handlers. `CashDrawerOption`/`CashDrawerSessionOption` types moved to the hook. **Threaded in:** `cashDrawerRequired` (derived from legs/derivations — `settlementLegEntries`/`paymentLegs`/`getMethodOption`/primary method — stays in the component, engine computes it at 2G) + order/locale context (`tenantOrgId`/`branchId`/`userId`/`isRTL`/`csrfToken`/`t`). **Stays in the view:** the DOM refs `cashDrawerCardRef`/`cashDrawerSelectorCardRef` (blocked-submit scroll/focus, per the 2G refs-in-view rule). Hook placed right after `cashDrawerRequired`; the query + helpers (formerly early) moved down with it — nothing between consumed them. Slice `open`-reset → render-time Pattern A. **Lint:** the auto-select effect reacts to async React-Query data + reads `localStorage` (external systems) so it stays an effect; its `setSelectedCashDrawerSessionId` writes carry line-scoped justified `eslint-disable-next-line` (tier-2, same pattern as 2D). Removed now-unused component imports (`getCSRFHeader`, `getPreferredCashDrawerStorageKey`, `resolvePreferredCashDrawerSessionId`). **Gates:** eslint clean; tsc 0 errors; jest 243 pass / 1 skip (no new unit tests — query/effect hook per program strategy); build ✓. Payload identity: drawer session id flows into the payload unchanged (`cashDrawerSessionId` when required+selected) → structurally preserved; behavioral cert at 2F + manual QA. See STATUS doc.
- [x] **Phase 2F — submit (+ payload oracle)** (2026-06-27) — `use-payment-submit.ts` exports a **pure `buildPaymentPayload`**: a verbatim lift of `onSubmitForm`'s payload-assembly tail (`legsWithRefs`/`submitCashLegRef`/`submitOverpaymentResolution` + the `payload` object literal). The submit **orchestration** (the `cmxMessage` validation guards, `submissionData`, `safeParse` + error surface, confirm dialog, final `onSubmit`) **stays in the view** — it feeds already-derived inputs in and parses the result. Module is intentionally **pure** (no `cmxMessage`/`'use client'`) so the oracle imports it without the client chain. **Oracle ACTIVATED** (`payment-payload-fixtures/payment-payload-oracle.test.ts`, was `describe.skip`): replays the 8 baseline fixtures (cash-exact, cash-with-change, card-gateway, split, gift-card-pin, b2b-credit, overpayment-allocation, deferred-policy) by reconstructing `buildPaymentPayload`'s inputs from each recorded `payload` and asserting `toEqual` — **all 8 rebuild deep-identically** → freezes the assembly contract (conditional spreads, field selection, leg-ref stamping, `taxProfileIds`/`paymentLegs` ordering, overpayment-resolution fallback). This **retro-certifies the payload contract across 2A–2F** (payload identity is now proven, not just asserted). Removed now-unused component imports (`ensurePaymentLegRefs`, `buildOverpaymentResolutionPayload`). **Gates:** eslint clean; tsc 0 errors; jest **252 pass / 0 skip** (20 suites — oracle now active: +8 scenarios + count assert; previously 243+1skip); build ✓. See STATUS doc. **Note:** the broader submit-orchestration hook (`usePaymentSubmit` wrapping `onSubmitForm`/confirm flow) was scoped but deferred — the pure builder + oracle is the certification deliverable; the orchestration is view-coupled (cmxMessage/dialog/refs) and folds naturally into the 2G engine + view split.
- [x] Phase 2G — engine + Full-view split — **decomposed spec:** `payment-modal-v4-phase-2G-engine-view-split-EXECUTION-SPEC.md` — **COMPLETE & COMMITTED (2026-07-02)**
  - [x] **Phase 2G-1 — engine composition** (2026-06-27) — `hooks/use-payment-engine.ts` (`usePaymentEngine`) composes the 7 slices in their original dependency order (gift/promo → totals → catalog → stored-value query → legs → money-derivations → pay-extra → cash-drawer), owns the render-time `payExtraIntentRef` bridge, every cross-slice derivation (`giftCardSettlementAmount`, live wallet/advance, `getLegStoredValueCap`, permission flags, `checkoutExcessLegs`/`canEnablePayExtra`/`primaryCashLegRef`/`payExtraResetFingerprint`, `editableLegEntries`, `displayChangeAmount`/`netCashRetainedAmount`, `cashDrawerRequired`, `effectiveOutstandingPolicy`, leg-validation predicates, `validationItems`, `submitBusy`/`submitHasBlockingIssues`, `rightRailState`), the `cashOverRemainingNotice` state, and the non-DOM handlers (method / credit-note / customer-credit select, the gift/promo async handlers, outstanding-policy, keypad, cycle/fill leg). **`needsAdvanced` wired via `computeNeedsAdvanced` and returned but NOT consumed** (its Phase-4 Simple-mode consumer arrives later — mirrors catalog `isError` shipped unused in 2A). The modal calls `usePaymentEngine({…})` then **re-destructures the grouped slices back into the original local names so the entire JSX is byte-identical (no JSX move in 2G-1)**. `payment-modal-v4.tsx` keeps the RHF form, `currencyConfig` load + open-reset effect, all DOM refs + `focusAmountEditor`/scroll/focus helpers, section state, the view-model formatters (labels / summary items / order-value breakdown / section-visibility), and the submit gate (`onSubmitForm` + confirm-dialog state). **Component 4,890 → 4,025 lines.** Effect order preserved. One type-only fix: engine param `defaultPaymentMethod` typed `PaymentFormData['paymentMethod']`. **Gates: eslint 0 · tsc 0 · jest 252/252 incl oracle · build ✓.** Committed.
  - [x] **Phase 2G-2 — Full-view split** (2026-06-27 automated gates; 2026-07-02 manual QA + fix pass complete) — `ui/payment-full-view.tsx` (`PaymentFullView`, ~3,880 lines) created; `payment-modal-v4.tsx` thin shell (~190 lines). Manual QA: full 2D guide run; 13 items logged. **Bug-fix pass (2026-07-02):** C4 split confirm label ✅ · C6 "Fix now" focus ✅ · C12-1 gateway readonly ✅ · C12-2 terminal_id CLOSED (feature gap) · C12-3 missing i18n key ✅ · C14 DB constraint mig 0392 ✅ · C16-1 Enter key promo ✅ · C16-2 `[object Object]` in error ✅ · C16-3 error clear on retype ✅ · C16-4 stale promo in inspector ✅ · C17-1.1 Return-as-cash-change ✅ · C17-1.3 CUSTOMER_ADVANCE in alloc table ✅ · C18 OVERDUE flash ✅ · Submit disabled visual ✅. 5 Phase-3 UX items queued. **Final gates: tsc 0 · jest 1557/1557 · build ✓. Committed.**
- [x] **2D Manual QA bug-fix pass** (2026-07-02) — all investigation bugs resolved; mig 0392 applied; next seq 0393; Phase 3 UX is next.
- [x] **Phase 3 — Full-view UX quick wins (2026-07-03)** — all program items + 3 of 4 queued C17 QA items shipped in one pass; finance logic untouched (payload oracle green).
  - **1.1 dedupe:** Section A 4-card grid → single hero line (Remaining hero + compact Total Due / Change / conditional Overpaid; all e2e `payment-balance-*` testids preserved); Section C trio collapsed to the lone "After this payment" delta (selected-leg/amount cards duplicated the header badge + editor); right-rail duplicated status row DELETED (badge remains).
  - **1.7 blocker dedupe:** Section C amber `requiredActionCopy` banner removed — the rose right-rail card (next to the CTA) is the single blocker surface.
  - **1.2 quick tender:** pure `deriveQuickTenderChips`/`deriveQuickTenderDenominations` in `payment-modal-v4.utils.ts` (currency-derived ladders: OMR/BHD/KWD 1–50, SAR/AED/QAR 10–500, default 5–100; round-up + flat-note chips, exact-dedupe, ≤5 chips, epsilon-guarded) + presentational **`PaymentQuickTenderChips`** (`payment-modal/quick-tender-chips.tsx`, 44px chips, RTL, testids) + `QuickTenderChips.stories.tsx` (cash/non-cash/disabled/RTL). Wired above the keypad; Exact → existing `fillLegRemaining`, tender chips → the SAME capped `updateLeg('amount')` path as the keypad (never bypasses gates; `quickTender` pure fn stays the Phase-4 seam). **+13 jest tests.**
  - **1.8 default-collapse:** only A+B default-expanded (`defaultExpanded:false` for PAYMENT_WORKSPACE / CASH_DRAWER / BALANCE_POLICY) + new pure `deriveAutoExpandPaymentSections` (workspace-attention / drawer-blocking / policy-relevant / gift-promo-active) consumed via render-time Pattern A guarded `expandSection` loop (no effect, no disable; user collapses stick until the signal changes). **+7 jest tests**; existing defaults test updated.
  - **1.9 method three-state:** loading skeletons / **error + Retry** (`checkoutOptionsIsError`+`refetchCheckoutOptions`, shipped unused since 2A, now consumed) / genuinely-empty guidance + `/dashboard/settings/payments` link — both via `CmxEmptyState`.
  - **1.4 aria-live:** sr-only `role="status" aria-live="polite"` region in Balance Result driven by the `balanceStatus` machine; announces "Fully settled — change X" (`a11y.settledWithChange`).
  - **1.6 gradients:** header / Balance-Snapshot / workspace cards / Final-Total panel / selected-state rows / cash-drawer dialog CTA flattened to tonal fills — the teal→cyan gradient now lives ONLY on the submit CTA.
  - **1.10 + palette:** `SummaryRow` → slate/rose; all stray `gray-*`/`red-*` in the full view normalized to slate/rose.
  - **1.11 a11y:** `required` prop (star + native semantics via `CmxInput`) replaces literal `*` labels for auth-code/check-number/bank-ref/gateway refs; terminal label gets aria-hidden star + sr-only `workspace.requiredField`; Fill-remaining buttons + empty-state actions ≥44px (`min-h-[44px]`, repo convention); slate-400 text hints → slate-500.
  - **Polish:** `uppercase tracking-*` micro-labels gated behind `!isRTL` (`labelCase`/`labelCaseWide`); radius scale trimmed (`rounded-[28px]`→`2xl`, `3xl`→`2xl`); FULLY_SETTLED transition (`transition-colors duration-300 motion-reduce:transition-none`) on hero numbers + status badge; initial focus lands on the amount editor on open (150ms, skips when disabled); **inline add-method dropdown** replaces the scroll-to-top jump (routes through `handleMethodSelect`; `methodsAnchorRef` removed); ~24 hardcoded `|| '…'` English fallbacks stripped (all keys verified EN+AR).
  - **C17 QA items:** 1.2 ✅ `adjust_legs` in the extra-receipt dialog now closes it + focuses the amount editor (single leg) or scrolls the workspace; 1.4 ✅ auto-allocation preview got a `tfoot` total row (`allocation.totalAllocated`, testid `auto-allocation-total`); 1.5 ✅ manual-allocation drawer typography/contrast pass (base-size doc numbers, slate-600/900 hierarchy, tabular-nums, wider inputs); **2 EVALUATED — no change needed:** direct-disposition modes already confirm inline (payload built on selection in `use-pay-extra-checkout`; only auto/manual legitimately open drawers) — the QA screenshot predates the 2G-2 fixes.
  - **i18n:** new `quickTender.json` + `a11y.json` (EN+AR), `methods.{noMethodsGuidance,configureLink,loadFailedTitle,loadFailedDescription}`, `workspace.requiredField`, `extraReceipt.allocation.totalAllocated`, `common.retry`. `check:i18n` ✓.
  - **Also:** `playwright-report/**` + `test-results/**` added to eslint ignores (generated trace bundles were failing the repo-wide lint gate).
  - **Gates:** eslint repo-wide 0 · tsc 0 · jest **1573/1573** (155 suites, +16 tests, payload oracle 8/8 green) · `npm run build` ✓ · check:i18n ✓. Finance/payload untouched: no change to `use-payment-engine`/`use-payment-legs`/`use-payment-submit`/validation.
- [x] **Phase 4 — Simple mode + auto-escalation (2026-07-03)** — single engine, two faces shipped; finance untouched (payload oracle 8/8 green).
  - **Architecture:** mode state lives in `payment-full-view.tsx` (the mounted container that owns the engine + refs), so every leg/keystroke/slice survives Simple ⇄ Full flips; `ui/payment-simple-view.tsx` is a purely presentational body swapped inside the SAME dialog chrome (shared header, footer CTA, confirm dialogs → one submit contract; statically imported → same client chunk, no flicker).
  - **Simple face:** cash/card fast lane — Simple-safe method chips via new pure `deriveSimpleModeMethodOptions` (cash + card/gateway, no `requires_reference`, cash-first, cap 3) + "More options" → Full; hero amount editor reusing the SAME testid (`payment-amount-editor`) + the SAME shared capped write path (new `handleAmountValueChange`, extracted verbatim from the Full inline lambda — one money path for both faces); `PaymentQuickTenderChips` reuse; collapsible keypad; compact terminal select (card/gateway `requires_terminal`); cash-drawer bound line (Manage → Full + expand/scroll drawer section); remaining-balance policy line (Change → Full + expand/scroll policy section); receipt card reusing extracted `SummaryRow` + hero Change + status with polite sr-only live region.
  - **Escalation:** `engine.needsAdvanced` (wired 2G-1, now consumed) flips Simple→Full via render-time Pattern A (no effect, no disable); amber `role="status"` banner lists up to 3 translated reasons (`mode.reasons.*`); **manual Simple segment disabled while `needsAdvanced` holds** (new reusable `PaymentModeToggle` in the shared header, + stories); blocked submit in Simple escalates to Full then runs the existing focus-first-blocking flow; focus preserved across flips (shared `amountInputRef` + restore effect when the focused control unmounted).
  - **Wiring:** `initialPaymentMode` prop threaded shell → container (default `'simple'` per locked decision); `new-order-modals.tsx` passes `initialPaymentMode="simple"`. Mode consts `PAYMENT_MODAL_MODE` in `payment-modal-v4.utils.ts` (DB-free, jest-safe). `SummaryRow` extracted to `payment-modal/summary-row.tsx` (+ stories) — shared by Full rail, submit-confirm summary, Simple receipt.
  - **i18n:** new `mode.json` EN+AR (toggle, disabled hint, escalation title, 9 reason labels, simpleView strings). `check:i18n` ✓.
  - **Tests:** +7 (`payment-simple-mode.test.ts` — mode consts + method-chip deriver matrix). **Gates: eslint 0 (repo-wide) · tsc 0 · jest 1580/1580 (156 suites) incl payload oracle 8/8 · build ✓ · check:i18n ✓.** Manual escalation scenarios (verification #9) pending a running app.
- [x] **Phase 5 — keyboard shortcuts (2026-07-03)** — `hooks/use-payment-shortcuts.ts`: Enter/F2/Ctrl(⌘)+Enter fire the SAME footer-CTA gate (`handleSubmit(onSubmitForm, onInvalidForm)`), document-keydown pattern mirroring `ready-date-picker-modal.tsx`. Pure `resolvePaymentShortcutAction` guard matrix (+6 tests): disabled while submit busy · validation blocking · ANY nested dialog/drawer open (close-guard, submit-confirm, cash-drawer, credit-note picker, extra-receipt, auto/manual allocation) · focus in input/textarea/select/contenteditable · Alt combos; plain Enter additionally defers to focused buttons/links (no double-fire). Works in both faces (container-level).
- [x] **Phase 6 — responsive / tablet (2026-07-03)** — `md–xl` 2-pane grid (`280px` tools | workbench) with the tools column sticky + internal scroll (core controls stay above the fold; CTA already in the fixed footer); `<xl` receipt rail → slide-over (`payment-rail-toggle` footer button, backdrop, in-panel close, RTL-aware translate, reduced-motion safe) resetting to the original pinned column at `xl+`; new reusable **`PaymentDockedSummaryBar`** (+ stories) docks Final Total + Change beside the CTA below `xl` for BOTH faces (replaces the footer `FinalOrderTotalPanel` block; the rail's pinned panel unchanged). No new i18n keys (reuses `orderValue.finalTotal`, `mode.simpleView.change`, `sections.receiptBrain`).
- [x] **Phase 7 — full documentation generation (2026-07-03)** — `/documentation` pass: new **`Payment_Modal_v4_Engine_Architecture.md`** (engine slice map, Simple/Full mode guide, shortcut matrix, tablet layout, reusable parts, prd-rules feature-doc checklist — permissions/nav/settings/flags/plan-limits/API/migrations/env all "none/unchanged", i18n + constants deltas listed, test map); review-doc status → Implemented; STATUS + CHANGELOG refreshed; stories exist for every new reusable part (QuickTenderChips, PaymentModeToggle, SummaryRow, PaymentDockedSummaryBar). **Final gates: eslint 0 (repo-wide) · tsc 0 · jest 1586/1586 (157 suites) incl payload oracle 8/8 · production build ✓ · check:i18n ✓.**

## Verification (each phase)
1. `cd web-admin && npm run build` → green. 2. `npx tsc --noEmit` → green. 3. `npx eslint . --quiet` → clean. 4. `npx jest <touched>`; full at phase end. 5. `npm run check:i18n` after strings. 6. **Payload identity (1–2G):** replay fixtures — identical. 7. **Reopen (2G):** every slice resets. 8. **Collect-Payment smoke (2A–2G).** 9. **Escalation (4):** single-cash stays Simple; split/gift/B2B/drawer-block flips to Full + banner; focus retained. 10. Manual: New Order → items → Payment; cash-with-change, split, B2B credit, gift-card PIN, overpayment allocation, deferred policy.
