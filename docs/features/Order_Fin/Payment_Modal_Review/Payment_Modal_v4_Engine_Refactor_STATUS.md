# Payment Modal v4 — `usePaymentEngine` Refactor · STATUS

**Last updated:** 2026-06-27 (Phase 2G-1 engine composition complete — `usePaymentEngine` composed, JSX byte-identical, all automated gates green; 2G-2 view split next)
**Program plan (authoritative):** `docs/features/Order_Fin/Payment_Modal_Review/happy-doodling-volcano.md`
**Design/review:** `Payment_Modal_v4_UX_Review_and_Engine_Plan.md` · **ADR:** `../ADR/ADR_payment_modal_single_engine_two_mode.md`
**Constraints:** no payload-contract change · one engine, two views · behavior-frozen extraction (Phases 1–3) · EN+AR i18n for new strings.

## Progress

| Phase | State | Notes |
|---|---|---|
| 0 — ADR + payload fixtures | ✅ Done | ADR accepted; 8 baseline fixtures captured + validated; oracle test skipped until 2F. |
| 1 — Pure logic extraction | ✅ Done & verified | `use-money-derivations`, `payment-validation` (`derivePaymentValidationItems`), right-rail copy builders, `computeNeedsAdvanced`. 59 new-hook tests + tsc + build. Verbatim lifts; validation/copy don't touch the payload. |
| QA bug-fix track | ✅ 2 fixed, 1 diagnosed | Allocation drawer z-index (Bug 2/3) fixed; gift-card-full backend 400 (Bug 1) diagnosed only. See `Payment_Modal_v4_QA_Bugs_2026-06-27.md`. |
| Collect-Payment `sourceType` fix | ✅ Done (2026-06-27, during 2G-1 smoke) | Pre-existing drift (NOT a 2G-1 regression): `order-collect-payment-modal.tsx` sent phantom `sourceType: 'LATER_COLLECTION'` (absent from `VOUCHER_SOURCE_TYPES`/`sys_fin_vch_source_type_cd`), rejected by `previewAutoAllocationRequestSchema` (`invalid_value`) when a later-collection customer chose auto-allocation. Fixed → `ORDER_PAYMENT_MODAL` (order-scoped; schema default; matches new-order modal) + removed dead `'LATER_COLLECTION'` from the `OverpaymentAllocationSourceType` union. No DB change. eslint 0 / tsc 0 / build ✓. |
| 2A — catalog hook | ✅ Done & verified (2026-06-27) | `use-payment-catalog.ts` (`usePaymentCatalog`). Verbatim lift of the catalog concern out of `payment-modal-v4.tsx`: card-brands + payment-terminals + checkout-options queries, `branchPaymentTerminals`/`checkoutMethods`/`customerCreditOptions`/`realPaymentOptions`/`creditMethodCodes`/`optionByMethodKey`/`getMethodOption`, presentation helpers `getOptionDisplayName`/`getMethodHint`, the `IMMEDIATE_METHOD_CODES`/`GATEWAY_METHOD_CODES` constants, and the `CheckoutSettlementOption`/`CheckoutOptionsResponse`/`PaymentTerminalOption` types. `t`+`isRTL` threaded in; `getPaymentLabel` duplicated in-hook for fallback labels (component keeps its copy for `summaryMethodLabel`, per spec "keep in BOTH"). `walletCreditOption`, `cashDrawers` (2E), `storedValueSummary` (2B) deliberately left in the component. **Additive only (finding 1.9):** `checkoutOptionsIsError` + `refetchCheckoutOptions` returned (not yet consumed; the checkout-options query still swallows failures into empty lists → behavior frozen). **Gates:** eslint clean; tsc 0 errors; payment jest **234 pass / 1 skip** (2F oracle scaffold); production build green (incl. Collect-Payment smoke). Payload identity: catalog is read-only and untouched by `onSubmit`; `getMethodOption`/`getOptionDisplayName` return identical values → payload unchanged (formal fixture replay at 2F). Per program test strategy, query hooks are covered by typecheck+build+suite — no brittle network unit test added. |
| 2B — gift card / promo | ✅ Done & verified (2026-06-27) | `use-gift-card-and-promo.ts` (`useGiftCardAndPromo`). Verbatim lift of the gift/promo **state + effects + reset** (program-plan anchor `:579–806`): 11 state atoms (promo validating/result/applied; gift validating/result/details/applied; PIN value/required/visible/field-error), `resolveGiftCardError`, the "reset details on code change" effect, the PIN-focus effect, and this slice's `open`-reset. `setValue`/`pinInputRef`/`giftCardNumber`/`t`/`tGiftCardErrors` threaded in (DOM refs stay declared in the view per the 2G rule). The async handlers (`handleValidatePromoCode`/`handleFetchGiftCardDetails`/`handleApplyGiftCard`/…) **stay in the component** — they read downstream legs/totals values the engine threads at 2G — and drive the slice via the returned setters. Lint (`react-hooks/set-state-in-effect`, production-grade — no file-level disable): the `open`-reset uses the repo's blessed render-time Pattern A (`react-effects-patterns.md §2`), behavior-equivalent since every atom's initial value equals its reset value; only the code-changed effect keeps a **surgical scoped disable** on its two co-located `setState`s — it legitimately writes the RHF store (external system, §5) and those resets can't move to render-time without racing the `setValue` writes. (Probed the rule to confirm the source component only passes incidentally, via a ref-mutation/import-call that "anchors" its effects — not a pattern worth replicating.) **Gates:** eslint clean; tsc 0 errors; payment jest **234 pass / 1 skip**; production build green (incl. Collect-Payment smoke). Payload identity: gift/promo state shapes + effect bodies are byte-equivalent; `appliedPromoCode`/`appliedGiftCard` feed totals/legs unchanged → payload unchanged (formal fixture replay at 2F). Per program test strategy, this query/effect hook is covered by typecheck+build+suite — no brittle network unit test added. |
| 2C — totals | ✅ Done & verified (2026-06-27) | `use-payment-totals.ts` (`usePaymentTotals`). Verbatim lift of the totals concern (anchor `:902–1118`): `serverTotals`/`totalsLoading` state, the debounced `preview-payment` fetch, the tax-rate + default-tax-profile load, `afterDiscountsForTax`/`fallbackTaxBreakdown`/`displayTaxBreakdown`/`profilesTaxAmount`, and the `totals` memo + `saleTotal` + `checkoutEligibilityAmount`. **Reorder:** the modal now calls hooks dependency-ordered **gift/promo → totals → catalog** (totals reads applied promo/gift; catalog's query key reads `checkoutEligibilityAmount`); `currencyConfig` stays in the component and feeds `decimalPlaces`/`currencyCode` in. The currency-config load split out of the old combined load effect; tax-rate + profiles load moved into the hook. **Lint (production-grade, no disables):** the async `fetchPreview`/tax-load `setState`s run in promise callbacks (not flagged); the debounce effect's `setServerTotals(null)` became a render-time "nothing to preview" clear; the tax-profile `open`-reset uses render-time Pattern A. Types `ServerTotals`/`TaxBreakdownLine`/`TaxProfileEntry` exported from the hook. **Gates:** eslint clean; tsc 0 errors; payment jest **234 pass / 1 skip**; production build green. Payload identity: fetch body + `totals` math byte-equivalent; `serverTotals`/`saleTotal` feed the same consumers → payload unchanged (formal fixture replay at 2F). Query/effect hook → typecheck+build+suite, no brittle network unit test. |
| 2D — legs (+ quickTender) | ✅ Done & verified (2026-06-27) | `use-payment-legs.ts` (`usePaymentLegs`). Verbatim lift of the legs concern: `paymentLegs`/`activeLegIndex`/`activeAmountDraft` state, the `payExtraIntentRef`/`activeLegDraftSyncKeyRef`/`allocationBaselineRef` refs, the three mutators `removeLegAt`/`updateLeg`/`upsertSettlementLeg`, the totals-change **reconciliation** effect, the active-leg **draft-sync** effect, `activeLeg`, and this slice's `open`-reset (render-time Pattern A). Threaded in: `getMethodOption` (catalog), `getLegStoredValueCap`+`focusAmountEditor`+`setIsDirtySinceOpen` (component), `saleTotal`/`giftCardSettlementAmount`/`decimalPlaces`/`t`. **Bridge (the crux):** the hook owns + returns `payExtraIntentRef`; the component keeps the render-time `legs.payExtraIntentRef.current = payExtraIntent` after `usePayExtraCheckout`, and the mutators read `.current` at call-time → behavior identical. **Placement:** hook called after `getMethodOption`/`getLegStoredValueCap`/`focusAmountEditor`/totals, before `useMoneyDerivations`; the check-field-sync + default-method effects (read `paymentLegs`) relocated just after the hook call — behavior-equivalent (they only co-fire with `reset` on `open`, same final values). **Additive seams (designed, unwired in 2D):** `addLeg` (always-append, no dedupe → #3 multi-card a post-2G UI change only) + pure **`quickTender('exact'|'change')`** (#2 direct-change-entry; #1 cash-rounding wraps the tendered computation later). `quickTender` lives in `payment-modal-v4.utils.ts` (jest-safe — no `cmxMessage` chain) and the hook re-exports it (**+9 jest tests**: exact cash/non-cash, change-entry inverse, BHD/OMR 3-dp, cap-never-exceeded, zero-remaining, clamp paths). **Gate-preserving:** `quickTender` applied amount always routes through `deriveLegAppliedAmount` capping → never bypasses overpayment/`payExtra`. **Lint (vs spec's zero-disable expectation):** the "ref-anchored → no fire" probe didn't hold once the effects were isolated in a hook (the React-Compiler `set-state-in-effect` rule fires on any sync setState in an effect whose setter isn't shared with event handlers). Fixes by tier: draft-clear → render-time (no disable); the reconciliation `setPaymentLegs` (coupled to a `cmxMessage` toast → cannot run in render) + the draft-sync `setActiveAmountDraft` (behavior-frozen ref-keyed sync) each keep **one line-scoped, justified** `eslint-disable-next-line` — the doc's prescribed fallback, matching the 2B gift-card precedent. **Gates:** eslint clean; tsc 0 errors; payment jest **243 pass (+9) / 1 skip** (2F oracle); production build ✓. Payload identity: verbatim mutators + identical leg shapes → read-model preserved; **structurally** certified now, full behavioral certification at 2F (oracle) + manual QA (split, cash-with-change, B2B credit, gift-card PIN, overpayment, deferred). Do **not** overclaim "fully certified" before 2F. |
| 2E — cash drawer | ✅ Done & verified (2026-06-27) | `use-cash-drawer.ts` (`useCashDrawer`). Verbatim lift of the cash-drawer concern: active-drawers query, 6 session-state atoms (selected session / dialog open / drawer-to-open / opening balance / opening-in-progress / request error), preferred-drawer `localStorage` helpers, `cashDrawerSessionChoices`/`selectedCashDrawerChoice`/`canOpenNewCashDrawerSession` derivations, the `cashDrawerBlockingMessage` (submit-gate reason), the auto-select effect, and the open-dialog/create-session handlers. `CashDrawerOption`/`CashDrawerSessionOption` types moved into the hook. **Threaded in:** `cashDrawerRequired` (derived from legs/derivations → stays in the component; engine computes at 2G) + `tenantOrgId`/`branchId`/`userId`/`isRTL`/`csrfToken`/`t`. **Stays in the view (2G rule):** the DOM refs `cashDrawerCardRef`/`cashDrawerSelectorCardRef` for blocked-submit scroll/focus. Hook placed right after `cashDrawerRequired`; the query + helpers (formerly defined early, near the stored-value query) moved down with it — nothing between them consumed cash-drawer state. Slice `open`-reset → render-time Pattern A. **Lint:** the auto-select effect reacts to async React-Query data + reads `localStorage` (external systems, react-effects-patterns §5) so it stays an effect; its `setSelectedCashDrawerSessionId` writes carry **line-scoped justified** `eslint-disable-next-line` (tier-2, same as 2D). Removed now-unused component imports (`getCSRFHeader`, `getPreferredCashDrawerStorageKey`, `resolvePreferredCashDrawerSessionId`). **Gates:** eslint clean; tsc 0 errors; payment jest **243 pass / 1 skip** (no new unit tests — query/effect hook per program test strategy); production build ✓. Payload identity: `cashDrawerSessionId` flows into the payload unchanged (present only when required + selected) → structurally preserved; behavioral cert at 2F + manual QA. |
| 2F — submit (+ payload oracle) | ✅ Done & verified (2026-06-27) | `use-payment-submit.ts` exports pure **`buildPaymentPayload`** — verbatim lift of `onSubmitForm`'s payload-assembly tail (`legsWithRefs`/`submitCashLegRef`/`submitOverpaymentResolution` + the `payload` object literal). Submit **orchestration** stays in the view (validation guards, `submissionData`, `safeParse`, confirm dialog, final `onSubmit`) — feeds derived inputs in, parses the result. Module is **pure** (no `cmxMessage`/`'use client'`) so the oracle imports it cleanly. **Oracle ACTIVATED** (`payment-payload-fixtures/payment-payload-oracle.test.ts`): reconstructs `buildPaymentPayload`'s inputs from each of the 8 recorded fixture payloads and asserts `toEqual` — **all 8 rebuild deep-identically**, freezing the assembly contract (conditional spreads, field selection, leg-ref stamping, `taxProfileIds`/`paymentLegs` order, overpayment fallback). **This retro-certifies the payload contract across 2A–2F (proven, not asserted).** Removed now-unused component imports (`ensurePaymentLegRefs`, `buildOverpaymentResolutionPayload`). **Gates:** eslint clean; tsc 0 errors; jest **252 pass / 0 skip** (20 suites — oracle now active); production build ✓. **Deferred:** the view-coupled `usePaymentSubmit` orchestration hook (cmxMessage guards / confirm flow / refs) folds into the 2G engine + view split; the pure builder + oracle is the 2F certification deliverable. |
| 2G-1 — engine composition | ✅ Done & verified (2026-06-27) · ⏳ manual gates pending | `use-payment-engine.ts` (`usePaymentEngine`) composes the 7 slices in their original dependency order (gift/promo → totals → catalog → stored-value query → legs → money-derivations → pay-extra → cash-drawer), owns the render-time `payExtraIntentRef` bridge, every cross-slice derivation (`giftCardSettlementAmount`, live wallet/advance, `getLegStoredValueCap`, overpayment permission flags, `checkoutExcessLegs`/`canEnablePayExtra`/`primaryCashLegRef`/`payExtraResetFingerprint`, `editableLegEntries`, `displayChangeAmount`/`netCashRetainedAmount`, `cashDrawerRequired`, `effectiveOutstandingPolicy`, leg-validation predicates, `validationItems`, `submitBusy`/`submitHasBlockingIssues`, `rightRailState`), the `cashOverRemainingNotice` state, and the non-DOM handlers (method / credit-note / customer-credit select, the gift/promo async handlers, outstanding-policy, keypad, cycle/fill leg). **`needsAdvanced` wired via `computeNeedsAdvanced` and returned but NOT consumed** (its Phase-4 Simple-mode consumer arrives later — mirrors catalog `isError` shipped unused in 2A). The modal calls `usePaymentEngine({…})` then **re-destructures the grouped slices back into the original local names → the entire JSX is byte-identical (no JSX move in 2G-1)**; `payment-modal-v4.tsx` keeps the RHF form, `currencyConfig` load + open-reset effect, all DOM refs + `focusAmountEditor`/scroll/focus helpers, section state, the view-model formatters (labels / summary items / order-value breakdown / section-visibility) and the submit gate (`onSubmitForm` + confirm-dialog state). **Component 4,890 → 4,025 lines.** Effect order preserved (shell currencyConfig+reset effects register before the engine's internal effects — benign because every slice open-reset is render-phase Pattern A and the open-gated effects converge). One type-only fix: engine param `defaultPaymentMethod` typed `PaymentFormData['paymentMethod']` (not `PaymentMethodCode`) — RHF subtlety where the broad code is assignable to the form field at the component call site but not via a fresh interface-typed `setValue` param inside the hook (verified against the pre-edit backup, which compiled clean). **Gates: eslint 0 errors** (1 pre-existing `watch()` incompatible-library warning, behavior-frozen) **· tsc 0 · jest 252/252 incl. the payload oracle + needs-advanced · `npm run build` ✓.** Payload identity holds (`buildPaymentPayload` untouched; oracle green). **Manual gates PENDING (need a running app + human): reopen-resets-every-slice + Collect-Payment smoke.** Not committed. |
| 2G-2 — Full-view split | ⬜ **Next — execute as dedicated run** | Relocate ~2,400 JSX lines into `ui/payment-full-view.tsx` (`PaymentFullView` owns the DOM refs + scroll/focus helpers + section state + **calls `usePaymentEngine`**); `payment-modal-v4.tsx` becomes the thin shell (form owner + submit orchestration + dialog frame) rendering `<PaymentFullView/>`. Re-confirm `order-collect-payment-modal.tsx` (shares `usePayExtraCheckout`) stays un-regressed. **Decomposed execution spec:** `payment-modal-v4-phase-2G-engine-view-split-EXECUTION-SPEC.md` (this folder) — boundary table (engine/view/shell) + refs-crux (engine is called from the VIEW, which owns the refs it consumes). Gate: eslint/tsc/jest(incl oracle)/build + the **full manual QA pass** (2D guide + reopen + Collect-Payment + 6 finance scenarios) since the oracle is payload-only. |
| 3 — Full-view UX quick wins | ⬜ | dedupe numbers, quick-tender chips, aria-live, blocker dedupe, etc. |
| 4 — Simple mode + escalation | ⬜ | wire `computeNeedsAdvanced`; `payment-simple-view.tsx`. |
| 5 — keyboard shortcuts | ⬜ | guardrailed Enter/F2/Ctrl+Enter. |
| 6 — responsive / tablet | ⬜ | 2-pane + docked CTA. |
| 7 — full documentation generation | ⬜ | `/documentation` pass over the prd-rules feature checklist. |

## Validation (Phase 0–1 + bug fixes)
eslint clean · `tsc` clean on touched files · production build green · 114 payment-suite + 59 new-hook tests pass.

## Payload fixtures — purpose (ORACLE NOW LIVE as of 2F)
The 8 baseline fixtures are the automated payload oracle. **As of Phase 2F the oracle is ACTIVE and green:** `buildPaymentPayload` is extracted (pure, in `use-payment-submit.ts`) and `payment-payload-oracle.test.ts` reconstructs its inputs from each recorded fixture payload and asserts deep-equality — all 8 pass, so the submit-payload contract is frozen and CI-guarded across 2A–2F. Any future extraction that perturbs the payload assembly now breaks this gate.

---

# Phase 2A — Execution Spec (ready to run in a fresh session)

> **Mandatory first step:** load the `/frontend` skill before writing any code. No DB/nav/permission/API change in this phase. Mirror the existing extracted-hook style in `web-admin/src/features/orders/hooks/use-money-derivations.ts` and `.../payment-validation.ts` (pure where possible, `'use client'`, JSDoc, behavior-frozen verbatim lifts).

**Goal:** extract the **catalog** concern from `web-admin/src/features/orders/ui/payment-modal-v4.tsx` into `web-admin/src/features/orders/hooks/use-payment-catalog.ts`, with **zero behavior change**, and **add `isError` + `refetch`** to the checkout-options query (enables finding 1.9, the method-list error/retry state, in Phase 3).

### Key gotcha (discovered, do not re-derive)
`checkoutEligibilityAmount = serverTotals?.saleTotal ?? checkoutAmount ?? total` — the checkout-options query key depends on **totals**. So **catalog ← totals**: the hook must receive `checkoutEligibilityAmount` as an **input param** (computed in the component, which still owns `serverTotals` until Phase 2C). Do not move `serverTotals`/`checkoutEligibilityAmount` into this hook.

### This is a 12-region, non-contiguous extraction
Catalog lines are physically interleaved with **cash-drawer** and **stored-value** code that MUST stay in the component. Line numbers below are pre-extraction approximations — re-grep each symbol before editing.

**Relocate into the hook (export the types):**
- Types: `CheckoutSettlementOption` (~:200–222), `CheckoutOptionsResponse` (~:224–227), `PaymentTerminalOption` (~:718–725). NOTE: `CheckoutSettlementOption` is referenced in ~15 places in the component → component imports it back from the hook.
- Module constants: `IMMEDIATE_METHOD_CODES` (~:1167–1174), `GATEWAY_METHOD_CODES` (~:1176–1181).
- Queries: `cardBrands` (~:706–716), `paymentTerminals` (~:727–737) + `branchPaymentTerminals` (~:739–745), `checkoutOptions` (~:747–761, add `isError`+`refetch`).
- Derived: `checkoutMethods` (~:792), `customerCreditOptions` (~:793), `realPaymentOptions` (~:1182–1190), `creditMethodCodes` (~:1192–1195), `optionByMethodKey` (~:1207–1212), `getMethodOption` (~:1286–1291), `getOptionDisplayName` (~:1310–1315), `getMethodHint` (~:1317–1322).

**Leave in the component (different concerns):** `cashDrawers` query (~:763–790) and all cash-drawer helpers; `storedValueSummary` query (~:800+) and `liveWalletBalance`/`getLegStoredValueCap`/`walletCreditOption` (stored-value); `currencyConfig`. The component derives `walletCreditOption` from the hook's returned `customerCreditOptions`.

### Hook contract
```ts
usePaymentCatalog({
  open, tenantOrgId, branchId, customerId,
  checkoutEligibilityAmount,   // threaded from serverTotals in the component
  isRetailOnlyOrder, isRTL,
}): {
  cardBrands, paymentTerminals, branchPaymentTerminals,
  checkoutMethods, customerCreditOptions,
  checkoutMethodsLoading, checkoutOptionsIsError, refetchCheckoutOptions,   // NEW: isError + refetch
  realPaymentOptions, creditMethodCodes, optionByMethodKey,
  getMethodOption, getOptionDisplayName, getMethodHint,
}
```
- `getOptionDisplayName` / `getMethodHint` use `getPaymentLabel` (keep that import in BOTH hook and component — also used by `summaryMethodLabel`).
- Component re-wires ~20 consumers to the destructured hook outputs, and feeds `getMethodOption` into the existing `useMoneyDerivations(...)` + `derivePaymentValidationItems(...)` call sites (already parameterized for it).

### Validation gate (all must pass)
1. `cd web-admin && npx eslint <touched files> --quiet` → clean (watch for now-unused imports: `useQuery`, `OrgCardBrandConfig`, `getPaymentLabel`, `PAYMENT_METHODS` — only remove if truly unused in the component after the cut).
2. `npx tsc --noEmit` → no NEW errors in touched files (repo has ~12 pre-existing unrelated errors: b2b pages, catalog/orders-access, use-category-products — ignore those).
3. `npx jest __tests__/features/orders/...` payment suites → green (114 baseline).
4. `npm run build` → green.
5. **Collect-Payment smoke:** confirm `order-collect-payment-modal.tsx` still builds (it shares `usePayExtraCheckout`, not catalog, but verify no fallout).
6. Per-step closeout: update this STATUS table + CHANGELOG; `/documentation` at phase end.

### Test note
Test files in this repo must use **separate `import type { … }`** lines (the babel-jest transform rejects inline `import { type X }`). Query/effect hooks like this one get typecheck + build + manual coverage, not brittle network unit tests (per the program test strategy).

### After 2A
Next is **2B gift card/promo**, then **2C totals** (which finally lets the component stop computing `checkoutEligibilityAmount` inline once totals is a hook). See the program plan for the full 2A–2G order.
