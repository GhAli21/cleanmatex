# RESUME — POS Session & Cash Drawer Hardening (session continuation)

**Updated:** 2026-09-25 (latest — cloud session on branch `claude/peaceful-babbage-eifpj0`). **The ▶ NOW entry directly below is the resume point.** All 7 R1 migrations applied. Writer items **W1–W10, W12, W14, W15 are done** (STATUS D36, D37). Next: **W11 + Cash in / Cash out** (§4B.2a-A), pending-deposit buttons (§4B.2a-B), R1 exit gates.

---

## ▶ NOW — 2026-09-25 (latest) — W2/W3/W10 done; next W11 + Cash in / Cash out

**Owner direction:** "why were W2/W3/W10 missed — go ahead". Answer + decisions in **STATUS.md D37** (short version: the earlier hand-off list dropped them; W1 had only covered their posting half).

### Done this pass

| Item | What changed | Files |
|---|---|---|
| **W10** (money bug) | Order-screen Verify → canonical VERIFY transition, so a verified cash leg is recognised in the drawer ledger. Response fields kept (+ `cashRecognized`); key `order_payment_verify:{paymentId}`; errors carry `code`. `verifyPaymentTx`, its types and test deleted. Verify button translates ledger codes. | `app/api/v1/orders/[id]/payments/[paymentId]/verify/route.ts`, `lib/services/order-settlement.service.ts`, `lib/constants/order-financial.ts` (comment), `src/features/orders/ui/order-financial/order-payments-credits-tables.tsx`, new `__tests__/api/v1/orders/payment-verify.route.test.ts`, deleted `__tests__/services/verify-payment.service.test.ts` |
| **W2** | Planner pre-transaction session lookup removed (gate decides in-tx); hint-required check kept. Submit route maps `CashDrawerLedgerError` → 422 code (was falling to `ORDER_SUBMIT_FAILED` since W1). Payment modal: every ledger code translated; `CASH_DRAWER_REQUIRED` / `CASH_DRAWER_SESSION_NOT_OPEN` / `DRAWER_SESSION_CLOSING` are new server-mirror `PAYMENT_REASON`s routed to the drawer guard. | `lib/services/order-settlement-planner.service.ts`, `app/api/v1/orders/submit-order/route.ts`, `src/features/orders/hooks/use-order-submission.ts`, `src/features/orders/payment/domain/{payment-reasons,server-error-routing}.ts`, tests: planner (no session read; hint still required), server-error-routing |
| **W3** | `collectPaymentTx` + standalone `transitionPaymentTx` in `withTenantContext`; both collect routes map ledger refusals to 422 codes; collect modal translates them. | `lib/services/order-settlement.service.ts`, `lib/services/payment-transition.service.ts`, `app/api/v1/orders/[id]/{collect-payment,payments}/route.ts`, `src/features/orders/ui/collect-payment/order-collect-payment-modal.tsx` |

### Validation this pass
- jest (full web-admin): **332/332 suites, 2963/2963 tests**.
- `npx eslint . --quiet`: **clean**. `npm run build`: **success**.
- Scoped typecheck (`NODE_OPTIONS=--max-old-space-size=13312 npx tsc --noEmit -p tsconfig.clf-check.json`): **only the 2 pre-existing `lib/db/prisma.ts` errors**.

### 💻 Moving to the local laptop (owner, 2026-09-25) — read first
- **All work is on branch `claude/peaceful-babbage-eifpj0`** (commits `38a973c` W4/W5/W6/W12/W14/W15, `1faf134` W2/W3/W10, plus this docs commit). It is **not merged to `main`** and no PR was opened. Locally: `git fetch origin claude/peaceful-babbage-eifpj0` then check it out (or merge it into your working branch) before continuing.
- **No new migrations** in either pass — nothing to apply. DB state unchanged: `0523`, `0526`–`0530` applied (local + remote).
- After pulling: `npm ci` (root, workspaces) → `cd web-admin && npx prisma generate`.
- **Typecheck on the laptop:** plain `npm run typecheck` may still hit the `.next/dev/types/routes.d.ts` issue / OOM; use `NODE_OPTIONS=--max-old-space-size=13312 npx tsc --noEmit -p tsconfig.clf-check.json` (tracked file; ~15 min, ~12 GB RAM). Expected result: only the 2 pre-existing `lib/db/prisma.ts` errors.
- Known-good baseline at hand-off: jest 332/332 suites (2963 tests), `npx eslint . --quiet` clean, `npm run build` success, `check:i18n` passed.
- **Manual QA worth doing locally** (not possible in the cloud container — no running DB/UI): (1) cash customer-account receipt with change → drawer expected cash = receipt amount; (2) order-screen **Verify** of a PENDING cash leg → drawer expected cash increases; (3) submit / collect with a closed drawer session → drawer guard shows "no open session" (translated); (4) cash refund with the execution flag OFF → voucher + drawer OUT; (5) gift-card sale always asks for a tender; (6) POS sessions list without `pos_session:view_all` shows only own sessions and `?userId=` cannot widen it.

### ▶ Pick up exactly here
1. **W11 + §4B.2a-A together** — Cash in / Cash out dialog + API (allow-list `DRAWER_CASH_IN_OUT_ROLES` in `lib/constants/cash-drawer.ts`, permission `cash_drawer:record_movement`, INTERACTIVE voucher, errors via `cashControl.ledgerErrors`), then delete `recordMovement`, the `cash-movement` route, `addDrawerMovement` and the old movement dialog.
2. §4B.2a-B pending-deposit button on the remaining screens (confirm which are done).
3. R1 exit gates (full eslint / typecheck / build / jest, QA guide, STATUS R1-complete row).

---

## ✅ 2026-09-25 (earlier, superseded by ▶ NOW above) — W4/W5/W6/W12/W14/W15 done

**Owner direction this pass:** "continue W6, then W4/W5, W11/W12/W14/W15" and then "you decide — production-ready, no gaps". Every design call made is recorded in **STATUS.md D36** (read it; it explains *why*, including the rejected options).

### Done this pass (code + tests; see D36 for rationale)

| Item | What changed | Files |
|---|---|---|
| **W6** customer-account receipt | One `RECEIPT_VOUCHER` + **one `CUSTOMER_CREDIT_RECEIPT` line**, posted via `postAndWireBizVoucher` INTERACTIVE (gate decides drawer). Allocation executor unchanged and still the only effect writer. Stable error codes (`CUSTOMER_RECEIPT_POST_ERRORS`), route CSRF + 422 codes. Reversal core refuses receipt vouchers. Temporary legacy mirror handler (retire in R3 with W13). Screen reuses `StoredValueTenderFields` (drawer picker, change due, **new opt-in bank/cheque reference fields**), `cmxMessage`, no `'OMR'` fallback, idempotency key = `car_{previewId}`. Access contract for `/dashboard/customers/account-receipt` gained its real `apiDependencies` (derive + hand-corrected methods/guards). | `lib/services/customer-receipt-posting.service.ts`, `app/api/v1/customer-receipts/post/route.ts`, `lib/services/wiring/customer-receipt-cash-drawer-wiring.handler.ts` (new), `lib/services/voucher-wiring.service.ts`, `lib/services/voucher-line-reversal.service.ts`, `app/api/v1/finance/vouchers/[voucherId]/reverse/route.ts`, `lib/types/customer-receipt-allocation.ts`, `lib/validations/customer-receipt-allocation-schema.ts`, `src/features/customers/ui/customer-account-receipt-client.tsx`, `src/features/customers/ui/stored-value-tender-fields.tsx`, `src/features/customers/access/customers-access.ts` |
| Bug fix (found during W6) | Stored-value cash mirror wrote a `CASH_OUT` for change on top of a net IN → old close screens short by the change. Change row removed. | `lib/services/wiring/stored-value-cash-drawer-wiring.handler.ts` |
| **W4** refund | CASH refund always executes (drawer-session hint required; gate decides OPEN) whatever `order_fin_refund_execution` says; flag now gates ORIGINAL_METHOD only. `processRefund` in `withTenantContext`. Route maps `CashDrawerLedgerError` → 422 code. Refund dialog always asks for a drawer on CASH, shows translated ledger errors. **Movement read-back kept until R3** (AR reconciliation needs it). | `lib/services/order-refund.service.ts`, `app/api/v1/orders/refunds/[refundId]/process/route.ts`, `src/features/billing/ui/refunds-list-client.tsx` |
| **W5** gift card | No-tender sale (`sellGiftCard`, `sellGiftCardAction`) deleted; dialog always tendered; `sellGiftCardWithTenderAction` not flag-gated; returns ledger error code. `fundStoredValue` in `withTenantContext`. | `lib/services/gift-card-service.ts`, `app/actions/marketing/gift-card-actions.ts`, `src/features/marketing/ui/gift-card-sell-dialog.tsx`, `lib/services/stored-value-funding.service.ts`, `docs/features/Gift_Cards/README.md` |
| **W12** | 4 dead legacy actions deleted; drawer CRUD actions gained `payment_config:view` / `payment_config:manage` checks (had none). Contract action `manageCashDrawers` added. | `app/actions/payment-config/cash-drawers-actions.ts`, `src/features/payment-config/access/payment-config-access.ts` |
| **W14** | `settleOrderTx` non-wiring branch + `settleOrder` wrapper deleted; submit fails closed (`SETTLEMENT_LEGS_REQUIRE_RECEIPT_VOUCHER`). | `lib/services/order-settlement.service.ts`, `lib/services/order-submit-orchestrator.service.ts` |
| **W15** | Every action in `billing/cash-drawer-actions.ts` checks the same permission as its API route (returns are inline literals on purpose — with `strict: false` a shared const breaks callers' `result.error` narrowing). Rewire half → R2. | `app/actions/billing/cash-drawer-actions.ts` |
| Bug fix (found by typecheck, pre-existing) | `GET /api/v1/pos-sessions` let the `?userId=` filter overwrite the actor id used by the own-scope restriction (visibility bypass + empty default list). Filter renamed `filterUserId`. Six `AuditExtraRow` entries got their required `key`. | `lib/services/pos-session.service.ts`, `app/api/v1/pos-sessions/route.ts`, `src/features/pos-sessions/ui/pos-sessions-screen.tsx` |
| i18n | `customers.accountReceipt.*` (tender, errors), `customers.storedValue.funding.*` (references), **new shared `cashControl.ledgerErrors.<CODE>`** (all 22 `CASH_LEDGER_ERRORS`, EN+AR) — use it for every future ledger-error display. | `messages/{en,ar}/{customers,cashControl}.json` |

Tests: new `customer-receipt-posting.service.test.ts`, `customer-receipt-cash-drawer-wiring.handler.test.ts`; updated reversal-core (receipt refusal), stored-value mirror (no change row), refund B9 (CASH always executes), B01 matrix / refund service / refund-flow (voucher mocks + session hint), settlement (settleOrderTx no longer writes facts), gift-card service (sellGiftCard block removed).

### Validation this pass
- **jest (full web-admin suite): 332/332 suites, 2959/2959 tests passing.**
- **`npx eslint . --quiet` (whole web-admin): clean.**
- **`npm run build`: success.**
- **`npm run check:i18n`: passed** (EN/AR trees, keys, placeholders aligned; only pre-existing warnings).
- **`check:ui-access-contract --route=/dashboard/customers/account-receipt --wire`: PASS**; `sync:ui-access-contract` run (inventories refreshed).
- **Typecheck:** plain `tsc` OOMs on this codebase even with a large heap; the scoped `tsconfig.clf-check.json` completes with `NODE_OPTIONS=--max-old-space-size=13312` (~15 min, ~12 GB — needs a 16 GB machine). Result: **only 2 errors, both in `lib/db/prisma.ts`** (Prisma `$extends` type complexity — pre-existing platform item tied to D32, not CLF). Everything this pass touched is type-clean; the pre-existing POS-session errors it surfaced were fixed (see table).

### ▶ Pick up exactly here
1. **W2** — `order-settlement-planner.service.ts:~318-326`: delete the pre-transaction `CASH_DRAWER_SESSION_REQUIRED` / `CASH_DRAWER_SESSION_CLOSED` check (the gate decides inside the tx); make sure order submit passes the drawer/session hint and maps `CashDrawerLedgerError` in its route.
2. **W3** — `collectPaymentTx` (`order-settlement.service.ts`): wrap in `withTenantContext`; map `CashDrawerLedgerError` in both collect routes (`orders/[id]/collect-payment`, `orders/[id]/payments`).
3. **W10** — `verifyPaymentTx` + route `orders/[id]/payments/[paymentId]/verify`: delegate to `transitionPayment` VERIFY, delete `verifyPaymentTx`.
4. **W11 + §4B.2a-A together** — Cash in / Cash out dialog + API (allow-list `DRAWER_CASH_IN_OUT_ROLES` in `lib/constants/cash-drawer.ts`, permission `cash_drawer:record_movement`, INTERACTIVE voucher), then delete `recordMovement`, the `cash-movement` route and `addDrawerMovement` (now permission-gated, W15) and the old movement dialog. Use `cashControl.ledgerErrors` for errors.
5. §4B.2a-B pending-deposit button on the remaining screens (confirm which are done), then R1 exit gates (full eslint / typecheck / build / jest, QA guide, STATUS R1-complete row).

**Follow-ups recorded, not done:** `/dashboard/marketing/gift-cards` contract is missing pre-existing `apiDependencies` (checkout-options, cash-drawers) and `actions` — run `derive --route=/dashboard/marketing/gift-cards --apply` and hand-correct methods; wallet top-up / advance "no-tender admin adjustment" path (stored-value tab, flag off) is an adjustment, not a sale — left as is, flag for owner review; the order cash mirror (`cash-drawer-wiring.handler.ts`) also writes a change `CASH_OUT` but its rows carry `order_payment_id`, so the legacy formula excludes them — correct, no change.

---

---

## 🟡 2026-09-25 (newest, parallel thread) — A6-1b planned, awaiting owner decision; moving to local laptop

**Scope of this thread:** A6 (cash tender/change rounding) only. The CLF thread (W-items, entry below) is owned by **another session** — do not mix the two.

- Loaded `/database`, `/multitenancy`, `/backend`. Analysed the code; **no code or migrations written.**
- Findings, risk and options recorded in STATUS **D36**; full step-by-step plan in `IMPLEMENTATION_PLAN.md` §A6 "A6-1b execution plan".
- **Resume by getting the owner's answer to:** (a) preview-only rounding UI now, residue persistence after CLF (recommended) vs (b) persist a `ROUNDING` voucher line now (collides with CLF writers); and confirm the resolver lives in a new `lib/services/cash-rounding.service.ts`. Then execute the plan steps 1-6 in order.

---

## 🟢 2026-09-25 (earlier, superseded by ▶ NOW above) — W1/W7/W8/W9 test-fix pass CLOSED; found+fixed a real gate-leak bug; typecheck OOM'd (not a real error) — EXACT resume point

**Picks up from:** the entry below ("R1 code: W1/W7/W8/W9 done, mid test-fix"), items 1-2 of its "NOT done yet" list. Item 3 (lint + typecheck) is half-done — see below.

### Done and verified this pass

1. **Ran `npx jest voucher-line-reversal.service.test` — found a real bug, not just a test gap.** In `voucher-line-reversal.service.ts`'s per-line loop (around the `gateLines.push(...)` call), the `cashFamily` guard only ever fired *inside* two `if (cashFamily && ...)` conditions (PENDING/NONE abandon, pre-CLF null-effect skip) — there was no branch that excluded a **non**-cash-family line (e.g. `CARD`, `CHECK`) from reaching the gate at all. Every reversal with a mixed cash+non-cash line set was sending the non-cash mirror line into `stampCashLinesTx` alongside the real cash one. **Fixed:** added `if (!cashFamily) continue;` right after computing `cashFamily`, before the PENDING/NONE and null-effect checks (which now only need to run for cash-family lines, so their own `cashFamily &&` prefix was simplified away too). `voucher-line-reversal.service.test.ts` now **13/13 passing** (was 12/13 — this is the test that caught it, "sends only the cash-family line to the gate...").
2. **Rewrote `voucher-reversal.service.test.ts`** exactly as directed — mocks `@/lib/services/voucher-line-reversal.service` → `reverseVoucherLinesInTx` at the module boundary (same pattern as `payment-transition.service.test.ts`), and now tests only what the thin wrapper (`reverseBizVoucher`) does on top of the mocked core: dispatch to `unwindOrderPaymentLine`/`unwindOrderCreditApplicationLine`/`unwindStoredValueFundingLine` by `pair.originalLineRole`, the `voucher_unwind:{reversalVoucherId}:{originalLineId}` idempotency-key format (payment) and `voucher_unwind:{reversalVoucherId}` prefix (credit app), `pair.reversalSessionId` passed straight through as `cashDrawerSessionId` (untouched — not re-derived), the `VOUCHER_UNWIND_DRAWER_SESSION_REQUIRED` throw when a cash REVERSE gets a null session, REVERSE-vs-VOID action selection by source payment_status, the already-unwound short-circuit, `recalculateOrderFinancialSnapshotTx` running exactly once per **distinct** order even across two pairs on the same order, and the `unwindEnabled=false` short-circuit (core called, zero unwind/recalc calls). The old "not found" / "no POSTED lines" / "party_name" tests were **not** duplicated here — they already live in `voucher-line-reversal.service.test.ts` (the core's own test file), per the plan. **14/14 passing**, new file entirely (old file had stale pre-CLF test bodies asserting inline mirror-creation logic that moved into the core module).
3. **`npx eslint` on all 3 touched files** (`lib/services/voucher-line-reversal.service.ts`, `__tests__/services/voucher-line-reversal.service.test.ts`, `__tests__/services/voucher-reversal.service.test.ts`) — **clean**, zero errors.

### NOT done yet — pick up exactly here

1. **`npm run typecheck` (plain, whole-project `tsc --noEmit`) crashed with a JS heap OOM** (`FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory`, ~4GB heap) before producing any real type-error output — **this is not a signal about the code**, it never got far enough to check anything meaningful. Per the "Environment note" in the entry below, the documented next step is the scoped config: from `web-admin/`, run `npx tsc --noEmit -p tsconfig.clf-check.json` (recreate the file from `tsconfig.json` + a trimmed `include` list if it's missing — it's an untracked scratch file). **This was not yet attempted this pass** — the session was asked to stop before it. If the scoped run *also* OOMs, retry with a raised heap budget, e.g. `NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.clf-check.json` (or higher — this machine's `tsc` run got to ~4GB before dying, so the default `--max-old-space-size` is being hit; whether that's newly worse or was always this close to the edge is unconfirmed, first time whole-project `tsc` was run standalone rather than via `npm run typecheck`'s wrapper in this program).
2. Once a clean typecheck signal is in hand: **continue the remaining R1 writer items** (`IMPLEMENTATION_PLAN.md` §4B.5) exactly as the entry below already specified — **W6** (customer-account receipts — rewrite `postCustomerAccountReceipt` to create real voucher lines instead of a header-only voucher + direct movement writes), **W4/W5** (delete the record-only cash-refund branch and the no-tender gift-card activation path — every cash event becomes a voucher, no flag-gated skip), **W11/W12/W14/W15** (delete `recordMovement`+its route, the 5 dead legacy actions in `cash-drawers-actions.ts`, the dead `settleOrder` branch, add `requirePermission` to `billing/cash-drawer-actions.ts`). **W13 stays deferred to R3** — do not touch the 3 mirror wiring handlers yet.
3. After the writer items: §4B.2a-A (Cash in/Cash out dialog + API), §4B.2a-B (pending-deposit ensure button on the 2 remaining screens — done only on the drawer tab per earlier session, confirm), then R1 exit gates (full eslint/typecheck/build/jest, QA guide, STATUS.md R1-complete row).

### Environment note update — same underlying issue, now hit a second way

The plain `npm run typecheck` OOM above is very likely the **same** underlying `.next/dev/types/routes.d.ts` corruption/size issue already flagged in the entry below (that entry found `tsc` failing outright on that file; this pass instead saw it exhaust heap trying) — not confirmed identical, but treat as the same root cause until proven otherwise. Don't spend time diagnosing Node's V8 heap behavior itself; go straight to the scoped `tsconfig.clf-check.json` path documented below.

---

## 🟡 2026-09-25 (earlier) — R1 code: W1/W7/W8/W9 done, mid test-fix

**Plan reference:** `IMPLEMENTATION_PLAN.md` §4B.5 (writer table), items W1, W7, W8, W9. §4B.2a for the 5 owner-approved additions (A-E), all already folded into the plan text — nothing new to decide, this entry is pure implementation status.

### Done and verified (typecheck + tests green)

- **W1 (voucher posting → the gate).** `voucher-wiring.service.ts`'s `postAndWireBizVoucherInTx` calls `stampCashLinesTx` at step 8a, **before** DRAFT lines flip to POSTED (the immutability trigger requires this order). `postAndWireBizVoucher` / `postAndWireBizVoucherInTx` both gained a **required** `mode: CashGateMode` parameter (no default — every one of the 7 call sites had to declare INTERACTIVE or DEFERRED on purpose). Assigned: order submit, later collection, refund execution, stored-value funding → `INTERACTIVE`; manual Finance voucher post (action + API route) → `DEFERRED`.
- **Core ledger (CLF-3, recorded earlier in STATUS.md D35).** `lib/services/cash-drawer-ledger/{cash-drawer-lock,cash-drawer-ledger-policy,cash-drawer-ledger-gate,cash-drawer-ledger.repository,cash-drawer-errors}.ts`, `lib/types/cash-drawer-ledger.ts`. Policy 28/28 tests, gate 5/5 on the real local DB.
- **`transitionPaymentTx` now takes an optional `tx`** (second param, `transitionPaymentTx(params, tx?)` — matches `postAndWireBizVoucher`'s own convention, not the plan's literal `transitionPaymentTx(tx, …)` wording; deliberate, noted here so it isn't "corrected" back). Core logic extracted to `transitionPaymentCoreTx(tx, params)`; the exported wrapper opens its own transaction only when no `tx` is supplied.
- **W7 (VERIFY).** `maybeCreateDeferredCashMovementTx` **deleted**. VERIFY of a cash leg now calls `recognizeCashLineTx(tx, {mode: DEFERRED}, lineId)` when `fin_voucher_trx_line_id` is set; `result.deferredCashMovementCreated` now means "recognised in the ledger", not "movement row created".
- **CANCEL/FAIL_BOUNCE/VOID guard (new, not in the original B10 code).** A leg whose voucher line has `cash_effect_code = 'DRAWER'` (already recognised) now throws `CASH_LEG_MUST_REVERSE` **before** the row is flipped — correcting live drawer cash must go through REVERSE. A `PENDING` cash line on CANCEL/FAIL_BOUNCE/VOID is abandoned via `abandonPendingCashLineTx` (→ `NONE`), not left dangling.
- **W9 (`voucher-line-reversal.service.ts`, new file) + `voucher-reversal.service.ts` rewritten.** `reverseVoucherLinesInTx(tx, {tenantOrgId, voucherId, reason, userId, lineIds?, cashDrawerId?})` is the new core: creates the mirror voucher + DRAFT lines, sends only the cash-family lines to `stampCashLinesTx` (mode `DEFERRED`), flips DRAFT→POSTED, marks originals REVERSED, computes REVERSED vs PARTIALLY_REVERSED, writes audit+outbox. A `PENDING`/`NONE` cash original is abandoned instead of mirrored; a pre-CLF original with **no** `cash_effect_code` at all gets no one-sided gate stamp (left alone — it predates the ledger). `voucher-reversal.service.ts`'s `reverseBizVoucher` is now a thin wrapper: calls the core, then (if `order_fin_voucher_unwind` is on) drives `unwindOrderPaymentLine`/`unwindOrderCreditApplicationLine`/`unwindStoredValueFundingLine` per `result.pairs`. `unwindOrderPaymentLine` no longer resolves its own drawer session (deleted `resolveOpenDrawerSessionId`) — it uses `pair.reversalSessionId`, the **same** session the gate already decided for the mirror line, passed straight through to `transitionPaymentTx(params, tx)` (joins the caller's transaction — closes the old "two separate transactions" bug named in D22's original writeup).
- **W8 (standalone REVERSE, `pending-payments/[paymentId]/transition` route).** `transitionPaymentCoreTx`'s REVERSE branch now calls `reverseVoucherLinesInTx` itself for cash-family legs (using the payment row's own `fin_voucher_id`/`fin_voucher_trx_line_id`), replacing the deleted `maybeCreateReversalCompensatingMovementTx`. **Idempotent by construction, not by a flag:** before calling the core, it checks for an existing mirror line (`WHERE reversed_line_id = originalLineId`) — this is what stops the nested case (W9's own unwind loop calling back into REVERSE) from double-reversing the same line. Throws `CASH_LEG_HAS_NO_VOUCHER_LINE` if the payment has no linked voucher line. The old `cashDrawerSessionId` client param is now unused/deprecated (kept only so old idempotency-cache hashes don't change); `CASH_DRAWER_SESSION_REQUIRED`/`_NOT_OPEN` can no longer be thrown by REVERSE (REVERSE is DEFERRED mode — the gate never refuses on session state).
- **Both routes** (`pending-payments/.../transition`, `finance/vouchers/[voucherId]/reverse`) now catch `CashDrawerLedgerError` and return its `.code` at HTTP 422.
- **`__tests__/services/payment-transition.service.test.ts`** fully rewritten for the above — **38/38 passing**. New mocks: `recognizeCashLineTx`, `abandonPendingCashLineTx`, `reverseVoucherLinesInTx` (all mocked at the module boundary — this file tests *how payment-transition calls them*, not the gate/core's own decisions).
- **`__tests__/services/voucher-line-reversal.service.test.ts`** — **new file, written, NOT YET RUN.** Covers: mirror creation, DRAFT→POSTED sequencing, gate call shape (only cash lines, DEFERRED mode), session written back from the gate (not the original hint), PENDING-abandon, pre-CLF no-stamp, party_name-from-header, full vs partial reversal / REVERSED vs PARTIALLY_REVERSED, and all 4 rejection paths.

### NOT done yet — pick up exactly here

1. **Run `npx jest voucher-line-reversal.service.test` and fix whatever fails.** Written against the real `reverseVoucherLinesInTx` (not mocked), so this is the first real signal on that new file's correctness — treat any failure as a real bug to fix, not a test to weaken.
2. **`__tests__/services/voucher-reversal.service.test.ts` still has the OLD test bodies and WILL fail as-is** (5 of its tests assert the pre-CLF inline logic — header lock, line creation, party_name lookup — that moved into `voucher-line-reversal.service.ts`). Rewrite it the same way `payment-transition.service.test.ts` was rewritten: mock `@/lib/services/voucher-line-reversal.service` → `reverseVoucherLinesInTx`, and test only what the thin wrapper itself does now — dispatch to `unwindOrderPaymentLine`/`unwindOrderCreditApplicationLine`/`unwindStoredValueFundingLine` by `pair.originalLineRole`, the idempotency-key format (`voucher_unwind:{reversalVoucherId}:{originalLineId}`), passing `pair.reversalSessionId` through untouched, `recalculateOrderFinancialSnapshotTx` per distinct order, and the `unwindEnabled=false` short-circuit (core called, no unwind calls at all). The "not found" / "no POSTED lines" / "party_name" tests **move** to the new core test file (already there) rather than staying duplicated here.
3. Once both test files are green: `npx eslint` + the scoped typecheck (see environment note below) on every file touched this session.
4. **Then continue the remaining R1 writer items** (§4B.5): **W6** (customer-account receipts — rewrite `postCustomerAccountReceipt` to create real voucher lines instead of a header-only voucher + direct movement writes), **W4/W5** (delete the record-only cash-refund branch and the no-tender gift-card activation path — every cash event becomes a voucher, no flag-gated skip), **W11/W12/W14/W15** (delete `recordMovement`+its route, the 5 dead legacy actions in `cash-drawers-actions.ts`, the dead `settleOrder` branch, add `requirePermission` to `billing/cash-drawer-actions.ts`). **W13 stays deferred to R3** — do not touch the 3 mirror wiring handlers yet (confirmed reasoning already in the plan: they're the only thing keeping old drawer-close screens accurate for refunds/stored-value/customer-receipts until the R2 reader switch).
5. After the writer items: §4B.2a-A (Cash in/Cash out dialog + API), §4B.2a-B (pending-deposit ensure button on the 2 remaining screens — done only on the drawer tab per earlier session, confirm), then R1 exit gates (full eslint/typecheck/build/jest, QA guide, STATUS.md R1-complete row).

### Environment note — do not try to fix this, it's out of scope

`npx tsc --noEmit -p .` fails on `.next/dev/types/routes.d.ts` (a Next.js dev-server-generated file, referenced by `next-env.d.ts`, currently malformed) — this is **pre-existing, unrelated to CLF**, and that path is outside this session's filesystem permissions (can't read or delete it to check/fix). Workaround in place: `web-admin/tsconfig.clf-check.json` (untracked scratch file, safe to delete or keep, recreate from `tsconfig.json` + trimmed `include` if missing) — `npx tsc --noEmit -p tsconfig.clf-check.json` from `web-admin/` gives a clean, real signal excluding that one corrupted generated file. Try plain `npm run typecheck` first in the new session in case the dev server has since regenerated the file correctly; fall back to the scoped config if it's still corrupted.

**Standing rule already in force this session, keep following it:** `ls supabase/migrations/` immediately before writing any new migration file — the plan's nominal numbers are predictions, not reservations (see the `0523` collision entry below for why this matters).

---

## 🔴 2026-09-25 (earlier) — `0523` migration collision with CLF's M1, caught mid-push and fixed — a cross-project detour, not a Wave A package. The owner asked to work both `cleanmatex` and `cleanmatexsaas` to unblock A6. Resolved HQ-CUR-1/2/3 (see STATUS.md) via web research + migrations `0520`-`0522`/`0524`/`0525` + an HQ admin UI, and separately shipped `cleanmatexsaas` Phases 1-3 (currency-setup P0 bug fixes, the A6 value unblocker, and full UI/backend reachability) along the way. **A6 itself (drawer-side wiring) is still not started** — this closed the value/schema blocker only, Phase 3 was UI polish with no effect on A6's blocked status. **A `0523` filename collision with CLF's own M1 migration happened during the owner's apply attempt and required a live incident fix — see the top entry below before touching any migration numbers.** Full detail below; STATUS.md's cross-project-obligations table and A6 work-package row are the authoritative summary.

---

## 🔴 2026-09-25 (latest) — `0523` migration collision with CLF's M1, caught mid-push and fixed

**What happened:** the owner ran `supabase db push --include-all` to apply the currency migrations. A separate, concurrently-written file — `0523_clf_drawer_catalogs.sql` (CLF's M1, written using the plan's own "nominal" number instead of checking true next-free) — collided with the already-authored `0523_currency_fk_phase_a.sql`. The push applied CLF's file first and recorded it as the real `0523`. It then ran `currency_fk_phase_a`'s DDL, which **fully executed and committed** (verified live via read-only remote query: `org_orders_mst`/`org_cash_drawer_sessions_mst`/`org_customer_wallets_mst`/`org_fin_vouchers_mst`/`org_gift_cards_mst`.currency_code are `text`, all 5 `NOT VALID` FKs exist, `convalidated: false` as designed) — but its trailing `INSERT INTO supabase_migrations.schema_migrations` failed on the duplicate `0523` key. **The Supabase CLI does not roll back the migration's DDL when only that final ledger insert fails** — so the schema change is live and correct, but untracked in Supabase's own migration history. `0524_currency_platform_enabled.sql` never ran (push stops on first error; confirmed `is_platform_enabled` doesn't exist on remote).

**Fixed this session (file renames only, no DB write — reading remote was read-only throughout):** `0523_currency_fk_phase_a.sql` → `0524_currency_fk_phase_a.sql`; `0524_currency_platform_enabled.sql` → `0525_currency_platform_enabled.sql`. Both headers annotated with the renumbering history and the exact repair command needed.

**Update:** `0524_currency_fk_phase_a.sql` was rewritten to be idempotent — every `ADD CONSTRAINT` is now guarded by a `pg_constraint` existence check.

**✅ FULLY RESOLVED 2026-09-25.** Remote applied cleanly with `supabase db push --include-all`. Local then surfaced a second, separate drift problem: its ledger still had two *stale* entries at versions `0523`/`0524`, recorded under their pre-rename identities from before the collision — so `supabase migration up` kept treating those versions as already-done and silently skipped the files that now actually sit there, leaving CLF's M1 catalog tables missing locally even after a "successful" run (confirmed directly by query). Fixed with `supabase migration repair --status reverted 0523/0524 --local` + `migration up --include-all`.

**Real gotcha hit mid-fix:** the first attempt at that repair omitted the target flag — `supabase migration repair` defaults to the **linked remote project**, not local. Running it bare reverted `0523`/`0524` on the already-correct remote ledger instead (DDL untouched, ledger-only operation, but still had to be undone with `--status applied ... --linked` before redoing the local fix with `--local`). Confirmed via the [official CLI reference](https://supabase.com/docs/reference/cli/supabase-migration-repair): always pass `--local`, `--linked`, or `--db-url` explicitly on this command, never rely on the default.

**Verified clean, both sides, read-only:** local and remote ledgers agree exactly through `0526`; all 5 CLF M1 catalog tables exist locally; all 5 currency FKs exist on both sides (`NOT VALID`, no duplicates); `is_platform_enabled` exists on both. Local has since moved ahead with `0527_clf_drawer_trx_tables` (CLF's own next step).

**(superseded)** the migration-apply detour is fully closed — this was accurate when written; the entry above (🟡 R1 code: W1/W7/W8/W9) is now the authoritative "▶ NOW".

**Lesson recorded in STATUS.md and IMPLEMENTATION_PLAN.md §4B:** a plan document's "nominal" migration number is a prediction from when the plan was written, never a reservation — always `ls supabase/migrations/` immediately before writing a new migration file, regardless of what a plan says the number should be.

**Considered and rejected — renumbering CLF instead:** the owner initially asked to restore the original `0523_currency_fk_phase_a.sql`/`0524_currency_platform_enabled.sql` names and move CLF's file out of the way instead. Checked first: CLF's `0523` is already cleanly applied **and tracked** in the remote ledger (zero drift as-is), and CLF is actively writing further sequential migrations (`0526_clf_ledger_columns.sql`, M2, appeared mid-discussion) — a moving target. Moving an already-clean, already-tracked migration to satisfy naming preference, while risking a fresh collision with CLF's next file, was strictly worse than the currency migrations (which need a ledger repair regardless of their number, since their DDL landed live-but-untracked either way). Owner agreed to leave CLF at `0523`/`0526` and keep currency at `0524`/`0525`. **Settled — do not revisit this without a new reason.**

---

## ⏩ 2026-09-25 — `cleanmatexsaas` Currency Setup Phase 3 (UI/backend reachability) shipped

**Context:** continuation of the cross-project detour below (Phase 1 + Phase 2), same session. Phase 2 had already unblocked A6; Phase 3 was explicitly **not** an A6 blocker — it closes out `implementation_plan_02.md`'s own gap list (§4) for the `currency-setup` admin screen itself (missing endpoints, the stub usage service, the `is_system` delete-guard bug, ~60-of-71-column form coverage). Owner: "ok do it all" (Phase 3 tasks 3.1-3.7).

**Shipped, `cleanmatexsaas` only, no `cleanmatex` migration:** backend `CreateCurrencyDto` grew to ~60 fields; new endpoints `GET /lookups`, `GET /:code/usage`, `GET /:code/format-preview`, `PATCH /:code/activate`; `CurrencyUsageService` rewritten from a hardcoded-`0` stub to real queries against the 5 Phase-A tenant tables (same 5 tables `0523` below adds FKs to); `CurrencyCommandService.setActive()` now really blocks deactivating a system or in-use currency server-side. Two real bugs fixed while wiring this in: `effectiveFrom`/`effectiveTo` would have crashed the mapper on first use (string passed where `.toISOString()` expected a `Date`), and the repository's lookup-table list still referenced a table migration `0520` had already dropped (`sys_currency_cash_rounding_mode_cd` → `sys_rounding_mode_cd`), invisible until `database.types.ts` was regenerated. Frontend: `CurrencyFormDrawer` rewritten to 4 tabs / ~60 fields, fully lookup-driven; the divergent frontend symbol/format renderer (D8/D9) deleted outright in favor of the backend's own resolver; `CurrencyTable` gained a filter bar, an `isPlatformEnabled` column, and had its `|| c.isSystem` delete-guard clause removed (the backend now enforces the real rule and surfaces the reason via `cmxMessage`). ~50 new i18n keys added to both `en.json`/`ar.json` with full Arabic translations.

**Gates:** `platform-web` — `tsc`/`eslint --quiet`/`check:i18n`/`build` all clean (one pre-existing double-cast `tsc` error in `currency-form-drawer.tsx` fixed along the way). `platform-api` — `tsc` clean for every currency-setup/currency-rounding file; remaining project-wide failures are pre-existing and unrelated (`org_user_branches_mst`, `ResolvedStng`). No migration — Phase 3 only consumes the `0520`-`0522`/`0524`/`0525` schema already authored in Phase 2 (renumbered from `0523`/`0524` after the collision documented at the top of this file).

**Docs refreshed:** `cleanmatexsaas/docs/features/Currency_Setup/progress_status.md` (new §1d, §2/§3/§4 tables updated) and `implementation_plan_02.md` (Phase 3 table marked shipped per-task, D4/D6/D8 closed, the "two divergent renderers" risk struck through).

**Not done:** Phase 4 (FX) — untouched, was never in Phase 3's scope. Full 13-of-13 blueprint validation rule audit — Phase 3 closed the symbol/format/date rules only.

**▶ NOW:** superseded — see the `0523` collision entry above for the current pointer. This entry only closes out `cleanmatexsaas`'s own Phase 3 backlog and has no further action of its own in `cleanmatex`.

---

## ⏩ 2026-09-25 — CLF (Cash Ledger Foundation) designed and approved; not implemented

**What happened:** the D22 payment-during-close gap led to an owner design discussion and a redesign of the drawer model — the two-domain cash ledger, recorded as [ADR-057](../ADR/ADR-057-Two-Domain-Cash-Ledger.md) (supersedes ADR-032; amends ADR-054 and ADR-056). Plan written as [`IMPLEMENTATION_PLAN.md` §4B](./IMPLEMENTATION_PLAN.md) (package CLF) and approved by the owner with changes A–E. STATUS rows: **D29** (design), **D30** (plan approved: cash in / cash out as finance vouchers, one `PENDING_DEPOSIT` drawer per branch, driver bags need no session, three releases, no maker ≠ checker), **D31** (petty cash in the cash in / cash out dialog without any ERP-Lite dependency; pending-deposit button on three screens sharing one API).

**Status:** design only. **No code and no migrations** for CLF exist. Current code still uses `org_cash_drawer_movements_dtl`, the mirror wiring handlers and `cash-drawer-cash-facts.ts`. Living docs were updated the same day with clearly labelled "target architecture" sections.

**Releases:** **CLF-R1 Ledger** (CLF-0, M1–M3, M5–M7, lock / policy / gate / repository, writer rewiring W1–W15, reversal fixes, cash in / cash out, pending-deposit provisioning) → **CLF-R2 Sessions** (M4, M8, M9, counts, two-step close, dispositions, follow-up screen, policy tab, readers, remaining UI) → **CLF-R3 Retirement** (M10, deletion of legacy code and tests). Each release ends with green gates, QA guide update and a STATUS row.

**Sequencing:** CLF runs after the Wave A items it does not supersede and **before Waves B–E**. Superseded Wave A items — **A4-3/3b/3c/3d** — must not be started (see §4B.13). The ▶ NOW pointers in older entries below that offer A4-3 are superseded by this entry.

**▶ NOW:** superseded — see the `0523` collision entry at the top of this file. Short version: run `supabase db push --include-all` (applies `0520`, `0521`, `0522`, `0525` — `0523` is CLF's own M1, already applied, and `0524` is currency FK phase A, now rewritten idempotent so a plain push completes it cleanly, no `migration repair` needed). After that, the owner picks: remaining non-superseded Wave A items (A6-1b wiring, A3-6b owner call) or start **CLF-R2** with M2 onward — M1 is already applied. Load `/database` + `/multitenancy` + `/backend` first. Migration numbers in §4B.6 are nominal — take the next free number when writing. **Check `ls supabase/migrations/` fresh every time, don't trust any number written in this doc** — CLF's own M2 already claimed `0526` (`clf_ledger_columns.sql`) since this entry was first written, so true next-free is a moving target; trusting a stale number here is exactly what caused this collision in the first place.

---

## ⏩ 2026-09-25 (cross-project) — HQ Currency Setup Phase 1 + Phase 2, unblocking A6

**Context:** mid-conversation, after presenting the A4-3 plan, the owner redirected to "work on both repos" to unblock A6 (cash-tender rounding), which STATUS.md had recorded as blocked on HQ supplying curated rounding values/denominations (`HQ_CURRENCY_HANDOFF.md`). Investigation found `cleanmatexsaas` has its own detailed plan for this (`docs/features/Currency_Setup/implementation_plan_02.md`), audited at ~55% complete with P0 production bugs unrelated to A6. Owner selected all four of its phases; only Phase 1 (bug fixes) and Phase 2 (the actual A6 unblocker) were completed this session — Phase 3 (UI reachability) and Phase 4 (FX) are untouched.

**`cleanmatexsaas` Phase 1 (shipped, no migration):** create/deactivate currency-setup mutations were both broken in production (uuid-typed `created_by`/`updated_by` fed a client-supplied string default `'system'`; `isActive` rejected by `forbidNonWhitelisted`). Fixed: actor resolved server-side from the JWT (`req.user.id ?? req.user.userId`), `HqPermissionGuard`+`RequireHqPermission` added, `AuditService.log()` on every mutation, `DELETE` replaced with `PATCH /activate|/deactivate`, `GET /lookups|/:code/usage|/:code/format-preview` added (previously-unexposed services wired in), plus D5 (search didn't reset page), D6 (browser `confirm()` → `CmxConfirmDialog`), D7 (hardcoded English toasts → existing i18n keys), D10 (`isoAlphaCode` now immutable after create), D11 (`minorUnit` range 0-4 → 0-6 to match the DB). Full detail + gates: `cleanmatexsaas/docs/features/Currency_Setup/progress_status.md` §1a.

**`cleanmatexsaas` Phase 2 (shipped) — the actual A6 unblocker:** owner directive was explicit — act as an expert, research and verify the GCC rounding/denomination data, treat solid research as "verified" (not blocked pending a human round-trip), and build it so future corrections are a UI edit, not a migration. Ran 9 WebSearch queries against central-bank/government/numismatic sources. Two real findings sharpen the handoff's own placeholder guesses: **AED and QAR cash-tender rounding is 25 fils/dirham**, backed by an actual Abu Dhabi Department of Economic Development circular (prices rounded to nearest 25 fils) and documented Qatari retail practice — not the handoff's guessed "1 or 5." OMR/KWD/BHD confirmed at 5 (baisa/fils). SAR seeded conservatively at 5 halala with `confidence: medium` flagged in its row metadata (weaker, uncited evidence for a coarser rounding claim). Applied the handoff's own proposed 15-context/7-mode rounding vocabulary and `is_tenant_overridable` defaults directly rather than waiting for a sign-off round-trip. Full citations are in each seeded row's `metadata.sources` and in migration `0521`'s header.

**Migrations authored in `cleanmatex`** (STOP-AND-WAIT — created, not applied): `0520_hq_currency_rounding_context_and_mode_catalog.sql`, `0521_hq_currency_rounding_rules_cf.sql`, `0522_hq_currency_denominations.sql`. Cross-checked against the live remote schema via read-only `supabase_remote_db` MCP queries before writing (confirmed exact FK constraint names, live column types — found and fixed a fresh bug during writing: `sys_currency_rounding_rules_cd` already has a `notes` column, so the migration was corrected to reuse it instead of adding a redundant `rec_notes`). Also folds the handoff's own-flagged `VARCHAR`→`TEXT` conversion for `sys_currency_cd` into `0520`, and retires `sys_currency_cash_rounding_mode_cd` (34 chars, over the 30-char limit; its one live FK repointed first).

**Tenant app code migrated in the same pass, not deferred:** `web-admin/lib/money/currency-rounding.ts`'s `resolveCurrencyRoundingRule` now takes an optional `context` param (default `ACCOUNTING`, byte-identical behavior for the one real caller, `order-calculation.service.ts`, which has never passed one) and resolves against the new `sys_currency_rounding_rules_cf` shape with the context→ACCOUNTING→no-op fallback the handoff specifies. `applyRoundingMode` now correctly implements all 7 unified modes, sign-safe for negative amounts (the old implementation was positive-only in 3 of its 4 branches — dormant until a context ever produces a negative rounding input, e.g. `REFUND`). New `lib/constants/rounding-context.ts` (`RoundingContext` type, DB-mirrors `sys_rounding_context_cd`). `CURRENCY_ROUNDING_MODES` extended from 4 to 7 codes (`CEIL` retired in favor of `CEILING`). Both dependent test files (`currency-rounding.test.ts`, `b17-currency-rounding-consistency.test.ts`) rewritten for the new two-query mock shape (rule lookup + currency minor-unit lookup) — no behavior change to the tests' actual assertions, only their mock plumbing.

**HQ admin UI (Phase 2.9/2.10), shipped so a correction never needs a migration again:** new `cleanmatexsaas` module `platform-api/src/modules/currency-rounding/` (repository + snake_case→camelCase mapper + `RoundingRuleService`/`DenominationService` + controller, same guard/audit pattern as Phase 1) at `/currency-rounding/{contexts,modes,rules,denominations}`, and `platform-web/src/features/currency-rounding/` (currency-scoped screen with a rules tab and a denominations tab, each a `CmxDataTable` + form drawer + `CmxConfirmDialog`) at `/system-codes/currency-rounding`, with a new sidebar entry.

**Gates — all green, this session:**
- `cleanmatex/web-admin`: `npx tsc --noEmit` (whole project) clean · `npx eslint` (touched files) clean · targeted suite 19/19 · **full suite 329/329 suites, 2885/2885 tests** (net +5 over the pre-existing 2880 baseline, from richer 7-mode test coverage; zero regressions) · `npx prisma validate`/`generate` clean (same pre-existing unrelated `onDelete: SetNull` warnings as every prior gate).
- `cleanmatexsaas/platform-api`: `npx tsc --noEmit` clean (same pre-existing unrelated test-file errors in `tenant-runtime-api`/`settings`/`notifications-hq`, none touching currency code) · targeted `eslint` clean · `npm run build` exit 0.
- `cleanmatexsaas/platform-web`: `npx tsc --noEmit` (whole project) clean · targeted `eslint` clean (fixed 9 new `no-explicit-any` errors by typing `ColumnDef<T>` properly instead of following the pre-existing `any`-typed pattern in the sibling `currency-table.tsx`) · `npm run check:i18n` passed · `npm run build` exit 0, `/system-codes/currency-rounding` confirmed in the route manifest.

**Deliberately not done in this Phase 1+2 pass:** Phase 3 (currency-setup form reachability, 10-of-71 columns) and Phase 4 (FX/exchange rates) — owner selected all four phases, but only 1-2 were completed here; 3-4 sized independently per `implementation_plan_02.md`. **Phase 3 was completed in the same session, later — see the entry above.** Phase 4 (FX) remains untouched. Migration "step 4" (dropping the now-legacy `rounding_unit`/`rounding_method`/`cash_rounding_*` columns) — the handoff itself holds this until after the app has migrated and the owner has applied `0520`-`0522`; not written yet. **A6's actual tenant-side wiring (A6-1b onward — `cash-drawer.service.ts` reading `CASH_TENDER`/`CASH_CHANGE`, tender/change UI) was not started** — this detour only removed the upstream value/schema blocker.

**▶ NOW (superseded — see the two entries above):** owner applies `0520`→`0521`→`0522` in order, then `cleanmatex` regenerates types (`npx prisma generate` already run against the edited schema; Supabase-generated `database.types.ts`/`database.generated.ts` still need a post-apply regen in both repos per `integration-contracts.md` §5). After that: either resume Wave A (**A4-3** or **A6-1b** wiring, now actually unblocked) or continue the `cleanmatexsaas` Currency Setup Phase 3/4 work, owner's choice. *(Phase 3 has since shipped — see the top entry — and the apply batch grew to `0520`-`0524`; the CLF entry above carries the current pointer.)*

**Same-day follow-up (still unapplied):** owner asked to enrich the three migrations before applying — (1) `description`/`description2` seeded for every row that has those columns (`sys_rounding_context_cd`'s 15 contexts, the GCC-six + USD `CASH_TENDER`/`CASH_CHANGE` rows, and the ACCOUNTING backfill/mechanical-seed rows in `0521`; `sys_currency_denominations_cd` has no `description` column, so nothing to add there), (2) `COMMENT ON TABLE`/`COMMENT ON COLUMN` added for every column of all four new tables (`sys_rounding_context_cd`, `sys_rounding_mode_cd`, `sys_currency_rounding_rules_cf`, `sys_currency_denominations_cd` — previously only a handful of columns had comments), (3) **USD added as a seventh curated currency** in both `0521` (`CASH_TENDER`/`CASH_CHANGE`) and `0522` (13 denomination rows) — researched via WebSearch, not guessed: the US Mint struck its last penny 2025-11-12 (Treasury's own FAQ + Richmond Fed research), so USD is seeded at 5-cent (nickel) rounding with the penny marked `is_in_circulation = FALSE`. All edits reviewed manually for SQL syntax correctness (column/value count parity, apostrophe-escaping) since the migrations still can't be applied to verify. No structural/Prisma changes — same table shapes as before, more seed data and documentation only.

---

## ⏩ 2026-09-25 — A3-4 closed (money as strings end-to-end); A3 is now fully done

**Owner directive, verbatim:** "You decide and choose and implement as expert for solid stable with follow best practices to build production-ready, with no gaps, no bugs, UI/UX best practices, production-ready." Picked **A3-4** over A4-3: A3-4 continues Wave A's "no float money bugs" thread already 5/7 done (A3-1/2/3/5/6/7), while A4-3 is a fresh Wave-0-sized new-table pass better done as its own dedicated session. Before starting, re-checked A6: it's **more blocked than its status line implied** — the DDL for the expanded `sys_currency_rounding_rules_cf` (packages HQ-1–HQ-3) hasn't even been authored yet, pending HQ's rounding-vocabulary sign-off (`HQ-CUR-3`, still "AWAITING HQ SIGN-OFF") — so it was correctly ruled out too.

**Scope, mapped before writing anything:** dispatched a read-only Explore pass over every type, service function, API route, UI consumer, test, and Zod schema touching cash-drawer/POS-session money fields (per this repo's CLAUDE.md plan-first rule). Confirmed: no migration needed (pure serialization/type plumbing), no response-side Zod schemas exist to update, and the SQL side (`::text` casts) was already correct from A3-2 — only the final JS-side conversion boundary needed to change from `number` to `string`.

**Shipped:** `toMoneyString()` added to `lib/utils/money.ts`. `PosSessionSummaryAmountRow.amount`, `SessionCloseResult.variance`/`.varianceThreshold`, and all 9 money fields across `lib/types/cash-drawer.ts` flipped `number` → `string`. `lib/money/format-money.ts`'s three formatters now accept `number | string`. `useTenantCurrency()` (the existing shared cross-feature context — not a new hook, per CLAUDE.md's reuse-before-creating rule) gained `moneyLocale` and an optional per-call currency-code override on `formatMoney`/`formatMoneyWithCode`, and the 3 duplicated hardcoded `.toFixed(3)` formatters flagged back in D24/A4-2 (`pos-session-hub.tsx`, `pos-sessions-screen.tsx`, `pos-session-drawer-close-summary.tsx`) now route through it — real tenant decimal places, not an OMR-only magic `3`.

**2 real bugs found and fixed along the way** (not part of the original task description — found because the cash-drawer session print page's local JS `+`/`-` recompute broke the moment `totalCashIn`/`totalCashOut`/`totalPayments` became strings): `getSessionSummary()`'s returned `reconciliation` object was silently missing `countedCash`/`variance` (present on the underlying `buildSessionReconciliation` call, dropped when `getSessionSummary` re-listed only a subset of fields) — added. The print page itself recomputed `expectedBalance`/`variance` via local JS arithmetic instead of the service's already-Decimal-exact values — the same float-drift class A3-1/A3-7 already fixed twice elsewhere, reintroduced a third time here, plus a latent bug where a legitimate `0` physical count was treated as "not yet counted." Rewired to consume the service's `reconciliation` object directly.

**Deliberately left untouched, with reasons recorded:** `CashDrawerWithCurrentSession` (server export + its client-side duplicate) stays `number` — its own comment already flags it as the back-compat type "used by existing POS and checkout consumers," a wider blast radius than this pass's scope map covered. `cash-drawer-detail-client.tsx`'s 4th duplicated formatter — confirmed via repo-wide grep to be orphaned (no `app/` route imports the component), so left alone rather than polishing dead code. `lib/constants/reconciliation-reports.ts` / the finance reconciliation report — an independent type, correctly out of scope per A3-7's own prior deferral. Request-body Zod schemas (`open-session`/`close-session`/`cash-movement`, drawer config forms) correctly stay `z.number()` — A3-4 is about response serialization, not operator input.

**One transitive test-infra fix:** the rewired `cmx-money-field-controller.tsx` initially imported `next-intl`'s `useLocale` directly, which bypassed 10 test files' existing mocks of `useTenantCurrency` and forced real (untransformed-ESM) `next-intl` resolution — failing all 10 with a syntax error. Fixed by exposing `moneyLocale` on the context itself instead of a fresh direct import; all 10 pass again.

**Gates (whole project):** `tsc --noEmit` clean (after fixing 2 real type errors it caught) · `eslint . --quiet` clean (same 2 pre-existing unrelated `cmx-sidebar.tsx` errors as every prior gate this program) · `npx jest --testPathPattern "cash-drawer\|pos-session"` 95/95 · `npx jest --config jest.db.config.js` (this program's 3 DB-integration files, local, in-band) 6/6 · `npx jest` full suite **329/329 suites, 2880/2880 tests** (exactly matches the pre-A3-4 baseline, after catching and fixing the next-intl regression above) · `npm run check:i18n` passed (no new keys) · `npm run build` compiled successfully, exit 0.

**▶ NOW:** **A3 is fully done** (only A3-6b remains, blocked on the owner confirming which of 4 flagged historical sessions are safe to touch — see D27). Wave A remaining: **A4-3/3b/3c/3d/4** (`org_cash_sess_curr_dtl` table + `allow_multi_currency_drawer` setting — confirmed Wave-0-sized, its own dedicated pass, see D25) or **A6** (cash tender rounding — now confirmed blocked at the DDL-authoring level too, not just the value-supply level, pending HQ's `HQ-CUR-3` sign-off; not a viable next pick until that lands).

---

## ⏩ 2026-09-24 (later same day) — "you decide" — closed A3-5/A3-6/A3-7, found+fixed D26 (critical), investigated A3-6b (D27)

**Owner directive, verbatim:** "You decide and choose and implement as expert for solid stable with follow best practices to build production-ready, with no gaps, no bugs, UI/UX best practices, production-ready." Chose to close out the remainder of **A3** (the "no float money bugs" sub-wave — Program goal #1) rather than start A3-4 (money-as-strings API contract, cross-cutting) or A4-3 (new `org_cash_sess_curr_dtl` table, Wave-0-sized) — both stay correctly deferred as their own dedicated passes, consistent with this program's own established sizing precedent (D23–D25).

**D26 — CRITICAL, found while verifying A3-7 against a real DB:** re-running the existing `cash-drawer-mutation-locking.db.test.ts` against the local DB (first real-DB exercise of A3-3's currency lookup since it shipped) failed immediately: `The column sys_currency_cd.decimal_places does not exist in the current database`. Confirmed via read-only remote MCP query that the column **does not exist on remote either** — this was never a local-DB-only staleness issue. The real, correctly-seeded column is `minor_unit` (same values: AED=2, OMR/BHD/KWD=3, JPY=0); `decimal_places` survives only as a stale, never-matching field on the hand-maintained `sys_currency_cd` Prisma model. Because this lookup sits inside `closeSession`'s `Promise.all`, its failure rejects the whole transaction — **every drawer-close attempt, for every tenant, has been throwing since A3-3 shipped on 2026-09-23**, and nothing caught it because A3-3's own gate history only ever ran mocked unit tests. **Fixed**: `cash-drawer.service.ts` now selects/reads `minor_unit`; `prisma/schema.prisma`'s `sys_currency_cd` model corrected (`decimal_places` → `minor_unit`, matching the live column), Prisma client regenerated. Verified: the exact DB-integration suites that failed now pass (5/5), full jest still 329/329 (2880/2880), `tsc`/`eslint`/`build` all clean. Confirmed by repo-wide grep that this was the *only* live caller — no other code path was ever at risk. **Not fixed, flagged as a real follow-up**: the same Prisma model has several other stale fields (`iso_code`, `iso_numeric`, `thousands_separator`, `is_default`) that don't match the live table either — currently dormant (nothing reads them), a full model reconciliation (`prisma db pull` + review) is its own pass.

**A3-5 (closed):** new `__tests__/db-integration/cash-drawer-decimal-precision.db.test.ts` — 500 sequential 0.0050 OMR cash payments close with **exactly** zero variance (bit-for-bit, not "within tolerance"), proving A3-1's Decimal path holds under a real long sequence. Passing against local DB, self-cleaning (0 leftover rows verified).

**A3-6 (verified, nothing to fix there):** the "known breaking test" `cash-drawer-close-preview.test.ts` reads `buildCashDrawerClosePreview`, which consumes the *cash-drawer* session-summary API — a different function from `getPosSessionSummary` (A3-2's actual target) — so A3-2 never touched what this test exercises and the original "will need updating" note didn't apply. Investigating *why* surfaced the real bug fixed under A3-7 below.

**A3-7 (closed, found a real bug beyond what was scoped):** `cash-drawer.service.ts`'s session-detail/close-preview/print *read* path (`buildSessionReconciliation`, `buildSessionSnapshot`, and two duplicated inline list-mapping formulas) had the identical `toNumber()` + JS `+`/`-`/`reduce` float-drift defect A3-1 fixed in the write path — just re-introduced three times over on reads nobody had re-audited. Fixed all of it in Decimal space, and extracted the three duplicated formulas into one shared `deriveExpectedCashAndVariance` helper (closes a real "one concept, three places" duplication at the same time — reuse-before-creating). Also gave `sumEffectiveCashPayments` a Decimal-returning `sumEffectiveCashPaymentsDecimal` sibling and fixed `getSessionSummary`'s `movementExpectedCash`. `reconciliation-reports.ts`/`CASH_VARIANCE_TOLERANCE` deliberately left alone — no live money-correctness bug found there this session, stays correctly scoped to A3-4's later broader pass.

**A3-6b (investigated, correctly deferred — see D27):** a read-only comparison query (remote MCP) of all 20 remote `CLOSED` drawer sessions found 4 real divergences between the stored `expected_cash_amount` and today's formula — but every one predates A3-1, and the divergence is large/structural (e.g. 88.9320 vs 0.0000), tracing to the older B16/B35 "count each cash fact once" filter landing *after* these sessions closed, not to float drift. Several look like deliberate QA large-variance fixtures. Correctly **not** mechanically recomputed — doing so would silently rewrite what looks like real historical QA evidence, the same risk class this repo already treats as its own governed runbook elsewhere. No DML executed.

**Gates (whole project):** `tsc --noEmit` clean · `eslint` clean on every touched file · `npm run build` exit 0 · `npx jest` (full suite) **329/329 suites, 2880/2880 tests** · DB-integration: this program's 3 `.db.test.ts` files **6/6 passing** (serially and scoped; 2 *unrelated* pre-existing suites plus this program's concurrency test flake only under full-26-suite parallel-worker contention — re-confirmed 100% green when run alone/in-band, not a regression, not investigated further as out of scope).

**▶ NOW:** Wave A remaining: **A3-4** (money-as-strings API contract — cross-cutting, its own pass) or **A4-3** (`org_cash_sess_curr_dtl` + `allow_multi_currency_drawer` — Wave-0-sized, its own pass). **A6** (cash tender rounding) still blocked on HQ's rounding values for its value-supply half; DDL/wiring half (A6-1b onward) is not blocked. A3-6b is a real open task but needs the owner to confirm which of the 4 flagged sessions are safe to touch before any recompute is written.

---

## ⏩ 2026-09-24 — A5 run early as a checkpoint (owner: "A5 or A3-4, I prefer A5")

**Context:** with A1, A2, A3-1/2/3, and A4-1/2 shipped, the plan offered A3-4 (continue Wave A's remaining items) or A5 (Wave A exit gates) as the next step. `IMPLEMENTATION_PLAN.md` titles A5 "Wave A exit" — normally implying Wave A is finished first — but the owner explicitly preferred A5 now. Ran it as an **interim checkpoint** on everything shipped so far, not as a claim that Wave A is done; STATUS.md's wave-status row and A5-1 work-package note both say this explicitly so a future cold session doesn't misread A5 as Wave-A-complete.

**A5-1 gates (whole `web-admin` project, not just this program's files):**
- `npx tsc --noEmit` — ✅ clean, 0 errors.
- `npx eslint . --quiet` — 🟡 2 errors, both in `src/ui/navigation/cmx-sidebar.tsx` (`react-hooks/set-state-in-effect`). **Pre-existing** — the exact same 2 errors were already flagged and left alone at the W0-5 gate on 2026-09-23 (unrelated file, not touched by this program). Not fixed in this pass either, for the same reason (CLAUDE.md: avoid unrelated refactors) — belongs to its own cleanup, not this program.
- `npm run build` — ✅ exit 0, full route manifest compiled.
- `npx jest` (full suite) — ✅ **329/329 suites, 2880/2880 tests passing**, zero regressions. One benign `--detectOpenHandles` worker-teardown warning at the end (known Jest/Windows artifact, not a failure).

**A5-2:** `Remediation_Work_Packages/QA_TEST_GUIDE.md` gained **§33** — the first owner-runnable QA scenarios for this program. Covers: the `cash-control-settings` admin screen (view/edit/save, view-only role for `operator`, client-side validation feedback, Arabic/RTL), the A4 multi-currency summary display (POS Sessions hub card + summary dialog, plus a same-as-before check for the ordinary single-currency case), and A3-3's tightened per-currency variance tolerance (an OMR drawer now correctly flags a variance the old flat tolerance silently absorbed; an AED drawer's behavior is unchanged). A1 (session numbering) and A2 (mutation locking) are explicitly called out as **not manually testable** — they are concurrency fixes with no UI surface, proven instead by the two `.db.test.ts` files already written in A1-5/A2-6.

**A5-3:** `/documentation` skill invoked. Updated: this program's `STATUS.md` (program-status line, Wave A row, work-package rows A5-1/A5-2/A5-3, new Validation gate history rows). Created this `RESUME_CONTINUATION.md` — the folder had none before (only `START_HERE.md` / `STATUS.md` / `IMPLEMENTATION_PLAN.md` / the architecture review). Did **not** generate the generic `/documentation` skill's full pack (README/development_plan/user_guide/etc.) — this program already has an established, deliberately lighter plan-and-status pattern (matching the sibling `Order_Fin/Remediation_Work_Packages` folder), and imposing the generic pack here would be unrequested churn against an already-working structure, not a gap.

**▶ NOW:** Wave A remaining work, owner's choice of order: **A3-4** (money-as-strings API contract — cross-cutting, touches every `SessionCloseResult`/drawer-API consumer), **A3-5** (500-payment Decimal regression test), **A3-6b** (recompute existing demo-data variance under the new Decimal path — safe pre-launch per `project_prelaunch_no_real_tenants`), **A3-7** (verify the reconciliation report still balances against the new Decimal path), or **A4-3** (the `org_cash_sess_curr_dtl` table + `allow_multi_currency_drawer` setting — confirmed comparable in size to all of Wave 0's cash-control package, its own dedicated pass per D25). **A6** (cash tender rounding) stays blocked on HQ's rounding values (`HQ-CUR-1`/`HQ-CUR-2`, see the Open cross-project obligations table) for its value-supply half, but its DDL/wiring half (A6-1b, A6-2, A6-2b, A6-3 onward) is not blocked and could be picked up independently. Do not re-run the full A5-1 gate suite again until a real Wave-A-close is reached — this pass already covers everything shipped through 2026-09-24.
