# RESUME — Order Fin Remediation Program (session continuation)

**Updated:** 2026-07-18, immediately before an owner `/clear` (expert correction pass 2026-07-16 and decision-finalization governance pass 2026-07-18 both applied). **Read this file + [CLAUDE.md](CLAUDE.md) first in any new session, then follow it exactly.**

---

## ⏩ FRESH-SESSION HANDOFF (read this first — 2026-07-19, corrected)

**Done & gate-green (B01–B35; owner commits some, rest uncommitted in the working tree):** B01, B02, B33, B34, B15, B16 (Part A expected-cash filter + Part B optional/non-blocking variance approval; mig 0407 applied by owner), B35 (Addendum A2 double-count fix + `order_fin_drawer_close_v2` flag REMOVED → correct drawer-close math is now unconditional), **B20** (TAX_CALCULATION + DISCOUNT_VALIDATION wired, new REFUND_REOPEN_CONSISTENCY check, `total_checked` 35→38 — **committed at `7c019061`**), **B29** (stale-doc correction sweep, docs-only), **B4/B5/B31** (later-collection BVM voucher parity + idempotency + D9-aware default status, one `collectPaymentTx` rewrite, ships unconditionally), **B7** (financial outbox processor finally consuming — mig 0410 APPLIED (owner, 2026-07-20)), and **B27** (financial permissions + approvals — 7 codes seeded, fail-open bug fixed, mig 0411 APPLIED (owner, 2026-07-20)). All gates green: tsc / eslint / jest (217/217 suites, 2084/2084 tests, zero known failures) / build / check:i18n.

**Seq 6 CLOSED 2026-07-23 — B30 + B32 IMPLEMENTED (uncommitted; B3 was already committed at `98a63f93` before this session started).** B30: one new `lib/services/payment-transition.service.ts` implements the D001 canonical-graph subset (VERIFY/CANCEL/FAIL_BOUNCE, PENDING/PROCESSING sources only — a COMPLETED leg needs B10 reversal) with D009 governed fallback classification (mandatory reason + one of the 5 classifications on CANCEL/FAIL_BOUNCE; `PAY_ON_COLLECTION`/`AR_CREDIT_INVOICE` reclassify `org_orders_mst.payment_type_code` — confirmed via code search this is the FIRST writer of that column outside order-creation; the other 3 classifications are recorded but do not auto-mutate the order, per CLAUDE.md's no-silent-money-mutation rule) and D010 idempotency (same in-tx `org_idempotency_keys` pattern as `collectPaymentTx`). New cross-order worklist at `/dashboard/internal_fin/pending-payments` (API: `GET /api/v1/finance/pending-payments`, `POST .../[paymentId]/transition`) plus Cancel/Fail-Bounce buttons added to the existing per-order Payments tab (the pre-existing Verify button/route untouched). **Correction discovered this session:** B30's own doc assumed B27 had seeded `orders:cancel_payment`/`orders:fail_payment` — re-verified against migration 0411, which only confirms the unrelated, long-orphaned `payments:cancel` code from migration 0095 (no live consumer). **Migration `0415_b30_b32_payment_transitions_and_permissions.sql` — APPLIED (owner, 2026-07-23) to local + remote, verified via remote DB** (8 actor-audit/fallback columns on `org_order_payments_dtl`, the 3 real permission codes + role grants, `chk_history_action_type` +2 values, nav entry — post-apply read-only queries against remote confirmed all 8 columns present, all 3 permissions active with correct role grants per role, `billing_pending_payments` nav row present with `parent_comp_id` resolved, and both new/updated CHECK constraints present). B32: `cashDrawerWiringHandler.canHandle` now gates on the effective resolved payment status (closes M7 — a drawer-required method configured to create PENDING no longer records premature cash-in); the deferred-movement-on-verify hook lives inside B30's transition service. **`allow_status_override` (M8) was investigated, NOT enforced — full reasoning in B32's Completion evidence:** the first implementation attempt gated the checkout-time explicit `paymentStatus: 'PENDING'` override behind this config field, which immediately broke a pre-existing, shipped, tested BVM Phase 6 Sub-item 6 (B7 closer) test asserting that override is unconditional regardless of config. Reverted; both test suites gained a pinning test documenting the decision instead. One incidental pre-existing test-fixture bug fixed along the way: `cash-drawer-change.idempotency.test.ts`'s `makeCashLine()` had `payment_status: 'POSTED'` (copy-paste of `line_status` above it) — harmless before B32's new gate, now fixed to `'COMPLETED'`. Also hand-fixed a `derive:ui-access-contract --apply` over-generation gotcha (same class as B7's scaffold gap) — the tool dumped ~30 unrelated apiDependencies + duplicate snake_case actions into the new contract entry (all confined to that one route block, verified via `git diff`); hand-trimmed to the 3 real actions + 2 real API deps. **Gates ALL GREEN:** tsc clean (2 pre-existing unrelated file errors untouched: `order-service.ts:2158`, `processing-piece-row.tsx:207`) / eslint 0 / full jest 222/222 suites, 2135/2135 tests, zero known failures (one transient Windows Prisma query-engine file-lock flake on the first run, self-resolved on retry — not a code issue) / build ✓ / check:i18n ✓ (7 pre-existing EN=AR placeholder warnings, unrelated) / check:ui-access-contract --wire PASS / sync:ui-access-contract PASS (144/144 routes, drift 0) / check:platform-info-inventories PASS.

**Correction (2026-07-19):** the previous version of this handoff line said "NEXT ACTION: implement B20" — stale; B20 was already implemented and committed before that line was written. This is exactly the class of drift B29 exists to catch. Left uncorrected here until now.

**B29 IMPLEMENTED 2026-07-19 (docs-only, uncommitted):** stale-doc sweep across the 12 non-`Remediation_Work_Packages` folders under `docs/features/Order_Fin/` — 7 files corrected with banners (ADR-030, 3 Fix_29_05_2026 status docs, 2 Opus_Validation_Report_18_06_2026 files, RECONCILIATION_GUIDE.md), all asserting refund-lineage/reconciliation-count claims the frozen report + B01/B20 have since superseded. Migration 0340's own commentary was re-verified and found NOT stale (it only claims backfill parity, not future writes). Full index in B29's Completion evidence. No code, no tests, no gates beyond link verification (all 10 corrected cross-references resolve).

**B4/B5/B31 IMPLEMENTED 2026-07-20 (uncommitted), one `collectPaymentTx` rewrite (`order-settlement.service.ts`):** real-payment legs now wire through `createBizVoucher` → `addVoucherLine` (per leg) → `postAndWireBizVoucher` — the same voucher/wiring path submit-order uses — instead of writing `org_order_payments_dtl`/`org_cash_drawer_movements_dtl` directly, closing the `ORDER_PAYMENT_LINK_EXISTS` gap (B4/H1). Collect's own overpayment engine (`computeCollectionOverpaymentMetrics`/`validateOverpaymentResolution`/disposition/allocation) was deliberately left untouched to guarantee financial parity — only the write mechanism changed. Method resolution now goes through `listEffectivePaymentMethodConfigs` (D9-aware, branch-merged) instead of bespoke tx queries, and per-leg status resolves via the submit planner's exported `resolveDefaultStatus` instead of the old hardcoded `gatewayCode ? 'PENDING' : 'COMPLETED'` (B31/M6). A top-level `org_idempotency_keys` guard (resource_type `collect_payment`) short-circuits an identical-payload replay to the cached result before the order lock even runs, and throws `IDEMPOTENCY_CONFLICT` (→ HTTP 409) on a changed payload; `idempotencyKey` is now required end-to-end (service type + both routes' Zod schemas, no more `randomUUID()` server-side fallback) (B5/H5). Incidental fix discovered en route: the wiring handler was dropping a CHECK payment's `check_bank`/`check_date` when creating the payment row (only `check_no` forwarded) — now forwards all three, benefiting submit's CHECK payments too. **Ships unconditionally, no feature flag** — owner decision 2026-07-20 (asked via AskUserQuestion, following the B35 precedent: a parity-tested correctness fix doesn't need a flag when the Preview-QA gate already sits between every commit and production; a flag would mean maintaining two parallel `collectPaymentTx` paths with no matching safety benefit). Full validation parity with submit's `validateSettlementPlan` (reference/terminal/gateway-config checks) was explicitly NOT added — collect's request contract has no terminalId/gateway fields to check against; documented as a deliberate, tracked gap in each Bxx file rather than silently dropped. Frontend (`order-collect-payment-modal.tsx`): stable per-dialog-open idempotency key (was regenerated per click — now regenerated only on open, so a retry of the same attempt reuses it), a pre-submit "pending until verified" notice when the D9 config explicitly overrides to PENDING, and 409→`idempotencyConflict` error mapping; new i18n keys `orders.collectPayment.idempotencyConflict`/`.pendingUntilVerified` (EN/AR). **Gates ALL GREEN:** tsc clean / eslint 0 / check:i18n ✓ / targeted jest 51/51 (rewrote the 3 old `collectPaymentTx` direct-write tests for the new voucher/wiring mocks + added B31/B5/nature-guard tests) / full jest 2041/2049 (8 fails = the pre-existing unrelated `order-calculation.service.test.ts` mock gap, not touched) / build ✓ (exit 0). Also fixed `__tests__/migrations/phase1-order-fin.test.ts`'s "1C" section, which was asserting the OLD F-10 `randomUUID()` fallback B5 deliberately supersedes — rewritten to assert the new required-key contract instead of leaving a stale regression-guard for intentionally-changed behavior. Full completion evidence (file lists, exact test names) in each of B04/B05/B31's own Bxx files — not duplicated here.

**B7 IMPLEMENTED 2026-07-20 (uncommitted; migration 0410 APPLIED (owner, 2026-07-20)):** the financial outbox (`org_domain_events_outbox`) has been emitted into since migration 0292 but never consumed until now. Root cause found: the only scheduled consumer, the `outbox-worker` pg_cron job (0296), calls a Supabase Edge Function whose 6 handlers are all placeholders, AND that job's own cron body has been a permanent no-op since day one (gated on an `app.settings.supabase_project_ref` GUC that can never persist — Supabase's `postgres` role isn't SUPERUSER — the identical problem 0355 already hit and fixed for the notifications outbox). Fix mirrors 0355's pattern exactly: new `sys_fin_runtime_cf` config table + SECURITY DEFINER wrapper + `net.http_post` → new `POST /api/finance/process-outbox` route (bearer-secret, `FINANCE_OUTBOX_SECRET`) → new `fin-outbox-processor` cron (every minute); the dead `outbox-worker` cron is unscheduled by the same migration. New `lib/services/outbox-processor.service.ts` registry fans each claimed event out to every handler owning its event type: `order-history` (thin wrapper over the already-idempotent, already-written `consumeOrderHistoryEvent`) and NEW `loyalty-earn.handler.ts` (wraps the existing-but-never-called `processEarnPoints` — H7's actual gap was that `queueEarnPoints`'s payload only carries `{customerId, orderAmount}`, and NOTHING anywhere in the codebase had ever implemented the `orderAmount → earnPoints` conversion; this package computes it as `floor(orderAmount × program.earn_rate_per_unit)` at consume time). Also fixed a genuine concurrency bug found while touching `outbox.service.ts`: `claimBatch()` was a `findMany`+`updateMany` pair with **no row lock at all** — two concurrent callers could double-claim the same event; rewrote it to call the already-existing-but-unused `claim_outbox_batch` SQL RPC (0296, real `FOR UPDATE SKIP LOCKED`). Added a genuine `DEAD_LETTERED` status (migration 0410) — previously an exhausted-retry event stayed `FAILED` with `next_retry_at=NULL` forever, indistinguishable from "about to retry." New ops-visibility screen at `/dashboard/internal_fin/outbox` (health counts + filterable list + manual retry), new permissions `finance_outbox:view`/`finance_outbox:retry` (seeded in the same migration), nav dual-write done, UI-access-contract golden path run in full (scaffold's auto-insert failed on `billing-access.ts`'s no-semicolon array-closing style — a tooling gap, so the contract entry was hand-added following the exact existing pattern; `check`/`sync` both PASS, 142/142 routes, drift 0). **No feature flag** — this closes a confirmed "nothing ever consumed this" gap, not a behavior change with a "before" state to preserve. **Migration 0410 APPLIED 2026-07-20 (owner), verified via remote DB** — `sys_fin_runtime_cf` populated, `fin-outbox-processor` cron `active:true` on `* * * * *`, dead `outbox-worker` cron gone. ⚠️ **`FINANCE_OUTBOX_SECRET` is STILL the empty placeholder** in `.env.local`/`.env.dbcloudjh` — the cron fires every minute regardless but the route 401s until the owner runs `SELECT value FROM sys_fin_runtime_cf WHERE key='outbox_secret_key';` and pastes it into both files + the deploy environment (this one step remains before B7 is actually live end-to-end). Gates: tsc clean/eslint 0/i18n ✓/targeted jest 26/26 (3 new suites)/full jest 2060/2068 [8 fails = pre-existing unrelated order-calculation mock gap, since fixed by B27]/build ✓/sync:ui-access-contract PASS/check:platform-info-inventories PASS. **Incidental fix along the way (unrelated to B7):** the full-jest gate run surfaced `refund.service.test.ts`'s self-approval test asserting behavior the owner had already changed in a same-day commit (`f7dccb8a`, "no need for maker-checker in approve... same user can approve if he has the required permission" → `ENABLE_SELF_APPROVAL_CHECK = false` in `order-refund.service.ts`) without updating the test — rewrote the test to match the now-intentional behavior.

**B27 IMPLEMENTED 2026-07-20 (uncommitted; migration 0411 APPLIED (owner, 2026-07-20)):** re-verified the §43 audit against the **live remote DB** (not a local-migration grep) before writing anything — of the 12 NOT_FOUND rows, 5 already had a seeded, role-granted code (`orders:discount`, `payments:cancel`, `erp_lite_gl:reverse`, `erp_lite_periods:reopen`, `stored_value:issue_credit_note` — only its UI entry point was ungated), narrowing the real gap to 7 codes. Seeded: `cash_drawer:approve_variance` (B16's `approveSessionVariance` has checked this since 2026-07-18 but it was never in the DB — genuinely missing until now), `stored_value:issue_wallet_credit` (new, wired into `topUpWallet`), `orders:rebill_authorize` (activates the REFUND_AND_REBILL path B01/B34 have hardcoded-rejected since B01 shipped), plus three inert placeholders reserved for not-yet-built packages (`orders:manual_charge` → B18, `orders:post_settlement_edit` → B12, `orders:rate_override` → B26) and `orders:discount_threshold_override` (reserved, no threshold config exists). Also **additively broadened `pricing:override`** to `admin`/`branch_manager`/`cashier`/`receptionist`/`supervisor`/`operator` (owner directive — match `orders:create`'s role set) and **fixed a genuine fail-open bug**: `addOrderItems`'s price-override gate in `lib/db/orders.ts` skipped the check entirely when the current user couldn't be resolved, and swallowed any `hasPermissionServer` error other than the literal string "Permission denied" — both now fail closed. Gated three previously **completely ungated** stored-value server actions (`topUpWallet`, `issueAdvance`, `issueCreditNoteAction` in `stored-value-actions.ts` — the API routes had checks, but the UI calls these server actions, which had none). Wired REFUND_AND_REBILL in both refund API routes (`refund/route.ts` + sibling `refunds/route.ts`) and `initiateOrderRefund` (closed a type-safety gap where `rebillAuthorized` wasn't excluded from the accepted param type) to a real `hasPermissionServer('orders:rebill_authorize')` check, replacing B01 §13's hardcoded 403. **Scope decision:** did not build a generic threshold-approval dialog component — zero current consumers of `orders:discount_threshold_override` exist, and B16 already shipped its own independent approval dialog before B27 started; deferred to whichever package first needs a second consumer (candidate: B12/B18/B26). **Incidental fix (unrelated to B27):** full-jest surfaced 8 pre-existing failures in `order-calculation.service.test.ts` — a baseline gap from commit `2413671b` (2026-07-18) adding an `evaluateBestAutoApplyPromo` call that the test's `discount-service` mock factory was never updated to include; added the missing mock (test-file-only, no production code touched). **Gates ALL GREEN:** tsc clean / eslint 0 / targeted jest 16-16 (4 new suites) / **full jest now 217/217 suites, 2084/2084 tests — zero known failures** (the order-calculation gap above is now fixed, so this is the first fully-green full-suite run all session) / check:i18n ✓ (pre-existing benign EN=AR placeholder warnings only, unrelated) / build ✓ (exit 0, all routes incl. `/dashboard/internal_fin/outbox`). Full detail in [B27](B27_Financial_Permissions_And_Approvals.md)'s own Completion evidence.

**B3 ARCHITECTURE LOCKED 2026-07-20 (docs-only, no code/migration yet):** researched the current code in full (Explore agent — findings preserved in [B03](B03_Stored_Value_Funding_Capture.md)'s own "Architecture decision" section) — `sellGiftCard`/`issueAdvanceTx`/`topUpWalletTx` all write ledger rows only, no tender/voucher/drawer capture; the "proven pattern" the original doc pointed to (customer-receipt-excess wallet/advance save) turned out to be ledger-only too, just with an audit backlink — the real proven pattern is `collectPaymentTx`'s `createBizVoucher → addVoucherLine → postAndWireBizVoucher → wiring handlers` sequence, which has no handler today for `GIFT_CARD_SALE`/`WALLET_TOPUP`/`CUSTOMER_ADVANCE_RECEIPT` lines. Owner shared an external architecture proposal (evaluated and partly adopted, partly trimmed — see B03 §Architecture decision for the full reasoning): **locked design** = two new tables `org_sv_fundings_mst` (funding header, supports multi-leg tenders, DB-generated `total_credit_amount`) + `org_sv_funding_tenders_dtl` (tender legs) — existing wallet/advance/gift-card ledger tables and BVM voucher tables keep their current roles, nothing replaced; new shared `stored-value-funding.service.ts` (synchronous confirmed-tender flow only — no async gateway-callback state machine, since no gateway integration exists for these 3 entry points today; trimmed from the external proposal as speculative) using the same `org_idempotency_keys`/`collectPaymentTx` idempotency pattern; two new wiring handlers (`stored-value-funding-wiring.handler.ts` + a **separate** `stored-value-cash-drawer-wiring.handler.ts`, not a broadened existing one); bundled defect fixes (idempotency keys added to `sellGiftCard`/`issueAdvanceTx`, sparse unique index on `org_gift_card_txn_dtl`); permission semantics frozen with **no new codes** (`stored_value:top_up_wallet` = customer-funded tender path vs. `stored_value:issue_wallet_credit` = existing no-tender admin adjustment — B27 already seeded both, just clarifying which is which); reversal contract defined but UI deferred; `GIFT_CARD_RELOAD` and GL-liability-outbox dropped from scope (no reload transaction exists; GL posting belongs to B6). **Status set to READY_FOR_IMPLEMENTATION** (architecture clarification under already-APPROVED D007/D008, no new decision needed) — **implementation (migration/service/handlers/UI/tests) has NOT started yet.**

**B3 MIGRATION AUTHORED 2026-07-20 (uncommitted; migration 0412 STOP-AND-WAIT, not yet applied):** the locked architecture was revised twice this same session, both times *before* any code was written, in response to two separate external (ChatGPT) architecture reviews the owner relayed mid-session — each evaluated on its merits against the live remote schema, not adopted wholesale. **v2:** dropped the originally-planned `org_sv_fundings_mst` funding-header table — the BVM RECEIPT voucher this design already creates (`id`/`total_amount`/`voucher_status`/currency/reversal fields) already serves as the funding aggregate, since the whole flow is one synchronous Prisma transaction with no async gateway-callback state to track across transactions; only `org_sv_funding_tenders_dtl` (tender-leg fact table) is a new table now. Two other v2-review suggestions (re-adding `GIFT_CARD_RELOAD`, a mandatory GL-liability-posting event) were evaluated and rejected as scope creep against already-recorded reasoning. **v3:** a second review caught real gaps — added `tendered_amount`/`change_returned_amount`/canonical `payment_status` columns to the tender table; froze the "one voucher = one role/target/customer/currency" invariant explicitly; closed a real bypass path (generic manual voucher-entry UI already offers the `GIFT_CARD_SALE`/`WALLET_TOPUP`/`CUSTOMER_ADVANCE_RECEIPT` line roles — once the wiring handler is registered those roles must be hidden from that UI for v1, or anyone with voucher-entry permission could credit a wallet/gift-card/advance outside the governed service); added an up-front reject-if-not-COMPLETED-set payment-status gate (no async completion path ships in this package, so a CHECK/BANK_TRANSFER leg that would resolve `PENDING` must be rejected at request time, not silently accepted or silently credited); named the completion outbox event `STORED_VALUE_FUNDING_COMPLETED`; and corrected the Rollback line — disabling the feature flag must block the 3 real-money entry points, not silently resume the legacy tender-less direct-ledger writes (that would reopen the exact C3 gap this package exists to close). Two more v3 suggestions (ledger-primitive source-tagging refactor, composite tenant-qualified FKs) were evaluated and explicitly deferred/rejected with reasoning — see B03's own "Revision v3" note for the full per-point evaluation of both external reviews. **Migration `0412_b03_stored_value_funding_capture.sql` authored to the v3 design** — one new table (`org_sv_funding_tenders_dtl`) + back-link columns on `org_fin_voucher_trx_lines_dtl`/`org_cash_drawer_movements_dtl` + new `SV_FUNDING_TENDER` cash-drawer movement type + sparse idempotency index on `org_gift_card_txn_dtl`. Composite branch FK verified against the live `org_branches_mst` PK `(id, tenant_org_id)`; `current_tenant_id()` RLS helper confirmed to exist. **STOP-AND-WAIT for owner apply.**

**Migration 0412 APPLIED (owner, 2026-07-20), verified via remote DB.** Backend core IMPLEMENTED same session (uncommitted): `stored-value-funding.service.ts` (`fundStoredValue` + `finalizeStoredValueFundingIfReady`), both wiring handlers (`stored-value-funding-wiring.handler.ts`, `stored-value-cash-drawer-wiring.handler.ts`, registered in `voucher-wiring.service.ts` in the order that makes `line.sv_funding_tender_id` available to the cash-drawer handler), `voucher-wiring.ts` type extensions, `OUTBOX_EVENT_TYPES.STORED_VALUE_FUNDING_COMPLETED`, `topUpWalletTx`/`issueAdvanceTx` gained voucher-backlink params (+`issueAdvanceTx` gained `idempotencyKey`), `sellGiftCard` gained `idempotencyKey`, new `finalizeGiftCardSaleTx` for crediting an already-created unfunded (`GENERATED`) card. Prisma schema updated + regenerated. **`npx tsc --noEmit` clean (0 errors) project-wide.** Nothing calls `fundStoredValue()` yet — it is fully wired but not yet reachable from any UI or server action.

**Also done this session:** manual-voucher-entry bypass closed (`add-line-dialog.tsx` no longer offers the 3 funding roles in its `receipts` group — real risk the moment the wiring handler landed); feature flag `order_fin_sv_funding_capture` registered (`FeatureFlags` type + `FLAG_CATALOG`, independent, default OFF); reusable `StoredValueTenderFields` component (method picker + cash-tendered/change + cash-drawer session picker, reusing the existing generic `useCashDrawer` hook and the `checkout-options` API route already used by the order payment modals — no new API route needed); 3 new governed server actions (`sellGiftCardWithTenderAction`, `topUpWalletWithTenderAction`, `issueAdvanceWithTenderAction`), each gated by the flag + the existing permission code (`gift_cards:sell` / `stored_value:top_up_wallet` / `stored_value:issue_advance`); all 3 screens retrofitted (gift-card-sell-dialog.tsx, customer-stored-value-tab.tsx) — behind the flag they show the tender step and call the new actions, flag-off leaves the pre-existing no-tender behavior completely unchanged (verified: full jest green throughout, no regressions); i18n EN/AR added; 2 new reconciliation checks (`SV_FUNDING_TENDER_TOTAL_MATCH`, `SV_FUNDING_VOUCHER_LINK_EXISTS`) registered in `reconciliation.service.ts` — defense-in-depth for the finalizer's own tender-sum-equals-voucher-total guarantee. **31 new tests** across 3 suites (fundStoredValue idempotency/validation/payment-status-gate, finalizer multi-leg timing/currency-guard, both wiring handlers' replay-safety, the reconciliation check) — plus fixed 2 pre-existing suites (`reconciliation.service.test.ts`, `integration/reconciliation-run.test.ts`) whose Prisma mocks predated `org_sv_funding_tenders_dtl`. One real bug caught by the project's own B02 grep-guard test (no literal `'COMPLETED'` string allowed under `lib/services/reconciliation/`): fixed by adding a proper `SV_FUNDING_TENDER_STATUS` DB-mirror constant instead of hardcoding the literal, then applied the same cleanup to the 2 other B3 files that had the same literal. **Gates ALL GREEN: tsc clean, eslint clean (ran full-project, not just touched files, 0 problems), full jest 220/220 suites 2108/2108 tests zero known failures, check:i18n PASS, `npm run build` clean (exit 0, zero warnings, all routes including `/dashboard/marketing/gift-cards` and `/dashboard/customers/stored-value`).**

**▶ NEXT ACTION: Seq 6 is fully implemented (B3 committed; B30 + B32 uncommitted, gates green, migration 0415 APPLIED (owner, 2026-07-23) and verified) — Seq 7 (B9, B10) is next** per the master sequence below, under the 2026-07-17 continuation directive (continue until the program is finished). Remaining steps for B3/B30/B32 are the standing release-promotion ones, not more code: owner commits the batch → deploy to Preview → run QA_TEST_GUIDE.md's scenarios (B3 §14's 12 scenarios flag ON/OFF; B30/B32 §15 scenarios already added) → owner approval recorded → only then may B3/B30/B32 be marked VERIFIED. **Owner policy update (live edit to this folder's CLAUDE.md, first noticed mid-B4/B5/B31 work, confirmed by commit `f7dccb8a` during B7):** "No need for maker-checker in approve even same user can approve if he have the required permission" — relaxes the different-approver assumption in B16's already-shipped `approveSessionVariance`, and in the planned B09/B34/B30 approval designs; B27's new codes were seeded without adding a same-user restriction, consistent with this directive. B16's `approveSessionVariance` was not retroactively changed (only `order-refund.service.ts` has been, by the owner directly).

**How to proceed each package:** read the `Bxx` file fully → load domain skills BEFORE writing code (`/database` `/backend` `/frontend` `/i18n` `/multitenancy` `/implementation`) → implement full-cycle → run gates (tsc, eslint, jest, build, check:i18n) → update the Bxx file + README + this RESUME + memory + **[QA_TEST_GUIDE.md](QA_TEST_GUIDE.md)** (owner directive: this living manual-QA guide MUST be updated with new scenarios after every implemented package; **each scenario must explain HOW/WHERE — the sidebar path + URL + what to click**, not just abstract steps — see its §0.1 Screen map). **Migrations = author SQL then STOP-AND-WAIT** (mark APPLIED (owner, 2026-07-20), move to next workable package; owner applies + regenerates Prisma, then that package resumes). **Owner control points:** commits, migration applies, Preview-QA approvals.

**Environment notes:** `supabase_remote_db` MCP works for **read-only** discovery SQL (authoritative); the `supabase_local` MCP may disconnect/reconnect between sessions. Never apply migrations via MCP. Open decision for the owner: none.

---

## Where the program stands

| Item | State |
|---|---|
| Authoritative current-state report | frozen at `docs/Audit_Reports/CleanMateX_Enterprise_Financial_Accounting_Audit_15_07_2026/CleanMateX_Order_Payment_Authoritative_Current_Implementation_Report_2026-07-15.md` (incl. Addendum A1) — never modify except explicit addenda |
| Documentation status | **UNDER_REVIEW** — 12 decisions, 34 packages (B01–B34), master + Phase 0 indexes, capability bundles; these are active planning files, not frozen artifacts |
| Approved decisions | **ALL TWELVE APPROVED (Expert).** 2026-07-16: D002 (Option A v2: five-facet vocabulary, origin-only source registry), D003 (Expert model v2: commercial refunds never reopen by default; reversal/void/bounce/chargeback reopen via status change; refund-and-rebill explicit + permissioned; stored-value reversal reopens only via credits term), D004 (Option B), D005 (Option A), D010 (Option A). 2026-07-18 (owner's authoritative decision pack): D001 (canonical transition graph + terminal set + controlled override), D006 (reusable reversal; `credit_reversal_reopens_due = 0`; governed `LOYALTY_RESTORE_PENDING`), D007 (five-layer boundaries + matrix + outbox failure coupling), D008 (five-artifact funding capture; liability never revenue), D009 (governed fallback classification set + defaults), D011 (governed amendment/delta model), D012 (IFRS 15 obligation model; v1 validated-completion recognition; NO workflow-percentage revenue; accounting-owner sign-off = implementation control only). Approval type, selected option, rationale summary, implementation consequences, and affected packages recorded in every decision file |
| Open decisions | **NONE — all twelve decisions are APPROVED (Expert).** D001, D006–D009, D011, D012 finalized 2026-07-18 (owner's authoritative decision pack, governance correction pass); the D012 accounting-owner sign-off survives only as an implementation control for the entity-specific over-time adjustment, not as an open decision |
| Implementation authorization | **Owner continuation directive (recorded 2026-07-17):** continue all remaining planning AND implementation phases, following the master sequence, until the program is finished. This directive is the explicit authorization instrument. Each subsequent package start is announced against this RESUME + master index (the owner may redirect at any boundary). Hard owner control points remain at every package: migration apply (stop-and-wait), commits, Preview QA approval. **Overnight extension (owner, 2026-07-17 evening — "I am going to sleep and you can continue until finish all"):** keep implementing package-by-package unattended; commits, migration applies, and Preview QA remain the owner's (queued for morning); when a package hits its migration stop-point, author the SQL, mark the package APPLIED (owner, 2026-07-20), and continue with the next workable package; the `hard = predecessor VERIFIED` start-gate is deferred to implementation-order only during this window (VERIFIED still requires Preview QA before any production promotion — nothing ships to production unattended) |
| Remaining decisions at their waves | **Resolved 2026-07-18:** D001, D006–D009, D011, D012 are all `APPROVED (Expert)` with selected option, rationale summary, implementation consequences, and affected packages recorded in each decision file — no decision waves remain; packages proceed on implementation sequencing only |
| Next action | **B34 CLOSED (IMPLEMENTED 2026-07-18):** consolidated gates all green (eslint 0, jest 1946/1960 — the 14 fails are the 4 known pre-existing owner suites, build ✓, check:i18n ✓, tsc clean except the 2 known pre-existing owner-file errors); UI-access-contract wire checks PASS for `/dashboard/internal_fin/refunds` and `/dashboard/orders/[id]`; sync drift 0; platform inventories validate 10/10 (run contract checks from repo root with PowerShell — Git Bash mangles the route arg). **Baseline committed + tagged per owner directive 2026-07-18** (see the master README Next action + tag `order-fin-approved-baseline-2026-07-18`). **Dependency order recalculated 2026-07-18:** Seq 1–2 done; no decision gates anywhere; one scoped package per phase (owner directive 2026-07-18). **B15 IMPLEMENTED 2026-07-18** (same day, after the baseline tag — B15 changes are uncommitted in the working tree; see B15 Completion evidence; gates ALL GREEN: tsc clean/eslint 0/i18n ✓/guard suite 18-18/full jest 1971-1983 [12 fails = 3 known pre-existing owner suites: cash-drawer ×2 + order-calculation; the flaky credit-note-picker-focus passed]/build ✓). **Two owner corrections folded into B15 same day (see B15 evidence):** (1) canonical tax facts live in `org_order_taxes_dtl` — the item-recalc path resolves header `vat_rate` stamp → effective rate from tax lines (Σtax÷Σtaxable) → (2) **zero-rated with a warning log** when neither exists (owner policy: no tax setup = tenant doesn't use tax — legitimate zero, never an error, never an assumed rate; `MISSING_VAT_RATE` code + i18n removed). Pre-Preview data check is therefore **currency-only**: (a) tenants whose TENANT_CURRENCY setting resolves empty, (b) wallets/advances with NULL currency_code — run read-only before the owner's Preview deploy. **Currency pre-Preview checks DONE 2026-07-18 (this session):** run read-only against **remote** (authoritative, `supabase_remote_db` MCP) → **CLEAN** (3 active tenants / 0 empty currency; 2 wallets + 2 advances / 0 blank) + local clean; queries + results recorded in B15 Completion evidence. **B16 FULLY IMPLEMENTED 2026-07-18 (this session):** Part A — expected-cash filter (active + COMPLETED-set + cash-family) centralized in new `lib/services/cash-drawer-cash-facts.ts`, applied at every expected-cash site (`closeSession`/`buildSessionReconciliation`/`getSessionSummary`/`loadPaymentTotalsBySession` + list/detail entry points), gated by new flag `order_fin_drawer_close_v2` (default OFF = legacy aggregate; catalog + FeatureFlags type added; HQ seed = cleanmatexsaas follow-up). **Part B — deferred variance approval:** owner applied migration `0407_b16_drawer_variance_approval.sql` + regenerated Supabase types mid-session; `prisma/schema.prisma` updated to mirror the 5 new columns (generated types alone don't reach `prisma.*` calls) + `npx prisma generate` re-run. Owner chose the **close-then-approve (deferred)** model over block-close-until-approved, and directed it be **OPTIONAL and non-blocking / default not mandatory** (2026-07-18): the close always completes; approval is opt-in per drawer (`variance_approval_threshold` NULL default = no gate) and a supervisor *may* record it for the audit trail — never required, never blocking; EN/AR banner + dialog copy reframed to optional. Cashier closes regardless of variance; over-threshold closes are flagged eligible-for-optional-approval (`variance_threshold_snapshot` persisted) until a **different** supervisor optionally approves with a mandatory reason (`approveSessionVariance`, `VarianceApprovalError` w/ stable codes) — server-enforced maker≠checker, single-shot. New API `POST .../session/[sessionId]/approve-variance` (`requirePermission('cash_drawer:approve_variance')`, fail-closed until B27 seeds it), new dialog + pending/approved banners on the session-detail screen (`useHasPermissionCode` gate), EN/AR strings. Gates ALL GREEN: tsc clean / eslint 0 / check:i18n ✓ / jest 39-39 (9 cash-facts + 30 service, up from 30 after Part B tests; fixed the pre-existing `cash-drawer.service.test` fail via the missing movements `findMany` + `canAccess` mocks) / build ✓ (both passes, exit 0). **Addendum A2 added to the authoritative report (verified 2026-07-18):** drawer expected-cash DOUBLE-COUNTS cash sales (settlement writes BOTH a session-linked payment row AND a `CASH_SALE` movement, both summed) — OUT of B16 scope (a separate, pre-existing defect); owner is actively resolving the movement-folding model in `buildCashDrawerClosePreview` (its tests currently fail 17.5→19.5). **A2 RESOLVED 2026-07-18 by B35 (owner directive):** unified ONE expected-cash formula across `closeSession`/`buildSessionReconciliation`/list-detail loaders + preview — payment ledger owns sale cash; movement term counts only MANUAL movements (`order_payment_id IS NULL`), excluding sale-mirror `CASH_SALE`/change. **Owner also directed dropping the `order_fin_drawer_close_v2` flag** — the filter + A2 fix now apply UNCONDITIONALLY (flag removed from FeatureFlags type + FLAG_CATALOG + all service usages); B16 variance approval is gated only by the per-drawer `variance_approval_threshold` (opt-in). Gates green: tsc clean/eslint 0/jest 39-39/build ✓; the 2 previously-failing `cash-drawer-close-preview` tests now PASS (I fixed the preview fallback to `opening+payments`). B9 coord noted (cash-refund OUT movements must not be excluded by the order_payment_id rule). See B35 file. **B20 IMPLEMENTED 2026-07-18, committed at `7c019061`** (TAX_CALCULATION + DISCOUNT_VALIDATION wired via the B02 shared module + new REFUND_REOPEN_CONSISTENCY check/constant; `total_checked` 35→38; 3 new checks + 15 new tests; full reconciliation suite 64/64; gates all green — no migration, no UI, per its Delivery-surfaces NOT_APPLICABLE justification). **B29 IMPLEMENTED 2026-07-19** (stale-doc sweep — 7 files across ADR/Fix_29_05_2026/Opus_Validation_Report_18_06_2026/Order_Fin_Docs annotated with correction banners linking the frozen report + B01/B20; migration 0340 itself re-verified as NOT stale; full index in B29's Completion evidence; docs-only, no gates beyond link verification). **B4/B5/B31 IMPLEMENTED 2026-07-20** — one `collectPaymentTx` rewrite: BVM voucher wiring (createBizVoucher/addVoucherLine/postAndWireBizVoucher, closes H1's ORDER_PAYMENT_LINK_EXISTS gap), a top-level `org_idempotency_keys` replay/conflict guard with required `idempotencyKey` end-to-end (closes H5), and D9-aware status resolution shared with submit via the exported `resolveDefaultStatus` (closes M6) — collect's own overpayment engine deliberately untouched for parity. Ships **unconditionally, no feature flag** (owner decision, following the B35 precedent). Gates green: tsc clean/eslint 0/i18n ✓/targeted jest 51-51/full jest 2041-2049 [8 fails = pre-existing unrelated order-calculation mock gap]/build ✓. **B7 IMPLEMENTED 2026-07-20** — the financial outbox finally has a real consumer: fixed `claimBatch`'s concurrency gap (now uses the existing `claim_outbox_batch` SQL RPC, real `FOR UPDATE SKIP LOCKED`), added a genuine `DEAD_LETTERED` status, wired the already-written `order-history`/`processEarnPoints` consumers via a new handler registry (`outbox-processor.service.ts`), unscheduled the dead `outbox-worker` cron (root cause: a GUC that can never persist on Supabase's non-superuser role — the same bug 0355 already fixed for notifications) and registered a working `fin-outbox-processor` cron in its place, plus a new ops-visibility screen at `/dashboard/internal_fin/outbox`. **Migration 0410 is authored but APPLIED (owner, 2026-07-20)** — nothing in B7 is live until the owner applies it and copies the generated secret into `FINANCE_OUTBOX_SECRET`. Gates green: tsc clean/eslint 0/i18n ✓/targeted jest 26-26/full jest 2060-2068 [8 fails = same pre-existing order-calculation gap]/build ✓/sync:ui-access-contract PASS (142/142, drift 0). Incidentally fixed a stale `refund.service.test.ts` assertion left behind by the owner's own same-day commit removing refund self-approval blocking. **B27 IMPLEMENTED 2026-07-20** — re-verified §43 against the live remote DB (5 of 12 NOT_FOUND rows already covered; real gap was 7 codes), seeded `cash_drawer:approve_variance`/`stored_value:issue_wallet_credit`/`orders:rebill_authorize` + 4 reserved placeholders, fixed the `pricing:override` fail-open bug in `addOrderItems` (skipped-check-on-unresolved-user + swallowed-permission-error, both now fail closed), gated 3 previously completely-ungated stored-value server actions, and wired REFUND_AND_REBILL to a real permission check in both refund routes + the `initiateOrderRefund` action (was hardcoded-rejected since B01 shipped). **Migration 0411 is authored but APPLIED (owner, 2026-07-20).** Incidentally fixed 8 pre-existing unrelated `order-calculation.service.test.ts` failures (missing `evaluateBestAutoApplyPromo` mock, baseline gap since commit `2413671b`) — **full jest is now 217/217 suites, 2084/2084 tests, zero known failures**, the first fully-green full-suite run this session. Gates: tsc clean/eslint 0/i18n ✓/targeted jest 16-16/full jest 2084-2084/build ✓. Full detail in B27's own Completion evidence. Seq 5 now ✅ CLOSED (B7 + B27). **NOW = Seq 6 (B3, B30, B32).** Per package: read the Bxx file fully, load domain skills before code, evidence + statuses after every task; migrations author → **STOP-AND-WAIT** (mark APPLIED (owner, 2026-07-20) once confirmed). Owner queue: commit + Preview deploy → QA (B01 §14; B02 equality; B33 D9-PENDING stays CURRENT; B34 flag-on QA; B15 currency; B16 filtered-vs-physical staging parity + deferred-approval scenario — seed/grant `cash_drawer:approve_variance` (seeded by B27, migration 0411 applied and verified) first; B20 fixture-drift scenarios; B4/B5/B31 collection-vs-submit parity + idempotency replay/conflict + PENDING-status scenarios; B27 permission-denied/granted scenarios) → approvals → VERIFIED. Known pre-existing failures outside the program (owner keypad/split-payment/promo commits): credit-note-picker-focus (flaky, unrelated); order-calculation mock gap is now FIXED (B27); the `cash-drawer.service` ×2 and `cash-drawer-close-preview` ×2 fails are now FIXED by B16/B35; tsc clean this session. **B3 IMPLEMENTED and committed (`98a63f93`) 2026-07-20/21** (see B03 Completion evidence — full-cycle stored-value funding capture, migration 0412 applied and verified). **B30 + B32 IMPLEMENTED 2026-07-23 (uncommitted)** — B30's `payment-transition.service.ts` covers VERIFY/CANCEL/FAIL_BOUNCE with D009 fallback classification + D010 idempotency, cross-order worklist screen/API, and per-order table Cancel/Fail-Bounce buttons; **migration `0415_b30_b32_payment_transitions_and_permissions.sql` APPLIED (owner, 2026-07-23) to local + remote** (seeds the 3 permission codes B27's own §43 audit assumed but never actually seeded — re-verified against migration 0411) — **post-apply verification via remote DB confirmed:** all 8 audit/fallback columns present on `org_order_payments_dtl`, all 3 permissions active with correct role grants (`orders:cancel_payment`/`orders:fail_payment` → super_admin/tenant_admin/admin/branch_manager; `orders:pending_payments_view` → those 4 + finance_manager), `billing_pending_payments` nav row present with `parent_comp_id` resolved, both `chk_ord_pay_fallback_classification` and the updated `chk_history_action_type` CHECK constraints present. B32's `cashDrawerWiringHandler.canHandle` status gate (M7) ships in the same session; `allow_status_override` (M8) was investigated and deliberately left unenforced against the checkout-time override to avoid regressing a pre-existing, shipped BVM Phase 6 Sub-item 6 test — full reasoning in B32's Completion evidence. Gates ALL GREEN: tsc clean (2 pre-existing unrelated errors untouched) / eslint 0 / full jest 222/222 suites, 2135/2135 tests, zero known failures / build ✓ / check:i18n ✓ / check:ui-access-contract --wire PASS / sync:ui-access-contract PASS (144/144, drift 0). Seq 6 now ✅ CLOSED (B3 + B30 + B32). **NOW = Seq 7 (B9, B10).** Owner queue: commit the Seq 6 batch (migration already applied) → Preview deploy → QA (B30/B32 §15 scenarios already added to QA_TEST_GUIDE.md) → approvals → VERIFIED |

## Standing rules that bind every session (do not re-negotiate)

1. **Folder [CLAUDE.md](CLAUDE.md)** governs all planning files; project root CLAUDE.md governs code.
2. **Skills before code:** `/database` (SQL/migrations), `/backend` (services/APIs), `/frontend` + `/i18n` (UI), `/multitenancy` (org_* queries), `/implementation` (features). Load BEFORE the first line of that domain.
3. **Migrations:** create the SQL file with the next sequence in `supabase/migrations/` (check at that moment; ≥0402 expected), then **STOP and wait for the owner to apply it** before continuing. Never apply via MCP/CLI. New migrations only — never modify existing ones. TEXT not VARCHAR. Every org_* query filters `tenant_org_id`.
4. **Gates before "done" per package:** `npx eslint . --quiet` (web-admin), `npx tsc --noEmit`, targeted + full jest, `npm run build`, `npm run check:i18n` when translations touched. Fix until green; report honestly.
5. **Full-cycle completeness (owner directive):** every capability ships backend + API + frontend screens + permissions + i18n/RTL + audit, usable end-to-end. Backend-without-UI = PARTIAL.
6. **No silent money mutation** (CRITICAL RULE 15) and the frozen layer rule: business event ≠ BVM voucher ≠ operational fact ≠ drawer movement ≠ GL journal. D003 v2 extends this: a normal refund never silently reopens a customer's due.
7. Update package status + Completion evidence + this RESUME + the master index at every package boundary; the owner commits (do not commit unless asked).
   **Release promotion (owner rule, 2026-07-16): commit → deploy to Preview → QA executes the package checklist on Preview → QA finished + owner approval recorded → only then promote to production.** No direct-to-production; VERIFIED and production flag activation both require the recorded Preview QA approval; rollback proven on Preview first.
8. Safety gates: production UI activation never exposes an unsafe backend — per-package Safety blocks are binding. **B34: production activation of any refund workflow only after B01 AND B02 VERIFIED; cash/original-method refunds only after B09 VERIFIED; permission-sensitive actions (rebill, manual exception) gated by the applicable B27 codes. B34 must not be built or activated against the pre-B1 API.**
9. **Progress after every task:** update plan progress + statuses (Bxx file, master index, this RESUME) after each task, not only at package boundaries.
10. **Docs after every package:** once a package finishes implementation + Preview QA, update/create/refresh all related documentation (feature docs, STATUS, guides — `/documentation` skill) before marking VERIFIED.
11. **Project CLAUDE.md always binds** (`F:\jhapp\cleanmatex\CLAUDE.md`): required skills loaded per domain before code, agents per task type, referenced rule docs read; Cmx reusables first — create a reusable component when an element will be used in 2+ places. Best practices throughout: no gaps, no bugs, UI/UX best practices, production-ready only.
12. **B26 is a planning umbrella only** — no implementation of any kind under the B26 ID until its FX, gateway settlement, chargeback, bank reconciliation, safe/deposit, and ECL areas are split into separate implementation packages.

## Implementation order (from the master index — follow under the 2026-07-17 continuation directive unless the owner redirects)

```text
NOW    → B1 (per B01 §15 rollout: migration [STOP-AND-WAIT] → service → tests §14 → verify)
Seq 2  → B2 → B33;  B34 design/flagged implementation (activation per Safety block:
         B01+B02 VERIFIED; cash/original-method after B09; B27 for permission-sensitive actions)
Seq 3  → B15, B16, B20(design), B29
Seq 4  ✅ DONE 2026-07-20 → B4 → B5, B31 IMPLEMENTED
Seq 5  ✅ CLOSED 2026-07-20 → B7 ✅ IMPLEMENTED (mig 0410 APPLIED (owner, 2026-07-20)) → B27 ✅
       IMPLEMENTED (mig 0411 APPLIED (owner, 2026-07-20))
Seq 6  ✅ CLOSED 2026-07-23 → B3 ✅ IMPLEMENTED (committed 98a63f93) → B30 + B32 ✅
       IMPLEMENTED (uncommitted; mig 0415 APPLIED owner 2026-07-23, local+remote, verified)
Seq 7  ← NOW → B9, B10
Seq 8  → B6 → B13              Seq 9 → B8, B19, B21, B22
Seq 10 → B11, B17, B18         Seq 11 → B12, B14
Seq 12 → B23, B24              Seq 13 → B25, B26 (umbrella — split first)
Cont.  → B28 slice per wave (a wave is not VERIFIED without it)
Decision waves: RESOLVED 2026-07-18 — all twelve decisions APPROVED (Expert); the former
wave gates (D001+D009→B30, D007+D008→B3/B6, D006→B13, D011→B12, D012→B25) are satisfied
and those packages proceed on implementation sequencing alone.
```

## B1 implementation checklist (first work in the new session, per the 2026-07-17 continuation directive)

1. Load `/database` + `/backend`; read [B01](B01_Refund_Lineage_And_Reopen_Due.md) fully (it is the spec; the APPROVED (Expert) v2 decisions are binding).
2. ✅ DONE 2026-07-17 — Migration `0404_b01_refund_lineage_and_context.sql` (rev 2 — table verified/owner-confirmed EMPTY in all environments, so all constraints are **unconditional**, no cutover): `original_credit_app_id` promoted (defensive copy-only backfill, same-tenant resolution guard) + composite FK + cap index; `refund_context` TEXT **NOT NULL** + registry CHECK; v2 origin-only source-registry CHECK (old CHECK replaced); D003-invariant-7 CHECK (positive reopen ⇒ REFUND_AND_REBILL/MANUAL_EXCEPTION); source↔lineage XOR CHECK; payment FK aligned tenant-composite; reopen-bounds CHECK pre-existed (0340, ensured); `PENDING_APPROVAL` added to the status CHECK (defect fix — see B01 §1a). **STOPPED → owner applies 0404 before step 3.**
3. Service: `initiateRefund` classification (D002 v2 map incl. CUSTOMER_CREDIT_RESTORE / GOODWILL_CONCESSION, lineage XOR validation, required route key, required `refundContext`), `processRefund` reopen per **D003 v2** (0 for all commercial paths; positive only for REFUND_AND_REBILL [rejected until the B27 code exists] and MANUAL_EXCEPTION; CANCELLATION_UNWIND from cancel unwind), wallet destination idempotency key `refund-${refundId}-wallet`, `classifyRefunds` column-first already exists.
4. Tests: full §14 matrix (18 scenarios, v2 expectations) + regression suite; gates green.
5. Update B01 Completion evidence + statuses + this RESUME; owner commits → **Preview deployment → QA runs the §14 scenario checklist on Preview → owner approval recorded** → production promotion; mark VERIFIED only when §16 criteria all hold AND the Preview QA approval is recorded.

## Memory pointer

Persistent memory `project_order_fin_remediation_wp_program.md` mirrors this state (auto-recalled in new sessions). If memory and this file disagree, **this file wins** — update memory.
