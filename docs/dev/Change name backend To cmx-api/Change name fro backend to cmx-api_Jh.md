
Change name fro backend to cmx-api:



you do this Next steps for you:
- From repo root run npm install (or cd cmx-api && npm install) so workspace deps are installed.
- Run cd cmx-api && npm run build to confirm the NestJS app builds.

- First time running OpenAPI contract test: cd cmx-api && npm run test:openapi to create the snapshot (commit the generated snapshot file).

I choose cmx-api, actually its empty until now, so plan to create it and plan to perfectly, completely, fully Update documentation, READMEs, diagrams, and any infra-as-code (e.g. Docker Compose, k8s manifests) and any document that reference backend.
